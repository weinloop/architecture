# MongoDB · 考点速记（易背版）

> 基于《14-MongoDB》整理。**重点**，架构近年常考；文档型数据库，记录为「字段-值」文档，类似 JSON。各节 **案例（航空运营智能管理平台）** 与《考纲/航空运营智能管理平台项目素材-核心.md》第 3、5、6 节（**数据服务、多终端采集、日志与运营数据、高可用目标**）及「嵌套、半结构化」业务数据可对齐。

---

## 一、章节定位与考法

| 项目 | 内容 |
|------|------|
| **章节** | 下篇 - MongoDB（重点 ★★★★★） |
| **特点** | 记录即**文档**，由**字段和值对**组成；类似 **JSON**；字段值可包含**其他文档、数组、文档数组**；嵌入式文档与数组**减少连接**需求 |
| **考法** | **BSON vs JSON**、**GeoJSON**、**分片策略**（哈希/范围）、**副本集**（Primary/Secondary、oplog） |

**案例（航空运营智能管理平台）**：适合存放 **行程单嵌套结构、行李与航段数组、旅客偏好标签、非固定字段的运维/埋点 JSON** 等；**票务资金与强一致库存** 仍以关系库/事务方案为主，MongoDB 多作 **内容型、日志型、分析型** 补充，与 **Elasticsearch、关系型库** 在检索与事务上分工。

---

## 二、文档模型要点

- **文档**：一条记录 = 一个文档，结构为「字段-值」对，可嵌套文档与数组。
- **优势**：与编程语言中对象类型对应自然；**嵌入式文档和数组**减少多表连接，适合半结构化、嵌套数据。

**案例（航空运营智能管理平台）**：一条文档内嵌入 **多航段、联系人、特殊服务请求**；**投诉工单** 附件与流转记录用数组追加，避免频繁多表 JOIN。

---

## 三、数据类型（★★★★★）

### 3.1 BSON（Binary JSON）

- **定义**：**二进制**形式的类 JSON 存储格式；支持内嵌文档和数组，并扩展了 JSON 没有的类型（如 **Date、BinData** 等）。
- **与 JSON 对比**：

| 对比项 | BSON | JSON |
|--------|------|------|
| **数据表示** | **二进制** | **纯文本** |
| **类型支持** | 更多原生类型（日期、整型、浮点、**二进制**等） | 类型相对有限 |
| **存储与速度** | **更紧凑、读写更快**；体积可能略大 | 相对不紧凑、较慢 |
| **易读性** | 需专门工具查看 | **人类可读**，便于调试与日志 |
| **适用场景** | **数据库存储**，要求读写速度、复杂结构及扩展类型 | **网络传输**、开发调试 |
| **压缩** | 紧凑，压缩优化不如文本明显 | 文本压缩算法优化效果更好 |

**口诀**：BSON 二进制快而紧凑、类型多；JSON 文本可读、宜传输与调试。

**案例（航空运营智能管理平台）**：API 层常用 **JSON** 交互；落库用 **BSON** 存 **Date（起飞时刻）、BinData（附件摘要）** 等，兼顾 **多终端** 序列化与存储效率。

---

### 3.2 GeoJSON（★★★★★）

- **定义**：描述**地理数据**的开放标准格式，**非 MongoDB 独有**；MongoDB **原生支持** GeoJSON，可存储与查询地理空间数据。
- **用途**：地理空间**索引与查询**，实现**位置服务、位置分析**等（如附近的人、范围查询）。

**案例（航空运营智能管理平台）**：**机场坐标、航线走廊、起降区域** 可建 **2dsphere** 索引；支持「**某机场半径内服务网点**」「**航线附近天气兴趣点**」等（答题写 **GeoJSON Point/LineString** 即可）。

---

## 四、分片集群（★★★★★）

- **目的**：解决**海量数据**存储与读写扩展——单机磁盘、CPU、内存、网卡等瓶颈；通过**分片**将数据分布到多节点。
- **两种分片策略**：

| 分片类型 | 原理 | 适用场景 |
|----------|------|----------|
| **哈希分片** | 对**片键**字段做**哈希**，用哈希值在分片间划分数据 | **随机访问**为主，无明确范围；数据均匀分布 |
| **范围分片** | 按片键的**连续范围**划分，同一范围的数据在同一分片 | 数据**按某属性连续增长**（如**时间顺序**）；**范围查询**高效；片键选择不当会导致热点与性能下降 |

**记忆**：哈希→随机、均匀；范围→连续、范围查询（如时间）。

**示意**：
```
  哈希分片：  key → hash(key) → 分片索引 → 落在某分片
  范围分片：  key 在 [A,B) → 分片1；[B,C) → 分片2；...
```

**案例（航空运营智能管理平台）**：**哈希分片**：按 **旅客 ID、设备编号** 分散读写，缓和热点；**范围分片**：**按日期/航班日** 存运行日志、埋点，便于 **时间范围查询与归档**（与素材 **日均 800GB 级实时数据、日志留存** 叙事可衔接，具体量级答题不必写实）。

---

## 五、高可用：副本集（Replica Set）（★★★★★)

- **组成**：一组 MongoDB 实例，包含 **1 个 Primary** 和 **多个 Secondary**。
- **读写**：**客户端（Driver）写只到 Primary**；Secondary 从 Primary **同步数据**，使副本集内数据一致，提供**高可用**。
- **同步机制**：通过 **oplog**（类似 MySQL 的 **binlog**）。Primary 完成写操作后，向 **local.oplog.rs** 集合写入一条 oplog；Secondary **持续从 Primary 拉取并应用** oplog，保持与 Primary 数据一致。

**示意**：
```
  客户端  →  写  →  Primary  →  oplog  →  Secondary 1
                        ↓                    Secondary 2
                  local.oplog.rs
```

**要点**：一主多从、写主读可从从、oplog 同步、local.oplog.rs。

**案例（航空运营智能管理平台）**：**Primary** 承接写入；**Secondary** 支撑 **报表读、备份**；故障自动选主支撑素材 **≥99.99% 可用** 目标中与 **文档类非核心路径** 结合的叙述（与关系库主从类比作答）。

---

## 六、速记清单（考前扫一眼）

1. **MongoDB**：文档 = 字段值对，类似 JSON；可嵌套文档与数组；嵌入式结构减少连接。
2. **BSON**：二进制 JSON；类型更多（Date、BinData）；更紧凑、更快；库存；JSON 文本、可读、宜传输与调试。
3. **GeoJSON**：地理数据标准格式；MongoDB 原生支持；用于位置服务、空间查询。
4. **分片**：哈希分片—随机、均匀；范围分片—连续、范围查询（如时间）；片键选择影响性能与热点。
5. **副本集**：Primary + 多个 Secondary；写只到 Primary；oplog（local.oplog.rs）同步，类似 MySQL binlog。

---

*文档由《14-MongoDB》提炼，保留必要表格与图形含义，便于背诵与考场回忆。*

*案例表述依据《考纲/航空运营智能管理平台项目素材-核心.md》；论文可裁剪，常与 **嵌套行程与工单、GeoJSON 机场航线、哈希/范围分片、副本集高可用、与票务强一致事务分工** 等关键词结合。*
