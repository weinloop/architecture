# 知识点汇总

> 由原《101 综合知识汇总》与《101 案例汇总》按主题重组合并；已对较长相同表述做行级去重，两篇互补内容并排保留。


## 软件工程概述

---

## 一、软件工程定义与 PDCA（★★★☆☆）

- **软件工程**：由**方法、工具和过程**三部分组成。
- **PDCA 循环**（选择题考过）：
  - **P（Plan）**：软件规格说明，规定功能及运行限制。
  - **D（Do）**：软件开发，开发出满足规格的软件。
  - **C（Check）**：软件确认，确认满足用户需求。
  - **A（Action）**：软件演进，运行中不断改进。

## 二、软件开发方法分类（★★★★★）

| 分类方法 | 具体方法 | 描述 |
|----------|----------|------|
| 开发风格 | 自顶向下 | 先定义整体结构，再逐步细化模块与功能 |
| 开发风格 | 自底向上 | 从具体模块开始逐步构建整个系统 |
| 性质 | 形式化方法 | 坚实数学基础，严格规格与验证 |
| 性质 | 非形式化方法 | 不把严格性作为主要着眼点 |
| 适应范围 | 整体性方法 | 适用于软件开发全过程 |
| 适应范围 | 局部性方法 | 适用于某个具体阶段 |

## 三、形式化方法与净室（★★★☆☆）

### 3.1 形式化方法

- **含义**：用数学模型与形式化语言描述需求/设计/行为，通过逻辑推理或模型检测获得**可证明的正确性**；常用于安全攸关、高可靠性系统。
- **特点**：① 正确性验证难且耗时；② 仍需与传统测试结合（形式化提供理论保证，测试发现运行时错误）。

### 3.2 净室软件工程（CSE）

- **定位**：一种**形式化方法**，不是测试方法、管理模型或单纯过程模型；强调在**设计与规约阶段**用数学与统计理论消除错误。
- **理论基础**：**函数理论**（程序=输入到输出的严格映射；完备性、一致性、正确性）、**抽样理论**（抽样测试推断系统可靠性）。
- **局限**：验证依赖严格数学，门槛高；不依赖传统单元测试但仍需考虑底层环境；仍属传统软件工程体系。

## 四、逆向工程与再工程（★★★☆☆）

| 概念 | 说人话 | 定义 |
|------|--------|------|
| **逆向工程** | 从低层（代码）推回高层设计 | 分析程序，在比源代码更高抽象层次建立程序表示 |
| **重构** | 同一抽象层改结构 | 同一抽象级别上转换系统描述形式 |
| **设计恢复** | 借助工具抽取设计信息 | 从已有程序抽象出数据设计、总体结构、过程设计等 |
| **再工程** | 逆向+新需求+正向工程 | 在逆向信息基础上修改/重构产生新版本 |
| **正向工程** | 从设计到代码 | 利用逆向得到的信息改变或重构系统 |

**抽象层次（低→高）**：实现级（语法树、符号表）→ 结构级（调用图、结构图）→ 功能级（程序段功能及关系）→ 领域级（与应用领域概念对应）。

## 五、基于构件的软件工程 CBSE（★★★★★）

### 5.1 构件的定义（萨默维尔）

| 构件特性 | 描述 |
|----------|------|
| 可组装性 | 外部交互通过公开接口；提供对自身信息的外部访问 |
| 可部署性 | 自包含，独立实体在构件平台上运行；通常二进制、无须部署前编译 |
| 文档化 | 完全文档化，接口语法甚至语义需定义 |
| 独立性 | 可无其他特殊构件下组装部署；需外部服务时在“请求”接口显式声明 |
| 标准化 | 符合标准化构件模型（接口、元数据、文档、组成、部署） |
| 可见性 | 无外部可见状态（特殊情况：容器管理状态、不影响功能的属性可可见） |

### 5.2 构件分类（按外部形态）

| 类型 | 内容 | 例子 |
|------|------|------|
| 独立而成熟的构件 | 成熟度高，完全隐藏接口，规定命令使用 | 数据库管理系统、操作系统 |
| 有限制的构件 | 提供接口与使用条件；可能资源冲突，需测试 | 基础类库 |
| 适应性构件 | 处理不兼容、资源冲突，可直接多环境使用 | ActiveX |
| 装配的构件 | 已装配在 OS/DBMS/信息系统层次，胶水代码连接 | 成品软件产品 |
| 可修改的构件 | 可改错、增功能，包装或写接口实现版本替换 | 功能模块 |

### 5.3 构件组装类型

| 组装类型 | 描述 | 特点 |
|----------|------|------|
| **顺序组装** | 按顺序调用已有构件创造新构件；先 A 再 B | 构件间不相互调用，需胶水代码保证接口兼容 |
| **层次组装** | 一构件直接调用另一构件服务；“提供”与“请求”接口需兼容 | 接口匹配无需额外代码，不匹配需转换；构件为网络服务时可能不适用 |
| **叠加组装** | 多构件叠加，新构件接口为原接口组合，分别调用 | 原构件互不依赖、不互相调用 |

**接口不兼容**：参数不兼容（操作名同、参数类型/数目不同）、操作不兼容（操作名不同）、操作不完备（提供/请求接口为对方子集）。

## 六、软件过程/生命周期模型（★★★★★）

- **软件过程 = 软件开发生命周期模型**（同义词）。
- **前提与模型**：需求完全确定→瀑布；仅基本需求→喷泉、螺旋、RUP、敏捷等；形式化为基础→变换模型。

### 6.1 瀑布模型

- **阶段**：需求分析与定义→系统与软件设计→实现与单元测试→集成与系统测试→运行维护。
- **缺点**：依赖早期需求调查，**不能适应需求变化**；需求完整性、正确性难确定。

### 6.2 原型/演化/快速原型模型

- 软考中**不做区分**，当做一个；适用于需求难完整定义，基于快速原型、用户反馈迭代直至最终产品。
- **特点**：迭代；功能开发后可及时验证需求；过早接触不稳定功能可能带来负面影响。

### 6.3 螺旋模型

- **螺旋模型 = 快速原型 + 风险分析**（选择题常考）。
- 适用于庞大、复杂、高风险系统；支持需求动态变化；迭代过多会增加成本、延迟提交。

### 6.4 喷泉模型

- 以**用户需求为动力、对象为驱动**；各阶段**相互重叠、多次反复**，可交互进行，可随时补充遗漏。

### 6.5 变换模型

- 基于**形式化规格说明语言和程序变换**；需严格数学理论和整套开发环境支持。

### 6.6 V 模型与测试依据

| 测试类型 | 测试对象 | 依据（文档） |
|----------|----------|--------------|
| 单元测试 | 模块/类 | **详细设计说明书** |
| 集成测试 | 模块间接口、已集成软件 | **概要设计文档** |
| 系统测试 | 完整集成系统 | **需求分析文档/开发合同** |
| 验收测试 | 准备交付的完整系统 | **用户需求** |

**注意**：概要设计是**集成测试**的依据，不是系统测试；系统测试依据是需求分析/合同；验收测试依据是用户需求。

### 6.7 增量模型 vs 原型/迭代

- **增量**：按模块完成顺序逐步实现，每次增量可独立可操作；**模块叠加、线性扩展**。
- **迭代**：分阶段循环（需求→设计→开发→测试）**逐步完善同一功能**，每次交付完整版本；**功能深度优化、螺旋上升**。
- 增量更早让客户使用并获得价值。

## 七、统一过程 RUP（★★★★★）

- **RUP**：用例驱动、以**体系结构为中心**、**迭代与增量**的软件开发过程；采用“4+1”视图描述体系结构（与后续架构 4+1 视图区分）。
- **九大核心工作流**（系分考过，必须背）：
  - **建需分食测**：建（业务建模）需（需求）分（分析与设计）食（实现）测（测试）。
  - **部配变项环**：部（部署）配变（配置与变更管理）项（项目管理）环（环境）。

## 八、敏捷方法（★★★★★）

### 8.1 敏捷宣言

- 个体和交互 **胜过** 过程和工具；可工作的软件 **胜过** 大量文档；客户合作 **胜过** 合同谈判；**响应变化** **胜过** 遵循计划。

### 8.2 适用场景

- 规模较小的项目；需求初步萌芽、尚未明确、**快速改变**。高关键性/可靠性/安全性要求可能不完全适合。

### 8.3 主要敏捷方法

| 方法 | 简介 |
|------|------|
| **极限编程 XP** | 轻量、短周期、频繁发布；交流、朴素、反馈、勇气；YAGNI、DRY |
| **水晶系列** | “机动性”方法，以人为中心 |
| **Scrum** | 侧重项目管理的迭代增量框架；团队协作、响应变化、商业价值 |
| **FDD** | 迭代、人/过程/技术三要素 |

### 8.4 Scrum（★★★☆☆）

- **角色**：Product Owner（业务价值、Backlog 优先级）、Scrum Master（流程推动、清障、赋能）、Development Team（跨职能、自组织、交付 Sprint 目标）。
- **活动**：Sprint Planning（Sprint Backlog、目标）、Daily Scrum（昨日/今日/障碍）、Sprint Review（演示、反馈）。
- **工件**：Product Backlog（产品待办）、Sprint Backlog（冲刺待办）、Increment（可交付增量）。

## 九、软件过程管理：CMM / CMMI（★★★☆☆）

### 9.1 CMM 五级

| 级别 | 含义 |
|------|------|
| 初始级 | 过程缺乏结构化，依赖个人经验 |
| 可重复级 | 能定义和文档化过程，确保项目成功 |
| 已定义级 | 过程标准化、文档化 |
| 已管理级 | 监控、量化过程，持续改进 |
| 优化级 | 动态调整过程适应不同项目 |

### 9.2 CMMI 五级与知识体系

- **等级**：初始级、已管理级、已定义级、**量化管理级**、优化级。
- **四大知识体系**：系统工程（SE）、软件工程（SW）、集成产品与流程开发（IPPD）、供应商采购（SS）。

## 需求工程

---

## 一、需求开发与需求管理总览

- **需求开发**四阶段：**获分归雁**——获（获取）分（分析）归（需求规格说明书/定义）雁（验证）。
- **需求管理**：**基变更**——基（基线）变（变更）更（跟踪）。

## 二、需求获取方法（★★★☆☆）

| 方法 | 要点 |
|------|------|
| 用户访谈 | 结构化/非结构化；准备—访谈—后续；灵活，对经验与沟通要求高 |
| 问卷调查 | 高效、低成本、可匿名；缺乏灵活性、返还率低 |
| 采样 | 样本大小 = α×(可信度系数/可接受错误)²，α 常取 0.25 |
| 情节串联板 | 图片讲故事；被动/主动/交互式 |
| 联合需求计划 JRP | 集会形式，开发与客户深入合作 |
| 收集资料 / 获取文档 / 参加业务实践 | 查阅文献、索要文档、参与业务流程 |

## 三、结构化分析方法 SA（★★★★★）

### 3.1 分析模型结构

- **核心**：数据字典；围绕其有三层——**数据模型（ER 图）**、**功能模型（DFD）**、**行为模型（状态转换图）**。

### 3.2 数据流图 DFD 基本符号

| 符号 | 表示内容 | 图形 |
|------|----------|------|
| 数据流 | 有名字和流向的数据 | 标名箭头 |
| 加工 | 对数据流的变换 | 圆圈 |
| 数据存储 | 可访问的存储信息 | 直线段 |
| 外部实体 | 系统外的数据来源/去向 | 标名方框 |

**补充**：数据存储被外部实体使用**必须经过加工**（真题曾考）。

**扩充符号**：**与***（所有输入到齐才加工/同时产生所有输出）、**或**+（任一输入即可/至少产生一个输出）、**异或**⊕（仅一个输入/仅产生其中一个输出）。

### 3.3 DFD 平衡原则（★★★★★）

- **父图与子图平衡**：数据流**个数一致**、**方向一致**。
- **子图内部平衡**：
  - **黑洞**：加工只有输入没有输出。
  - **奇迹**：加工只有输出没有输入。
  - **灰洞**：输入不足以产生应有输出（挂羊头卖狗肉）。
  - **数据存储**：正常应既有读又有写；某子图可只有读或只有写。

### 3.4 状态转换图与数据字典

- 状态转换图：初态（黑点）、终态（黑点外圈）、中间状态（圆角四边形）；软考多不考画法，已由 UML 状态图替代。
- 数据字典：对 DFD 中所有命名元素定义，与 DFD 配合完整描述系统逻辑模型。

## 四、面向对象分析 OOA（★★★★★）

### 4.1 三种模型

- **功能模型**：系统“做什么”；用例图/数据流图。
- **动态模型**：时间维行为；状态图、顺序图等。
- **对象模型**：类、属性、关系；**最基础、最核心**，为功能与动态模型提供结构基础。

### 4.2 用例模型

- **四阶段**：**人合细条**——人（识别参与者）合（合并需求获得用例）细（细化用例描述）条（调整用例模型）。

**用例图元素**：参与者、用例、**通信关联**（参与者与用例之间，横线/箭头）。**包含、扩展、泛化**作用于**用例与用例**之间。

| 关系 | 描述 | 构造型 | 箭头指向 |
|------|------|--------|----------|
| **包含** | 提取公共行为，基本用例→抽象用例 | `<<include>>` | 指向抽象用例 |
| **扩展** | 基本用例 + 多个扩展用例（不同场景） | `<<extend>>` | 指向基本用例 |
| **泛化** | 子用例继承父用例 | — | 指向父用例 |

**参与者**：人、**其他系统**、**硬件设备**、**时钟**；一定在系统之外。参与者之间也可有泛化。

### 4.3 类图六关系（★★★★★）

**强弱顺序**：泛化=实现 > 组合 > 聚合 > 关联 > 依赖。

| 关系 | 定义 | 特点 |
|------|------|------|
| **关联** | 明确、稳定的语义联系 | 强语义、结构稳定 |
| **依赖** | B 变化可能引起 A 变化；A 对 B 临时使用 | 临时、偶然 |
| **泛化** | 父类与子类，子继承父并可扩展 | is-a |
| **聚合** | 整体与部分；部分可属多整体，生命周期可不同 | 好聚好散 |
| **组合** | 整体与部分；部分唯一归属，生命周期相同 | 与聚合比，生命周期同步 |
| **实现** | 类实现接口 | 接口定行为，类实现 |

## 五、UML 与 4+1 视图（★★★★★）

### 5.1 UML 结构

- **构造块、规则、公共机制**。公共机制：详述、修饰、通用划分（类/对象）、扩展机制（构造型、标记值、约束）。

### 5.2 图的分类

- **结构图（静态）**：类图、对象图、包图、组合结构图、构件图、部署图、轮廓图。
- **行为图**：用例图、活动图、状态机图、顺序图、通信图、时间图、交互概览图。**用例图在 UML 2.x 下为行为图**。

### 5.3 4+1 视图（= RUP 4+1）

| 视图 | 描述 | 关注角色 |
|------|------|----------|
| **逻辑视图** | 功能模块及关系 | 最终用户 |
| **进程视图** | 线程/进程、并发与同步 | 系统集成人员 |
| **实现视图** | 代码/构件组织 | 编程人员 |
| **部署视图** | 软件到硬件的映射 | 系统工程人员 |
| **用例视图** | 用例与参与者，功能需求 | 所有人 |

### 5.4 顺序图与活动图（案例常考）

| 图 | 定义 | 核心概念 |
|------|------|----------|
| **顺序图** | 对象间消息按时间顺序的交互 | 对象、生命线、执行发生、消息、交互片段（选择/循环/并行） |
| **活动图** | 控制流与数据流，方法/用例内部行为 | 活动、组合活动、对象流、转移/分支、并发/同步、分区 |

### 5.5 UML 四类关系

- **依赖、关联、泛化、实现**；包含/扩展属依赖；组合/聚合属关联。

### 5.6 SysML（★★★☆☆）

- UML 的扩展，面向系统工程；新增如**模块定义图、需求图、参数图**。需求图侧重需求间关系，用例图侧重场景交互。

## 六、需求定义与验证

- **需求定义产物**：**软件需求规格说明书（SRS）**。
- **需求验证**：需求确认；**评审**（正式会议征求干系人意见）、**检查**（非制作者详细验证）、**走查**（开发人员带领团队讲解，非正式）。三者区别：评审=正式征求意见，检查=正式+严格验证，走查=非正式。

## 七、需求管理（★★★★★）

### 7.1 需求基线

- 通过正式评审和批准的规约或产品，只能通过正式变更控制变更。

### 7.2 需求跟踪（★★★☆☆）

- **正向跟踪**：SRS 中每个需求是否在后继工作成果中有对应点（如设计是否实现某需求）。
- **反向跟踪**：设计/代码/测试等是否都能在 SRS 中找到出处。目的：确保需求被定义并实现。

### 7.3 需求风险（了解）

- 无足够用户参与、忽略用户分类、需求不断增加、模棱两可、不必要的特性、SRS 过简、估算不准确等；各阶段均有对应风险与应对措施。

---

### （案例分析向补充）

---

## 一、结构化分析

### 1.1 数据流图 DFD（★★★★★）

#### 1.1.1 基本符号（必背）

两种常见记号：**Gane & Sarson** / **Yourdon & Coad**，名称不同、含义一致。

| 符号名称 | 表示内容 | 图形表示 |
|----------|----------|----------|
| **数据流** | 有名字和方向的数据 | 标有名字的**箭头** |
| **加工（数据处理）** | 对数据流的变换 | **圆圈** |
| **数据存储** | 可访问的存储信息 | **直线段**（双线或开口矩形） |
| **外部实体** | 系统外的数据来源/去向 | 标有名字的**方框** |

**符号示意（Gane & Sarson / Yourdon 两种画法等价）**：
```
数据流：   --------→   （带名字的箭头）
加工：      (  P  )    （圆圈，内写加工名）
数据存储：  ═══════   或  ┌─────┐  （直线段/开口矩形）
外部实体：  ┌─────┐    （方框，如 管理员、外部系统）
```
**重要规则**：数据存储要被外部实体使用，**必须经过加工**。

#### 1.1.2 平衡原则（★★★★★ 必考）

**（1）父图与子图平衡（分层细化）**

- **个数一致**：父子两层数据流个数一致  
- **方向一致**：数据流方向一致  

**（2）子图内部平衡——三种错误**

| 类型 | 含义 | 记忆 |
| **黑洞** | 加工只有输入没有输出 | 只进不出 |
| **奇迹** | 加工只有输出没有输入 | 只出不进 |
| **灰洞** | 输入不足以产生应有输出（挂羊头卖狗肉） | 该出 A 却出 B |

**数据存储**：通常既有**读**又有**写**；某张子图可只有读或只有写。

**简答模板**：分层数据流图平衡 = ① 父子图数据流个数、方向一致；② 每张图内加工输入/输出平衡，避免黑洞、奇迹、灰洞。

### 1.2 状态转换图 / 状态机图（★★★☆☆）

- 即 UML 中的**状态图**，描述**状态 + 事件 → 状态变化**。
- **主要概念**：状态、初态、终态；事件、转移、动作；并发状态机。

#### 1.2.1 基本符号

| 元素 | 表示 |
| **初态** | 黑圆点 |
| **终态** | 黑圆点 + 外圈圆 |
| **中间状态** | 圆角矩形 |
| **状态转换** | 带箭头的线（可标触发事件） |

**符号示意**：
```
初态 ●  →  [ 状态A ] --事件--> [ 状态B ]  →  ◎ 终态
```

## 二、面向对象分析 · UML（★★★★★）

### 2.1 UML 常考图（记这 6 类）

| 名称 | 描述 |
| **类图** | 类、接口、协作及关系 |
| **用例图** | 用例、参与者及关系 |
| **顺序图** | 对象间消息，**强调时间顺序** |
| **通信图/协作图** | 对象间消息，**强调结构协作**（UML1 叫协作图） |
| **活动图** | 控制流/数据流，顺序、分支、并行 |
| **状态机图** | 对象状态转移 |

其他：对象图、包图、组合结构图、构件图、部署图、时间图、交互概览图、外廓图——了解即可。

### 2.2 用例图（★★★★★）

#### 2.2.1 基本步骤（顺序背）

1. **识别参与者**  
2. **合并需求获得用例**  
3. **细化用例描述**  
4. **调整用例模型**  

#### 2.2.2 参与者类型

| 类型 | 说明 | 例子 |
| **人** | 使用系统的人 | 患者、医生、管理员 |
| **其他系统** | 与本系统交互的系统 | 企业 OA 系统 |
| **硬件设备** | 需交互的设备 | IC 卡读写器 |
| **时钟** | 定时触发 | 定时交卷 |

**注意**：参与者一定在**系统之外**，不是系统一部分。

#### 2.2.3 用例关系（必背：箭头指向 + 构造型）

| 关系 | 含义 | 构造型 | 箭头指向 |
| **包含** | 提取公共行为，原始用例→基本用例，提取出的→抽象用例 | `<<include>>` | 指向**抽象用例** |
| **扩展** | 基本用例 + 多种扩展场景，扩展用例可选 | `<<extend>>` | 指向**基本用例** |
| **泛化** | 父子用例，子继承父 | — | 指向**父用例** |

**关系示意**：
- **包含**：基本用例 ----`<<include>>`----→ 抽象用例
- **扩展**：扩展用例 ----`<<extend>>`----→ 基本用例
- **泛化**：子用例 ----泛化----→ 父用例

### 2.3 类图 · 六大关系（★★★★★）

**强度顺序（口诀）**：**饭（泛）食（实现）组聚关羽（依）**  
即：**泛化 = 实现 > 组合 > 聚合 > 关联 > 依赖**

| 关系 | 定义 | 记忆 |
| **关联** | 明确、稳定的结构联系 | 工人—任务 |
| **依赖** | B 变化可能引起 A 变化，A 依赖 B | 司机依赖汽车 |
| **泛化** | 一般与特殊，父类与子类 | 狗、猫 泛化 动物 |
| **聚合** | 整体与部分，部分可属多整体，**好聚好散** | 学生—班级 |
| **组合** | 整体与部分，部分只属一整体，**生命周期同步** | 汽车—发动机 |
| **实现** | 类实现接口 | 音频播放器 实现 播放器接口 |

**聚合 vs 组合**：聚合=部分能单独存在；组合=部分随整体生灭。

### 2.4 顺序图（★★★★★）

- **用途**：对象间消息的**时间顺序**，用例分析/设计。
- **元素**：生命线、消息（同步/异步）、端点、门、**组合片段**。

#### 2.4.1 组合片段类型（常考）

- **alt**：条件分支  
- **loop**：循环  
- **par**：并行  
- **opt**：可选  

### 2.5 通信图 / 协作图（★★★★★）

- **元素**：对象、**通信链接**、消息。
- **与顺序图区别**：顺序图强调**时序**；协作图强调**对象结构与协作**。语义等价，可互相转换；关注时序用顺序图，关注结构用协作图。

### 2.6 活动图（★★★★★）

- **用途**：控制流/数据流，顺序、分支、**并行**；与流程图最大区别是**分叉/合并**和**信号（发送/接收）**。

#### 2.6.1 主要元素

| 元素 | 含义 |
| 初始/终止节点 | 开始 / 结束 |
| 动作状态 | 原子步骤 |
| 决策节点 | 分支条件 |
| 合并节点 | 多分支汇合 |
| 分叉/合并 | 并行控制流 |
| 泳道（分区） | 按角色/部门划分 |
| 对象节点/对象流 | 对象传递 |
| 信号节点 | 发送/接收事件（异步） |

**活动图与流程图区别**：活动图有**分叉/合并**（并行）和**信号发送/接收**（异步）。
```
     [动作A] → ◇决策 → 分支1 → ...
                    → 分支2 → ╋合并 → [动作B]
     ╋分叉 → [并行1] ╋
          → [并行2] ╋ 合并
```

## 三、常见图对比（★★★★★ 简答常考）

| 图类型 | 主要关注点 | 对比结论 |
|--------|------------|----------|
| **用例图** | 参与者与系统交互 | “做什么”，需求视角，不涉及实现 |
| **类图** | 类、属性、方法、关系 | “怎么做”，实现视角；与用例图形成需求→设计 |
| **数据流图** | 数据加工、流动、存储 | 数据在系统中**如何流动** |
| **活动图** | 工作流、顺序/分支/并行 | **任务执行步骤**，行为与流程 |
| **流程图** | 步骤与判断 | 与活动图类似，更偏程序逻辑 |
| **状态图** | 状态与触发条件 | **对象随事件的状态演化**，非任务流 |
| **顺序图** | 消息时间顺序 | **时序**，请求—响应过程 |
| **通信图** | 对象连接与消息 | **结构与协作**；与顺序图内容同、视角不同 |

**一句话**：用例“做啥”、类图“咋做”、DFD“数据怎么流”、活动图“步骤怎么走”、状态图“状态怎么变”、顺序图“按时间谁先谁后”、通信图“谁和谁连”。

## 四、用例描述结构（了解）

| 部分 | 说明 |
| 用例名称/编号 | 与用例图一致 |
| 简要说明 | 为参与者带来的价值 |
| 事件流 | 为达目标的一系列活动 |
| 非功能需求 | 可度量、可验证 |
| 前置/后置条件 | 执行前必须满足的状态 / 执行后可能的状态 |
| 扩展点 | 扩展/包含用例及使用情况 |
| 优先级 | 开发先后 |

## 系统设计

---

## 一、系统设计概述

- **狭义**：概要设计/整体设计/蓝图设计。
- **广义**（软考）：概要设计 + 详细设计方案与模型。
- **概要设计**：高层次规划，确定系统基本结构、组成要素及关系，解决“如何组织、如何实现”。
- **详细设计（模块设计）**：在概要设计基础上确定各模块实现方式、接口、算法等，解决“如何完成具体任务”。

### 1.1 设计类型（7 类，了解）

| 类型 | 主要任务 |
|------|----------|
| 网络设计 | 网络结构、设备分布、布线、节点权限、管理软件 |
| **代码设计** | 面向对象设计、结构化设计；唯一化、规范化、系统化 |
| 输入/输出设计 | 确保数据完整性、正确性、一致性 |
| **处理流程设计** | 模块内部结构（局部数据、控制流、加工过程） |
| 数据存储设计 | 存储方式/介质/组织、容量估算；概念/逻辑/物理设计 |
| 用户界面设计 | 黄金三原则：用户控制、减轻记忆负担、界面一致 |
| 安全性与可靠性设计 | 运行环境与数据处理控制 |

## 二、处理流程设计（★★★☆☆）

### 2.1 流程六要素（ISO 9000）

- **输入资源、活动、活动的相互作用（结构）、输出结果、用户、价值**。

### 2.2 流程设计工具

| 大类 | 工具 | 描述 |
|------|------|------|
| 图形 | 程序流程图 PFD | 图框表示操作，独立于语言；不用于描述模块输入/输出与数据加工（真题考过） |
| 图形 | N-S 图（盒图） | 顺序/选择/WHILE/UNTIL/多分支；强结构化 |
| 图形 | IPO 图 | 模块输入、输出、数据加工 |
| 图形 | PAD | 五种基本控制结构，适合嵌套与层次 |
| 图形 | 判定树 | 树形表示逻辑判断 |
| 表格 | 判定表 | 条件与行动一目了然，多条件多结果 |
| 语言 | PDL（伪代码） | 自然语言+结构化语法，逐步求精 |

### 2.3 工作流 WFMS 与 WRM

- **WFMS**：定义、创建并管理工作流执行；功能**简直过分**——建（建模）直（执行）过（过程管理）分（分析）。
- **WRM 六模块**：**指引丁克调管**——指（执行服务）引（引擎）丁（流程定义）克（客户端）调（调用应用）管（管理监控）。

## 三、面向对象设计 OOD（★★★★★）

### 3.1 设计类分类（实体/控制/边界）

| 维度 | 实体类 | 控制类 | 边界类 |
|------|--------|--------|--------|
| **定义** | 映射需求实体，存需永久存储的信息 | 控制用例工作，对用例特有控制行为建模 | 封装用例内外数据流，系统与外界交界 |
| **命名** | 业务领域名词 | 动宾短语转化名词 | 与系统接口相关，体现外部交互 |
| **作用** | 代表业务对象，对用户有意义 | 协调用例中对象交互，推进用例 | 与外界交互，隔离外部变更 |
| **生命周期** | 通常永久 | 用例执行时产生、结束后消亡 | 随系统运行存在，交互时发挥作用 |
| **属性/方法** | 一定有属性，操作按需 | 通常无属性，一定有方法 | 可有属性也有方法 |
| **示例** | 学员、课程 | 身份验证器 | 窗口、通信协议、打印机接口、报表 |

**判定顺序**：先看是否窗口/协议/打印机/报表→边界类；否则名词→实体类，动名词→控制类。

### 3.2 面向对象设计原则（SOLID 等）

| 原则 | 描述 |
|------|------|
| **单一职责 S** | 类只有一个设计目的 |
| **开闭 O** | 对扩展开放、对修改封闭；通过抽象层扩展，是其他原则基础 |
| **里氏替换 L** | 基类对象处可用子类替换；变化类设计为抽象类或接口 |
| **接口隔离 I** | 接口提供客户端需要的行为；大接口拆成小接口 |
| **依赖倒置 D** | 抽象不依赖细节，针对接口编程；实现开闭的主要机制 |
| **组合/聚合复用** | 用组合或聚合复用，比继承更灵活、耦合低 |
| **最少知识（迪米特）** | 实体尽量少与其他实体交互，降低耦合 |

## 四、结构化设计（★★★★★）

### 4.1 结构化设计四活动

- **数据设计、软件结构设计、人机界面设计（接口设计）、过程设计**。

### 4.2 模块划分原则

- 模块**大小适中**（约 1～2 页或 50～200 行）；**扇入扇出合理**（平均 3 或 4，不超过 7）；**深度与宽度适当**；**信息隐蔽**（黑盒、仅通过接口交互）。

### 4.3 耦合（7 种，低→高）

| 类型 | 描述 |
|------|------|
| **非直接耦合** | 无直接关系，通过主模块控制与调用联系 |
| **数据耦合** | 参数表传递简单数据值 |
| **标记耦合** | 参数表传递记录/数据结构 |
| **控制耦合** | 传递的信息用于控制对方内部逻辑 |
| **外部耦合** | 多模块访问同一全局简单变量，且非经参数表传递 |
| **公共耦合** | 多模块访问同一公共数据环境（全局结构、共享区等） |
| **内容耦合** | 直接访问对方内部数据/非正常入口/代码重叠/多入口 |

### 4.4 内聚（7 种，高→低）

| 类型 | 描述 |
|------|------|
| **功能内聚** | 完成单一功能，各部分协同、缺一不可 |
| **顺序内聚** | 处理元素相关且**必须顺序执行**，**有数据传递与依赖** |
| **通信内聚** | 处理元素集中在一个数据结构区域上 |
| **过程内聚** | 按特定次序执行，**不一定有数据传递与依赖** |
| **瞬时（时间）内聚** | 任务在同一时间间隔内执行 |
| **逻辑内聚** | 完成逻辑相关的一组任务 |
| **偶然内聚** | 完成无关系或松散关系的任务 |

**顺序 vs 过程**：顺序—成分与同一功能密切相关且存在数据传递与依赖；过程—按次序执行但可无数据依赖。

## 五、设计模式（★★★☆☆）

### 5.1 创建型

- 工厂方法、抽象工厂、生成器、原型、单例。

### 5.2 结构型

- 适配器、桥接、组合、装饰、外观、享元、代理。

### 5.3 行为型

- 责任链、命令、迭代器、中介者、备忘录、观察者、状态、策略、模板方法、访问者、解释器。

---

### （案例侧合并补充）

---

## 一、类识别（★★★☆☆）

### 1.1 三类与识别方法（词性法）

设计得好的类至少有一种职责：**属性封装数据，方法封装行为**。类按职责分为三类：

| 类型 | 识别方法（词性） | 作用 | 示例 |
|------|------------------|------|------|
| **实体类** | **通常是名词** | 保存需永久存储的信息，表达属性和关系 | 学员类、课程类、账户、图书、借阅记录、罚金 |
| **控制类** | **动宾短语**（动词+名词 / 名词+动词）或**纯动词** | 控制用例工作，对用例特有的控制行为建模，具有协调性 | 身份验证器、借阅、归还、预订、续借 |
| **边界类** | **通常是名词**（多为固定几类） | 系统接口与外部交互，将系统与外部环境隔离 | 窗口、通信协议、打印机接口、报表、读者证等“连接外部与控制类”的东东 |

- **实体**：名词语义存数据（名词 → 实体）
- **控制**：动宾/动词管流程（动宾、动词 → 控制）
- **边界**：窗口协议报表面，连外连控是边界

**边界类特殊记忆**：窗口、通信协议、打印机接口、报表 + 其他能“连接外部和控制类”的都可视为边界类；其余不易判断时从“是否沟通外部与内部控制”来判。

### 1.2 三类关系示意

```
  [外部参与者]  ←——→  边界类  ←——→  控制类  ←——→  实体类
     (人/系统)         (界面/协议)    (用例控制)    (持久化数据)
```

- **边界类**：与外部交互的“界面层”。
- **控制类**：协调用例流程，一般一个用例一个（可复用）。
- **实体类**：存数据、表关系，其他类一般不向控制类发消息，而由控制类发出消息。

---

### 1.3 边界类 · 实体类 · 控制类（概念要点）

- **边界类**：描述外部参与者与系统的交互；对系统外部环境与内部运作之间的交互建模，包括转换事件、记录系统表示方式（如接口）的变更。
- **实体类**：数据管理与业务逻辑层面；主要职责是存储和管理系统内部信息，可有与实体对象密切相关的行为。
- **控制类**：描述一个用例的事件流控制行为，控制用例中的事件顺序；控制其他类工作，通常每用例一个控制类，也可多用例共用；其他类一般不向控制类发消息，由控制类发出消息。

## 二、面向对象设计原则（★★★★★）

### 2.1 七大原则一览

| 原则 | 英文/缩写 | 核心含义 |
|------|-----------|----------|
| **单一职责原则** | SRP (S) | 类应该**只有一个**设计目的 |
| **开闭原则** | OCP (O) | 对**扩展开放**，对**修改关闭**；通过抽象层实现，是其他原则的基础 |
| **里氏替换原则** | LSP (L) | 使用基类对象的地方可替换为子类对象，反之不一定；变化类设计为抽象类或接口 |
| **接口隔离原则** | ISP (I) | 接口提供客户端**需要的行为**，大接口拆成小接口；或把接口视为“角色” |
| **依赖倒置原则** | DIP (D) | **抽象不依赖细节**，针对接口编程；实现开闭的主要机制 |
| **组合/聚合复用原则** | — | 通过**组合或聚合**复用已有对象，比继承更灵活、耦合度低 |
| **最少知识原则** | 迪米特法则 | 软件实体应**尽量少**与其他实体相互作用 |

### 2.2 原则关系示意

```
开闭原则(O) ←—— 依赖倒置(D) 是实现 O 的主要机制
     ↑
单一职责(S)、里氏替换(L)、接口隔离(I) 等共同支撑可扩展、可维护
组合/聚合复用：优先组合/聚合，少用继承，降低耦合
最少知识：少交互，降低耦合
```

---

### 2.3 开闭原则（要点）

**答**：面向对象设计的开闭原则（Open-Closed Principle，OCP）指软件实体（如类、模块、函数等）应对**扩展开放**、对**修改关闭**。即：在不修改原有源代码的前提下，通过扩展来增加新行为；能够在不改动原有代码的情况下改变模块行为。实践中常通过**抽象层**（抽象类、接口）实现，依赖倒置原则是实现开闭原则的主要机制。

### 2.4 各原则一句话记忆

| 原则 | 一句话 |
|------|--------|
| S 单一职责 | 一个类只干一类事 |
| O 开闭 | 能扩展不能改（加抽象层） |
| L 里氏替换 | 基类能用的地方子类也能用，变化放抽象/接口 |
| I 接口隔离 | 接口只给客户需要的，大接口拆小 |
| D 依赖倒置 | 面向接口编程，抽象不依赖细节 |
| 组合/聚合复用 | 用组合/聚合复用，少用继承 |
| 最少知识 | 少跟别的实体打交道（迪米特） |

---

## 层次结构

---

## 一、层次架构简介与反模式

### 1.1 层次架构要点

- **定义**：系统组成**层次结构**，每一层为**上层服务**，并作为**下层客户**；内部层接口可只对**相邻层**可见。
- **特性**：**关注分离**——本层组件只负责本层逻辑，角色与职责清晰，便于开发、测试、管理与维护。
- **常见三层**：**表现层** → **中间层（业务层）** → **数据访问层**。

**层次关系示意**：
```
  表现层（上层）  —— 使用 ——→  中间层  —— 使用 ——→  数据访问层（下层）
       ↑ 服务对象                    ↑ 服务对象
```

### 1.2 污水池反模式（Sinkhole Anti-Pattern）

- **现象**：请求**只是穿过**各层，没有在每层做实质处理。例如：界面层把“获得数据”交给业务层，业务层再交给持久层，持久层简单 SQL 查库，数据原路返回，**没有任何二次处理**。
- **后果**：层次隔离失效、结构不清晰、业务规则变更时易遗漏或不一致、可维护性降低。
- **解决**：**层次开放**——部分数据处理可**直达核心层次**，不必层层穿越。

**示意**：
```
  污水池反模式：  [界面] → 透传 → [业务] → 透传 → [持久] → SQL → 原路返回（无加工）
  改进：          部分请求可跨层直达核心层，在关键层做真实逻辑
```

## 二、表现层架构：MVC / MVP / MVVM（★★★★★）

### 2.1 三者对比表

| 维度 | MVC | MVP | MVVM |
|------|-----|-----|------|
| **核心组件** | Model、View、**Controller** | Model、View、**Presenter**（呈现器） | Model、View、**ViewModel**（视图模型） |
| **Model** | 数据存储与业务逻辑 | 同左 | 同左 |
| **View** | 用户界面展示 | 用户界面交互接口 | 绑定数据的 UI |
| **第三元** | 控制器：处理输入，协调 M 与 V | 呈现器：处理逻辑，**控制视图更新** | 视图模型：**数据转换与双向绑定** |
| **数据流** | **单向**：V→C→M→V（经 C 更新） | **双向**：V↔Presenter↔M（Presenter 主动更新 V） | **双向**：V↔ViewModel↔M（**自动同步**，基于绑定） |
| **View 与其它** | 可调控制器，可能访问 Model | 通过**接口**与 Presenter 通信，**不直接访问** Model | 通过**数据绑定**与 ViewModel 交互，**无直接逻辑调用** |

- **MVC**：单线 C 协调，V 可能碰 M。
- **MVP**：P 控 V，V 不碰 M，通过接口。
- **MVVM**：VM 双向绑定，V 不调逻辑。

**数据流示意**：
```
  MVC:   View ——→ Controller ——→ Model ——→ (Controller 更新) View
  MVP:   View ←→ Presenter ←→ Model   （Presenter 主动更新 View）
  MVVM:  View ←→ ViewModel ←→ Model   （绑定自动同步）
```

## 三、中间层架构（★★★☆☆）

### 3.1 三层 + DomainModel—Service—Control

- **整体**：表示层、**业务层**、数据库层。业务层内部按 **DomainModel — Service — Control** 解耦。
- **作用**：职责分离、服务与服务控制隔离，提高**可重用性**和**灵活性**。

| 组件 | 职责 |
|------|------|
| **DomainModel** | 封装**业务实体及核心逻辑**（如用户、订单的属性和行为），保证业务规则一致 |
| **Service** | **协调跨领域**业务操作（事务、权限等），可调用多个 DomainModel 完成复杂功能 |
| **Control** | **服务控制器**：根据业务状态和参数决定 Service 的**执行顺序与依赖**，实现服务间松耦合 |

**示意**：
```
  表示层
     ↓
  业务层：  Control → Service → DomainModel（多实体）
     ↓
  数据库层
```

## 四、数据访问层（★★★★★）

### 4.1 数据访问模式对比

| 模式 | 核心思想 | 特点 |
|------|----------|------|
| **在线访问模式** | 应用与数据库**持续连接**，实时执行数据操作 | 实时性强、直接操作库；依赖网络与连接稳定性 |
| **DAO（数据访问对象）** | 将**数据访问逻辑与业务逻辑分离**，用接口封装数据库操作 | 解耦数据层与业务层，可换库，代码复用 |
| **DTO（数据传输对象）** | 跨进程/网络传输数据的**轻量容器**，**无业务逻辑** | 减少传输次数、可序列化、与数据源结构解耦 |
| **离线数据模式** | 数据从数据源取走后按**预定义结构存本地**，与数据源**断开** | 离线处理，支持 XML/JSON，不依赖持续连接 |
| **ORM（对象/关系映射）** | 在关系库与对象间建**映射**，像操作对象一样操作库 | 面向对象、自动 SQL、减少数据访问层开发 |

**DAO**：把“和数据库打交道”封装在 DAO 类里，业务层只调接口（如 `BookDAO`：addBook、deleteBook、getBookById），实现类里用 JDBC 等做具体 SQL。  
**DTO**：像“快递包裹”，只装数据、不装逻辑；例如后端查库后只把前端需要的字段放进 ProductDTO 传给前端。

### 4.2 在线访问 vs ORM（简答常考）

| 方式 | 优点 | 缺点 |
|------|------|------|
| **程序在线访问** | 性能较优、能处理复杂查询 | 要求程序员懂 SQL，修改维护相对困难 |
| **ORM** | 降低学习与开发成本、无需手写 SQL、减少代码量与 SQL 质量风险 | 复杂查询处理困难、性能一般不如直接 SQL |

**选题 ORM 的常见理由**：团队缺乏数据库经验、避免 SQL 质量与维护成本、应用相对简单、性能非瓶颈。

### 4.3 增加数据访问层的原因（约 100 字）

实现**数据与业务逻辑分离**，提高可维护性、扩展性和灵活性。数据访问层负责数据库操作、封装访问细节，使**业务逻辑层与具体存储技术解耦**，简化开发；便于对数据访问统一管理、优化，提升整体性能与可靠性。

### 4.4 ORM 落地：Hibernate（★★★☆☆）

- **作用**：ORM 框架，完成 **Java 对象 ↔ 数据库表** 的映射。
- Hibernate 结构中常见**持久化对象/实体**、**Session / SessionFactory**、**持久化层/数据访问层**等。

## 五、物联网层次架构（★★★☆☆）

| 层次 | 介绍 | 典型设备/技术 |
|------|------|----------------|
| **感知层** | 物联网基础，**识别物体、采集信息**，解决物理世界数据获取；传感器采集后经**短距离传输** | 传感器、RFID 标签与读写器、二维码与识读器、摄像头、GPS、M2M 终端、传感器网关等 |
| **网络层** | **连接感知层与应用层**，传递与处理信息，将感知数据安全可靠传到应用层，按需处理 | 通信网、互联网、网络管理中心；接入网（光纤/无线等）、传输网（电信网、广电网）；3G/4G、IPv6；路由器、交换机、网关等 |
| **应用层** | 最顶层，**行业智能化应用**，与行业技术深度融合，对感知数据**计算、处理、知识挖掘**，实现控制、管理与决策 | 智能操控、安防、远程医疗等应用；终端产品；物联网中间件、云计算 |

**记忆**：**感（感知）— 网（网络）— 应（应用）**；感知采数据，网络传数据，应用用数据。

**示意**：
```
  应用层（行业应用、中间件、云）
       ↑
  网络层（通信网、接入/传输、路由/交换/网关）
       ↑
  感知层（传感器、RFID、二维码、GPS 等）
```

## 云原生架构设计

---

## 一、云原生架构原则（★★★★★）

### 1.1 七条原则一览

| 原则 | 描述 |
|------|------|
| **服务化原则** | 规模大时拆分服务，提升复用与流量治理能力 |
| **弹性原则** | 系统**自动伸缩**，**适配业务需求**（资源动态适配） |
| **可观测性原则** | 通过日志、链路跟踪等掌握系统状态 |
| **韧性原则** | **抵御软硬件异常**，保障系统稳定（故障场景下的保护） |
| **全流程自动化原则** | 自动化交付，提升效率、降低风险 |
| **零信任原则** | 基于身份访问控制，替代传统网络边界安全 |
| **持续演进原则** | 架构适应技术与业务变化，渐进式升级 |

**弹性 vs 韧性（必区分）**：
- **弹性**：侧重**资源动态适配业务需求**（扩缩容、负载适配）。
- **韧性**：侧重**故障场景下的系统保护**（容错、降级、恢复）。

## 二、云原生架构模式（★★★★★）

### 2.1 七种模式核心要点

| 模式 | 核心定义 | 典型场景/工具 |
|------|----------|----------------|
| **服务化架构**（SOA/微服务/小服务） | 按业务域或功能**拆分服务**，通过 API/协议交互；常结合 DDD、容器化与 CI/CD。优点：灵活扩展、独立部署、低耦合；缺点：分布式治理成本高 | 电商订单、支付、库存拆微服务 |
| **Mesh 化**（Service Mesh） | 将 **RPC、负载均衡、限流熔断、安全认证** 等从业务进程剥离，由 **Sidecar/Proxy（Mesh）** 统一处理；业务只写逻辑。优点：统一治理、解耦；缺点：额外性能开销 | Istio、Linkerd |
| **Serverless** | **无需管理服务器**，写业务逻辑即可；平台负责部署、运行、弹性伸缩与关闭；事件/流量触发，按调用量或时长计费。优点：运维成本低、按需伸缩；缺点：冷启动、厂商锁定 | 图片处理、日志分析、IoT 采集 |
| **存储计算分离** | **计算与存储解耦**：计算无状态，数据在分布式/云存储；有状态恢复靠 **WAL + Snapshot**。优点：弹性、成本优化；缺点：分离带来延迟 | Snowflake、TiDB、云数仓 |
| **分布式事务** | 跨库/跨服务下保持一致性。常见：**XA**（两阶段、强一致、性能差）、**消息最终一致**（MQ 异步、性能好）、**TCC**（Try/Confirm/Cancel、侵入强）、**SAGA**（本地事务+补偿）、**SEATA AT**（代理自动回滚） | — |
| **可观测架构** | **Logging**（多级日志）、**Tracing**（分布式链路）、**Metrics**（QPS、响应时间、并发等）；结合 SLO/SLA 做监控与告警 | Prometheus、Grafana、ELK、Jaeger |
| **事件驱动（EDA）** | 以**事件**为核心；事件带 Schema，可发布/订阅；有 QoS 与失败补偿。优点：解耦、异步、可扩展 | Kafka、Pulsar、EventBridge；订单完成触发发货、库变更触发缓存更新 |

**示意**：
```
  服务化：  [订单服务] [支付服务] [库存服务]  ←→ API/协议
  Mesh：    业务进程 + Sidecar(通信/治理/可观测)
  Serverless： 事件/流量 → 云函数 → 按量计费
  存储计算分离：  [计算节点-无状态]  ←→  [分布式存储]  （WAL+Snapshot 恢复）
  可观测：  Logging + Tracing + Metrics → 监控/告警
  EDA：     生产者 → 事件总线 → 消费者（异步、解耦）
```

## 三、云原生反模式 / 不好的实践（★★★☆☆）

| 反模式 | 问题描述 | 解决方案 |
|--------|----------|----------|
| **庞大的单体应用** | 依赖隔离不足、代码耦合、模块接口缺乏治理、发布难以协调、单模块故障影响整体 | **服务化适度拆分**：梳理聚合根，明确服务边界与接口，使组织与架构匹配 |
| **单体“硬拆”为微服务** | 过度拆分导致与组织能力不匹配、服务间数据依赖、性能下降 | **合理评估粒度**：考虑组织能力与业务需求，避免过度拆分；优化服务间数据交互与性能 |
| **缺乏自动化的微服务** | 规模大时人工开发/测试/运维导致交付慢、风险高、成本增 | **建立自动化**：自动化测试、发布、环境管理等，以应对复杂度 |

## 四、容器技术（★★★★★）

### 4.1 容器 vs 虚拟机（必背）

| 对比维度 | 容器 | 虚拟机 |
|----------|------|--------|
| **运行原理** | **OS 层虚拟化**，**共享宿主机内核**，直接运行在 OS 之上 | **硬件虚拟化**，模拟完整硬件，运行**独立 OS** |
| **资源隔离** | **cgroups** 限制资源（CPU、内存、磁盘 I/O），**namespace** 隔离（进程、网络、文件系统等）；共享内核 | 硬件虚拟化，**完全隔离**，每 VM 独立内核与硬件 |
| **启动速度** | **秒级**（毫秒级），无需启完整 OS | **分钟级**，需加载整个 OS |
| **资源占用** | **少**，仅应用及依赖，共享内核 | **多**，每 VM 独立 OS 与硬件模拟 |
| **应用移植性** | **好**，镜像打包应用与依赖，环境一致 | 受虚拟硬件与 OS 影响，迁移需考虑兼容 |
| **隔离性** | **较弱**，共享内核，存在内核级安全风险 | **强**，VM 相互独立 |
| **管理复杂度** | 相对简单，管镜像与容器生命周期，编排工具管集群 | 复杂，需管多 OS 安装、配置、补丁及虚拟硬件 |

### 4.2 容器 vs 容器编排（必背）

| 对比维度 | 容器 | 容器编排技术 |
|----------|------|----------------|
| **定义** | 将**应用及依赖**打包成**独立单元**的技术，可在不同环境运行，**共享宿主机内核** | **自动化部署、管理、扩展**容器化应用的技术，管理**多容器集群** |
| **核心功能** | 隔离运行环境，**快速部署与迁移** | **集群管理、资源调度、服务发现、负载均衡、自动伸缩、故障恢复**等 |
| **代表工具** | **Docker** | **Kubernetes（k8s）** |
| **部署难度** | 单容器简单（拉镜像、启容器） | 编排系统复杂（多组件、集群初始化） |
| **适用场景** | 单一/简单应用，或微服务中**单服务**载体 | **大规模、复杂**容器集群，需资源调度、伸缩、服务治理 |
| **管理粒度** | **单容器**生命周期（创建、启停、删除） | **集群级**：多容器分组、调度、健康监控、动态扩缩 |

### 4.3 Docker 常用指令

| 指令 | 说明 |
|------|------|
| `docker ps` | 查看当前运行的容器 |
| `docker ps -a` | 查看所有容器（含已停止） |
| `docker run [选项] [镜像] [命令]` | 启动容器 |
| `docker stop / docker kill` | 停止 / 强制停止容器 |
| `docker restart` | 重启容器 |
| `docker rm` | 删除容器；`docker rm $(docker ps -a -q)` 删全部 |
| `docker logs` | 查看容器日志 |
| `docker exec -it [容器] /bin/bash` | 进入容器 |
| `docker build` | 构建镜像 |
| `docker images` | 查看镜像 |
| `docker rmi` | 删除镜像 |
| `docker pull / docker push` | 拉取 / 推送镜像 |
| `docker stats` | 资源使用情况 |
| `docker info` | Docker 信息 |

### 4.4 Kubernetes 常用指令

| 指令 | 说明 |
|------|------|
| `kubectl get nodes` | 查看集群节点 |
| `kubectl get pods` | 查看当前命名空间 Pod 列表 |
| `kubectl describe pod <pod-name>` | 查看 Pod 详情（排障） |
| `kubectl delete pod <pod-name>` | 删除 Pod |
| `kubectl get services` | 查看服务列表 |
| `kubectl get deployments` | 查看 Deployment 列表 |
| `kubectl set image deployment/<名> <容器>=<新镜像>` | 更新 Deployment 镜像 |
| `kubectl logs <pod-name>` | 查看 Pod 日志 |

## 面向服务架构（SOA）

---

## 一、什么是 SOA（概念简答）

**从应用角度**：SOA 是一种**应用框架**，将日常业务应用划分为**单独的业务功能与流程**（即服务），使用户可构建、部署和整合这些服务，且**不依赖具体应用与运行平台**，从而提升业务灵活性、资产重用、管理简便与开发部署速度。

**从软件原理角度**：SOA 是一个**组件模型**，将应用的不同功能单元（**服务**）通过**定义良好的接口与契约**联系起来。接口以**中立方式**定义，**独立于**硬件平台、操作系统和编程语言，使不同系统中的服务能以**统一、通用**的方式交互。

**默写要点**：组件模型 + 服务 + 良好接口与契约 + 中立/独立于平台与语言 + 统一通用交互。

## 二、SOA / 微服务 / 单体对比（★★★★★）

### 2.1 微服务 vs SOA（五维度）

| 特性 | 微服务 | SOA |
|------|--------|-----|
| **服务粒度** | **极细**，单服务单业务功能（如商品展示、订单、支付各一服务） | **较大**，一服务可含多业务功能（如 ERP 中采购+库存一服务） |
| **通信方式** | 以 **HTTP/RESTful API** 为主，语言平台无关、易集成 | **多样化**，除 HTTP 外常用 **SOAP** 等，规范与安全性要求高 |
| **数据管理** | **每服务独立数据库**，数据源唯一 | 多服务**共享库**或经 **ESB** 交换整合 |
| **架构风格** | **去中心化**，无中心服务总线，轻量通信 | 可有**中心化 ESB**，负责注册、发现、路由、消息转换等 |
| **复杂性** | 服务独立、去中心，单服务简单；但**服务多**带来治理、监控、运维挑战 | 服务间耦合与 ESB 使设计、开发、维护相对复杂，对团队要求高 |

### 2.2 SOA vs 单体架构

| 特性 | SOA | 单体架构 |
|------|-----|----------|
| **服务粒度** | 业务功能**分解为多服务**，粒度较细，可复用 | **整体紧耦合**，功能难单独拆分与复用 |
| **通信方式** | **多协议**（SOAP、HTTP 等），标准接口，**分布式部署** | **方法调用**，同一应用内部，无复杂外部协议 |
| **数据管理** | 多服务**共享库**或经 **ESB** 交换 | **单一数据库**，所有模块共一数据源 |
| **架构风格** | **分布式**，服务可独立部署；可有 **ESB** 协调 | **集中式**，功能集中一应用、同一环境 |
| **故障影响** | 单服务故障**隔离较好**，仅影响依赖该服务的功能 | 任一部分故障可能导致**整体不可用** |

## 三、服务注册模式（★★★★★）

### 3.1 目的与三角色

**引入目的**：解决**服务提供者**与**服务请求者**之间的**寻址与发现**。通过注册中心统一发布与存储服务信息，调用方**不直接依赖具体地址**，而是**动态查询**获取调用方式，实现解耦，支持动态发现、负载均衡与版本演进。

| 角色 | 操作内容 |
|------|----------|
| **服务提供者** | 使用 **UDDI** 将 Web 服务**发布**到注册中心，用 **WSDL** 描述服务 |
| **服务注册中心** | **接收并存储**提供者通过 UDDI 发布的服务信息，供请求者**查找** |
| **服务请求者** | 借助 **UDDI** 在注册中心**查找**服务，依据 **WSDL** 了解调用方式，通过 **SOAP** 与提供者建立连接并使用服务 |

**流程示意**：
```
  提供者 --publish/UDDI--> 注册中心 <--bind/UDDI-- 请求者
       |                        |                        |
    WSDL 描述              存储/发现                 WSDL 了解
       |                        |                        |
       +-------- SOAP 调用 -----+------------------------+
```

### 3.2 UDDI、WSDL、SOAP（必背）

| 协议 | 概念 | 主要作用 |
|------|------|----------|
| **UDDI**（通用描述、发现和集成） | 基于 **XML** 的、用于发现与集成 Web 服务的协议/规范 | **发布与搜索** Web 服务信息；提供者注册，使用者查找与连接 |
| **WSDL**（Web 服务描述语言） | 描述 Web 服务**接口**的 **XML** 格式语言 | 定义功能、输入输出参数、绑定协议、访问端点等，供调用方了解**如何调用** |
| **SOAP**（简单对象访问协议） | 基于 **XML** 的**消息传输**协议，用于 Web 服务间通信 | 定义消息格式、编码规则、RPC 约定；可与 HTTP、SMTP 等结合 |

### 3.3 REST 规范要点（★★★★★）

- **全称**：Representational State Transfer（表述性状态转移）。
- **RESTful**：符合 REST 约束与原则的架构；未创造新技术，而是更好利用现有 Web 准则。

| 主题 | 要点 |
|------|------|
| **资源与 URI** | 资源是任何可被引用的事物（实体或抽象）；**URI** 为资源唯一标识（地址/名称） |
| **统一资源接口** | 使用标准 **HTTP 方法**（GET、PUT、POST、DELETE 等）访问资源 |
| **资源的表述** | 客户端获得的是资源的**表述**，形式多样（如 HTML、XML、JSON） |
| **资源的链接** | 用**超媒体**在表述中加入链接，引导客户端，增强连通性 |
| **状态的转移** | **服务端不保存客户端状态**；每次请求自带所需信息，实现状态转移 |

## 四、ESB 模式（★★★★★）

### 4.1 ESB 概念

**企业服务总线（ESB）**：由**消息中间件**发展而来，采用**“总线”模式**管理与简化应用集成拓扑，基于**开放标准**支持应用在**消息、事件和服务**级别的互联互通。

**ESB 提供**：
- **位置透明**的消息路由与寻址、**服务注册与命名**管理；
- **多种消息传递范型**（请求/响应、发布/订阅等）；
- **多种传输协议**、**多种数据格式及相互转换**；
- **日志与监控**功能。

**与服务注册**：ESB 与服务注册模式可**混用**（如架构图中 ESB 与 UDDI 注册中心并存）。

### 4.2 SOA 与 ESB 简答（约 200 字）

**SOA**：见第二节“什么是 SOA”的软件原理定义（组件模型、服务、良好接口与契约、中立、统一通用交互）。

**ESB 在 SOA 中的作用与特点**：提供位置透明性的消息路由和寻址服务、服务注册和命名的管理功能；支持多种消息传递范型（如请求/响应、发布/订阅）、多种传输协议、多种数据格式及其相互转换；还提供日志和监控功能。

---

## 五、SOA 设计原则（★★★★★）

| 原则 | 简要概括 |
|------|----------|
| **无状态** | 避免请求者依赖提供者状态 |
| **单一实例** | 防止功能冗余 |
| **明确定义的接口** | 由 **WSDL** 定义，WS-Policy 描述规约，XML Schema 定义消息格式 |
| **自包含和模块化** | 封装组件，功能实体可独立部署与管理 |
| **粗粒度** | 服务数量适度，靠消息交互 |
| **松耦合** | 使用者仅见接口，服务位置、实现不可见 |
| **重用能力** | 服务可重用 |
| **互操作性、兼容和策略声明** | 不同系统、应用、设备或组件能相互通信 |

## 六、典型架构

### 6.1 ESB 层次结构（四层）

| 层次 | 作用 |
|------|------|
| **应用工具层** | **流程编排**与 **ESB 管理**；流程编排负责业务流程设计与协调 |
| **流程控制层** | 通过**节点**控制流程走向，对**服务调用顺序、条件分支**等管理，保证流程按预期执行 |
| **数据转换与适配器层** | **适配器**连接不同系统/服务，实现通信；**数据转换**在不同格式间转换，保证数据可被正确识别与处理 |
| **服务总线层** | 基于 **JMS**（Java 消息服务），提供**可靠、异步、安全**的消息传递，为 ESB **基础通信层** |

**示意**：
```
  应用工具层（流程编排、ESB 管理）
       ↓
  流程控制层（节点、调用顺序、分支）
       ↓
  数据转换与适配器层（适配器、数据转换）
       ↓
  服务总线层（JMS、消息传递）
```

### 6.2 微服务六模式（★★★★★）

| 模式 | 定义 | 示意 |
|------|------|------|
| **聚合器微服务** | 将**多个微服务的 API 组合成统一接口**，整合响应后返回客户端，简化交互 | 客户端 → 聚合器 → 服务A、B、C → 聚合器 → 客户端 |
| **代理微服务** | 在**客户端与微服务间**增加中间层，处理**路由、负载均衡、安全验证**等 | 客户端 → 代理 → 微服务 |
| **链式微服务** | 微服务**按顺序串联**，每服务处理部分功能并传递结果，形成完整流程 | 客户端 → S1 → S2 → S3 → 响应 |
| **分支微服务** | 按**业务子系统**拆成独立微服务，通过 **API 或消息队列**通信，模块化开发 | 多个并列微服务，API/MQ 互联 |
| **数据共享微服务** | 提供**统一数据访问接口或复制机制**，支持多应用/服务共享数据 | 多服务 → 统一数据层/复制数据 |
| **异步消息微服务** | 通过**消息队列**异步通信，**解耦**服务依赖，提升吞吐与响应能力 | 服务 → MQ → 服务 |

**微服务优势**：解耦、独立部署、技术选型灵活、容错性好、易扩展。

## 系统架构设计

---

## 一、4+1 视图

| 模型 | 五视图 |
|------|------------|
| 架构 4+1 | **罗凯进五常**——逻辑、开发、进程、物理、场景 |
| RUP/UML 4+1 | **落实进步用**——逻辑、实现、进程、部署、用例 |
| ABSD 4+1 | **逻实进配**（+ 用例） |

## 二、基于架构的软件开发 ABSD（★★★★★）

### 2.1 三个基础

1. **功能分解**：使用基于模块的内聚与耦合技术。
2. **选择体系结构风格**：实现质量与商业需求。
3. **软件模板**：利用已有系统结构。

### 2.2 特点

- 由**商业、质量、功能需求**组合驱动；**递归、迭代**；自顶向下细化到构件与类。
- **用例**捕获功能需求；**质量场景**捕获质量需求（变更、性能、可靠性、交互性）；含预期与非预期场景。

### 2.3 六阶段

| 阶段 | 要点 |
|------|------|
| 体系结构需求 | 需求来源：质量目标、商业目标；需求过程、需求评审；标识构件 |
| 体系结构设计 | 需求激发设计，多视图表达，迭代 |
| 体系结构文档化 | 输出：体系结构规格说明、测试质量设计说明书；完整、高质量、从使用者角度、**保证最新** |
| 体系结构复审 | 外部人员（用户代表、领域专家）；最小化可运行系统；标识风险、发现缺陷 |
| 体系结构实现 | 按文档组装构件，连接合成，构件与整体测试 |
| 体系结构演化 | 需求变化归类→演化计划→修改/增删构件→更新交互→组装测试→技术评审 |

## 三、软件架构风格（★★★★★）

### 3.1 数据流

- **批处理**：每步独立程序，前一步**整体**结束并**整体传递**数据后下一步才开始；**可并行**弱。
- **管道-过滤器**：前一步**输出即后一步输入**，数据流连接；过滤器+管道；**可并行**。

**区别**：批处理整体传递、前后不一定数据直接关联；管道-过滤器流式、前后输入输出直接相连。

### 3.2 调用/返回

- **主程序/子程序**：单线程、层次调用，过程调用为连接件。
- **面向对象**：数据与操作封装在对象中。
- **层次型**：每层为上层服务、作下层客户；**相邻层**交互；接口一致可替换。
- **客户端/服务器**：资源不对等；二层=客户应用+数据库服务器+网络；三层=表示层+**应用服务器**（功能层）+数据层。

### 3.3 以数据为中心

- **仓库**：中央数据结构 + 对之操作的独立构件；如 IDE、企业数据管理。
- **黑板**：解空间**分级结构**，多知识源协同；如语音识别、模式识别。

### 3.4 虚拟机

- **解释器**：解释引擎 + 被解释代码存储区 + 当前状态 + 执行进度；灵活、效率较低；适合用户自定义规则、业务灵活组合。
- **规则系统**：规则集、规则解释器、选择器、**工作内存**；专家系统/决策支持。

### 3.5 独立构件

- **进程通信**：构件为独立过程，连接件为**消息传递**（点对点、异步/同步、RPC）；分布式、微服务。
- **事件系统**：**隐式调用**，构件注册到事件，事件触发时自动调用；工具集成、一致性约束、语法检查等。

### 3.6 其他

- **C2**：构件与连接件均有顶/底；构件顶连连接件底、构件底连连接件顶；构件间不直连；连接件可多连。
- **过程控制（闭环）**：软件与硬件呈反馈循环，输入→输出→环境新状态；空调、定速巡航。

## 四、架构复用与 DSSA（★★★☆☆）

### 4.1 软件产品线

- 一组系统**共享公共可管理特性集**，用**公共核心资产**按规定方式集成开发；核心资产库含架构、可剪裁元素、文档、测试计划等。
- **机会复用**：开发中发现可复用即复用；**系统复用**：开发前规划哪些复用。

### 4.2 复用基本过程

- **获取**可复用资产（可靠、适用、易理解、易修改）→**管理**（构件库：存储、分类、检索、浏览、维护）→**使用**（选择、定制、组装集成）。

### 4.3 DSSA

- **垂直域**：特定系统族，通用软件体系结构；适用于成熟稳定领域。
- **水平域**：多系统/族的功能共有部分；不限于成熟领域。
- **三活动**：领域分析（领域模型）→领域设计（DSSA）→领域实现（可重用信息）。
- **四角色**：领域专家、领域分析人员、领域设计人员、领域实现人员。
- **五阶段**：定义领域范围→定义领域特定元素（词典、共性与差异）→定义设计/实现约束→定义领域模型与体系结构→产生/搜集可重用产品单元。

### 4.4 基于构件的软件工程 CBSE（掌握）

- **定义**：基于分布对象技术、强调通过**可复用构件**设计与构造软件系统的软件复用途径。
- **构件（组件）**：自包容、可复用的程序集（源程序或二进制），对外**统一接口**；外部只能通过接口访问，不可直接操作内部。核心特性：**自包容**、**可重用**。

**构件的 5 大特征**

| 特征 | 说明 |
|------|------|
| **可组装性** | 所有外部交互经**公开定义接口**进行，且需支持自身信息的外部访问。 |
| **可部署性** | 自包含，能作为**独立实体**在构件平台运行；**二进制形式**，部署前无需编译。 |
| **文档化** | 完全文档化，用户依文档判断是否满足需求。 |
| **独立性** | 可独立组装、部署；若依赖其他构件需**显式声明**。 |
| **标准化** | 符合某种**标准化构件模型**，确保在 CBSE 过程中规范使用。 |

**构件的 3 种组装方式**

| 方式 | 说明 |
|------|------|
| **顺序组装** | 按顺序调用已有构件，用两个已有构件创造新构件。 |
| **层次组装** | 一个构件直接调用另一构件服务，被调用者为调用者提供所需服务。 |
| **叠加组装** | 两个及以上构件组合创建新构件，合并原功能并提供**新接口**。 |

**接口不兼容的 3 种情况**

| 情况 | 说明 |
|------|------|
| **参数不兼容** | 操作名相同，但**参数类型/个数**不同。 |
| **操作不兼容** | 提供接口与请求接口**操作名不同**。 |
| **操作不完备** | 一构件的提供接口是另一构件请求接口的**子集**，或相反。 |

## 五、质量属性与架构评估（★★★★★）

### 5.1 九大质量属性（面向架构评估）

| 属性 | 描述 | 对策（简） |
|------|------|------------|
| **性能** | 响应能力、处理时间、吞吐量 | 优先级队列、增加资源、减少开销、并发、资源调度 |
| **可靠性** | 错误/异常下维持功能；平均失效时间 | 冗余、错误检测与恢复、异常处理、备份与恢复、负载均衡 |
| **可用性** | 正常运行时间比例；故障间隔/恢复速度 | 心跳、冗余、快速恢复、监控与预警、故障转移 |
| **安全性** | 合法服务、阻止非授权；机密性、完整性、不可否认、可控 | 认证、授权、加密、防火墙、入侵检测、审计日志 |
| **可修改性** | 快速、高性价比变更；含可维护、可扩展、重组、可移植 | 模块化、接口标准化、抽象封装、配置与版本管理 |
| **互操作性** | 与其他系统/环境交互 | 标准协议与接口、数据格式转换、中间件、服务注册发现 |
| **可移植性** | 适应不同运行环境 | 标准 API、抽象层/虚拟机、兼容性测试 |
| **可测试性** | 易于发现缺陷、验证功能与性能 | 分层模块化、测试接口、白盒支持 |
| **易用性** | 好学、好找、好操作、少犯错 | 统一交互与视觉、信息架构、反馈、无障碍、引导与帮助 |

### 5.2 质量属性场景六要素

| 组成部分 | 含义 |
|----------|------|
| 刺激源 | 生成刺激的实体 |
| 刺激 | 到达系统时需考虑的条件 |
| 环境 | 刺激发生的条件 |
| 制品 | 被激励的对象 |
| 响应 | 激励后采取的行动 |
| 响应度量 | 对响应度量的方式 |

### 5.3 敏感点、权衡点、风险点

| 概念 | 定义 | 句式/示例 |
|------|------|-----------|
| **敏感点** | 一个或多个构件（及关系）的**特性** | “某设计/协议**将影响**某质量属性” |
| **权衡点** | **影响多个质量属性**的特性；多个敏感点的综合 | 加密级别**同时影响**安全性与性能 |
| **风险点** | 可能导致架构问题或无法满足质量属性的特性/决策 | “如果…，可能…，影响…” |
| **非风险点** | 对架构无负面影响或可接受 | 使用成熟中间件在当前架构下无明显风险 |

### 5.4 ATAM（★★★★★）

- **目标**：对**性能、可用性、安全性、可修改性**在开发前评价与折中。
- **四阶段**：① 场景和需求收集；② 体系结构视图和场景实现；③ 属性模型构造和分析；④ 折中（识别敏感点、权衡点）。
- **实践四阶段**：演示→调查和分析（含上述四步）→测试→报告。
- **效用树**：树根→**质量属性**→属性分类→**质量属性场景**（叶子）；对场景按重要度与实现难易度标 H/M/L，形成优先级对（如 H,L）= 重要且易实现。
- **ATAM 关注**：**需求**（出发点与终点）。

### 5.5 SAAM 与 CBAM

- **SAAM**：场景技术；主要关注**可修改性**；场景开发、架构描述、单场景评估、场景交互、总体评估。
- **CBAM**：按**投资回报率 ROI** 选择架构策略；质量属性变化带来效用。

---

### （案例侧合并补充）

---

## 一、软件架构风格（★★★★★）

### 1.1 风格总览（概念辨析表）

| 一级风格 | 二级风格 | 核心概念 | 典型场景 |
|----------|----------|----------|----------|
| **数据流** | 批处理 | 分步执行，前一步完成**整体传递**数据，**无并行** | 传统数据处理、批处理脚本 |
| **数据流** | 管道-过滤器 | 前一步**输出即后一步输入**，可**并行** | Unix Shell、编译器各阶段 |
| **调用/返回** | 主程序/子程序 | **单线程**控制，分层调用 | 早期结构化程序 |
| **调用/返回** | 面向对象 | 数据与操作**封装**，**消息**传递 | Java/C# 应用 |
| **调用/返回** | 层次型 | 每层为上层服务、作下层客户，**相邻层**可见 | 分层系统 |
| **调用/返回** | 客户端/服务器 | 表示层/功能层/数据层，**网络**交互 | Web、数据库应用 |
| **以数据为中心** | 仓库 | **中央数据存储**，独立构件操作 | IDE、企业数据管理 |
| **以数据为中心** | 黑板 | **分级解空间**，多**知识源**协同 | 语音识别、模式识别 |
| **虚拟机** | 解释器 | 自定义语言**解析执行**，可动态扩展 | 游戏脚本引擎 |
| **虚拟机** | 规则系统 | 规则集+规则解释器+选择器+**工作内存** | 专家系统 |
| **独立构件** | 进程通信 | 构件为**独立进程**，连接件为**消息传递** | 分布式、微服务 |
| **独立构件** | 事件驱动 | **隐式调用**，事件**广播**触发响应 | UI 系统 |
| **特殊** | C2 | 构件经连接件**分层**，**顶部到底部**通信 | 嵌入式、分布式 |
| **特殊** | 过程控制 | **反馈循环**调节物理状态 | 空调、巡航 |

**批处理 vs 管道-过滤器**：批处理需“落盘”（Tape/文件），整体传递；管道-过滤器**数据连续流动**、可并行、更高效。

**进程通信 vs 事件驱动**：进程通信是**显式**消息传递（点对点、RPC 等）；事件驱动是**隐式调用**——构件**注册**到事件，事件触发时系统**自动调用**已注册过程。

### 1.2 若干风格示意

**批处理**：`[步骤1] → Tape → [步骤2] → Tape → [步骤3]`（顺序、整体传递）

**管道-过滤器**：`[Filter1] → Pipe → [Filter2] → Pipe → [Filter3]`（流式、可分支/并行）

**仓库**：多个**独立构件** ↔ **中央数据库**（仅通过中心交换，构件间无直接连接）

**黑板**：**知识源 1～N** ↔ **黑板**（共享解空间）；实线=读写黑板，虚线=访问外部数据/计算

**解释器**：**存储器**（程序+当前状态）↔ **执行引擎**（取指、解释、更新状态）→ 输出

**层次型**：上层 → 调用 → 下层；仅**相邻层**可见，接口一致可替换实现。

**二层/三层 C/S、B/S**：二层=客户端(表示+业务)+数据库服务器（胖客户端）；三层=表示+**应用服务器**(业务)+数据（瘦客户端）；B/S=浏览器+Web 服务器+数据库，零安装、易推广，动态交互与高并发 OLTP 弱于 C/S。

---

### 1.3 解释器架构 vs 面向对象架构（选型比较）

**结论**：选**解释器**。  
- **可修改性**：解释器规则为独立语法，由解释器解析，**修改更容易**；面向对象需改具体类。  
- **灵活性**：解释器可按用户**灵活解释执行规则**，优于面向对象。  
- **性能**：面向对象一般略优（解释器多一层）。综合前两项优势更大，故选解释器。

## 二、面向架构评估的九大质量属性（★★★★★）

### 2.1 九大属性及对策

| **性能** | 响应能力、处理时间、单位时间处理量 | 优先级队列、增加资源、减少开销、并发、资源调度 |
| **可靠性** | 在错误/异常使用下维持功能；**平均失效时间** | 冗余、错误检测与恢复、异常处理、备份与恢复、负载均衡 |
| **可用性** | **正常运行时间比例**；故障间隔/恢复速度 | **心跳**（检测在线）、冗余/双机热备/集群、**快速恢复**、**故障转移**、内置监控与预警 |
| **安全性** | 合法服务+阻止非授权；机密性、完整性、不可否认、可控 | 认证、授权、加密、防火墙、入侵检测、审计与日志 |
| **可修改性** | 快速、高性价比变更；含可维护、可扩展、可移植等 | 模块化、接口标准化、抽象与封装、配置管理、版本控制 |
| **互操作性** | 与其他系统/环境交互；跨语言、跨系统 | 标准协议与接口（REST、SOAP）、数据格式转换、中间件、服务注册与发现 |
| **可移植性** | 适应不同运行环境；跨 OS/硬件/云 | 抽象层/虚拟机、减少平台依赖、兼容性测试 |
| **可测试性** | 易于发现缺陷、验证功能与性能 | 分层与模块化、测试接口、可执行语句覆盖率等度量 |
| **易用性** | 好学、好找、好操作、少犯错 | 统一交互与视觉、信息架构、即时反馈、快捷操作、多语言与无障碍、引导与帮助 |

**可用性 vs 可靠性**：若场景强调短时**切换备用 / 故障转移 / 重定向仍可访问**，多归为**可用性**；若强调**平均失效间隔**等度量，则更贴近**可靠性**语境。实务中两项常并列出现。

### 2.2 效用树与质量属性归类

- **效用树结构**：树根 → **质量属性** → 属性分类 → **质量属性场景**（叶子）。  
- **ATAM 默认四类**：**性能、可用性、安全性、可修改性**。  
- 判别线索：**响应时间/吞吐**→性能；**故障恢复/重定向/备用**→可用性；**审计/日志/攻击检测/访问控制**→安全性；**规则可配置/界面可调/扩展人天**→可修改性。  

## 三、质量属性场景描述（六要素）（★★★★★）

### 3.1 公式与语义

**公式**：质量属性场景 = **刺激源** + **刺激** + **环境** + **制品** + **响应** + **响应度量**

| 要素 | 理论 | 白话 | 例 |
|------|------|------|-----|
| **刺激源** | 人、计算机系统或其他刺激器 | 谁触发 | 大量用户、开发人员 |
| **刺激** | 刺激到达时需考虑的条件 | 触发条件 | 同时点击秒杀、请求到达 |
| **环境** | 刺激发生时系统可能处于的状态 | 何时/何状态 | 正常访问、降级模式 |
| **制品** | 受刺激的模块或系统 | 测谁 | 商品展示模块、订单模块 |
| **响应** | 激励到达后采取的行动 | 系统怎么做 | 处理请求、返回结果、切换备用 |
| **响应度量** | 可度量的指标，用于验证 | 如何衡量 | 响应时间 200ms、吞吐 500QPS、故障修复时间 |

**故事串**：**谁**（刺激源）在**什么条件**下（刺激）、**系统处于何状态**（环境）、对**谁**（制品）**做了什么**（响应），并用**什么指标**衡量（响应度量）。

### 3.2 各质量属性六要素简表

| 质量属性 | 刺激源 | 刺激 | 环境 | 制品 | 响应 | 响应度量 |
|----------|--------|------|------|------|------|----------|
| **可用性** | 系统内/外部 | 疏忽、错误、崩溃、时间 | 正常→降级 | 处理器、信道、存储器、进程 | 检测、记录、通知、隔离故障、恢复 | 故障修复时间 |
| **可修改性** | 最终用户、开发、管理员 | 改变功能、扩容等 | 设计时/编译时/构建时/运行时 | 界面、平台、交互系统 | 修改且不影响其他功能 | 成本、努力、对其它功能影响 |
| **性能** | 用户请求、他系统 | 事件到达 | 正常→超载 | 系统 | 处理刺激、改变服务级别 | 等待时间、期限、吞吐量、抖动、缺失率 |
| **可测试性** | 开发、测试、客户 | 分析/架构/设计/集成交付时触发 | 设计时/开发时/编译时/部署时 | 设计、代码段、应用 | 提供状态访问、计算值、测试环境 | 语句覆盖率、缺陷触发概率、测试时间等 |
| **易用性** | 最终用户 | 想学习系统、有效使用 | 运行时或配置时 | 系统 | 友好界面与适应性设计，错误影响最小 | 用户满意度、成功操作占比 |
| **安全性** | 内/外部个人或系统 | 试图访问/修改数据、访问服务 | 网络环境 | 系统服务、数据 | **认证、授权、审计、加密、监控** | 检测、溯源、恢复 |

**可用性六要素示例**（模板）：刺激源：系统内部、系统外部。刺激：疏忽、错误、崩溃、时间。环境：正常操作、降级模式。制品：系统处理器、通信信道、持久存储器、进程。系统响应：检测事件并记录、通知相关方、禁止故障源、一段时间不可用后恢复、在正常或降级模式下运行。响应度量：故障修复时间。

## 四、系统架构评估（★★★★★）

### 4.1 敏感点、权衡点、风险点、非风险点

| 概念 | 定义 | 句式/例 |
|------|------|----------|
| **敏感点** | **一个或多个构件（及关系）**的特性 | “**…将影响…**”；如：对查询处理时间的要求**将影响**传输协议与处理设计 |
| **权衡点** | **影响多个质量属性**的特性，是多个敏感点的综合 | “**…能提升 A 但会降低 B**”；如：加密级别高→安全性↑、性能↓ |
| **风险承担者** | 影响架构决策的利益相关者 | 财务部门、销售部门 |
| **场景** | 风险承担者角度的交互描述；常用刺激、环境、响应 | 客户在公共 Wi-Fi 下登录网银，10 秒内完成认证并显示账户 |
| **风险点** | **可能**导致架构出问题或不满足质量要求的特性/决策 | “**如果…，可能…，影响…**” |
| **非风险点** | 对质量属性无负面影响或可接受的特性/决策 | 选用成熟 MQ（如 RabbitMQ），经市场验证，当前架构下无明显风险 |

### 4.2 三种架构评估方法

| 方法 | 描述 | 优点 | 缺点 |
|------|------|------|------|
| **调查问卷/检查表** | 用问卷或检查表，凭经验与知识评估 | 利用经验与知识 | 依赖主观推断 |
| **基于场景的评估** | 分析架构对场景的支持程度；**SAAM、ATAM** 属此类 | 系统化评估对场景的支持 | 需详细定义场景，可能耗时 |
| **基于度量** | 建立质量属性与度量的映射，从架构文档推导 | 客观、可量化 | 需精确度量与映射，专业要求高 |

---

### 4.3 SAAM（★★★☆☆）

- **思想**：用**实际使用场景**对架构“挑刺”，检查是否扛得住。  
- **主要分析**：**可修改性**；协调风险承担者达成共识。  
- **输入**：问题描述、需求声明、架构描述。  
- **过程**：场景开发 → 架构描述 → 单场景评估 → 场景交互评估 → 总体评估。  
- SAAM 是 **ATAM** 的基础。

### 4.4 ATAM 与效用树（★★★★★）

- **ATAM**：在 SAAM 基础上，针对**性能、安全性、可修改性、可用性**，在开发前**评价与折中**；用**效用树**等工具做**优先级排序**再评估。  
- **四阶段**：场景与需求收集 → 架构视图与场景实现 → 属性模型构造与分析 → 权衡（迭代）。  
- **效用树**：树根 → **质量属性**（上述四类）→ 属性分类 → **场景**（叶子）。  
- **修剪与优先级**：保留重要场景（通常≤50），按**重要度 H/M/L** 与**实现难易度 H/M/L** 标优先级对，如 (H,L)=重要且易实现。

### 4.5 ATAM 实践四阶段

| 阶段 | 主要活动 |
|------|----------|
| **演示** | 介绍 ATAM 流程、业务目标、待评估架构 |
| **调查和分析** | 确定架构设计方法；**生成质量属性效用树并排序**；分析架构与质量属性匹配度 |
| **测试** | 头脑风暴候选场景；筛选高优先级场景并验证架构支持能力 |
| **报告** | 汇总**风险、非风险、敏感点、权衡点**；提交效用树、场景列表及改进建议 |

## 信息安全

---

## 一、系统安全分类（★★★☆☆）

| 类型 | 要点 |
|------|------|
| **实体安全** | 环境、设备、媒体；防损失与破坏 |
| **运行安全** | 风险管理、审计跟踪、备份与恢复、应急 |
| **信息安全** | OS 安全、数据库安全、网络安全、病毒防护、访问控制、加密、认证 |
| **人员安全** | 意识、法律、技能 |

## 二、等级保护（★★★☆☆）

| 等级 | 适用对象 |
|------|----------|
| 第一级 用户自主保护 | 普通内联网用户 |
| 第二级 系统审计保护 | 需保密的非重要单位，内联网/国际网商务 |
| 第三级 安全标记保护 | 地方国家机关、金融机构等重要单位 |
| 第四级 结构化保护 | 中央级国家机关、广电等关键部门 |
| 第五级 访问验证保护 | 国防关键部门、依法特殊隔离单位 |

## 三、数据加密（★★★★★）

- **对称**：AES（128/192/256）、DES（56）、3DES（168/有效 112）、IDEA（128）、RC5；国产 SM1/SM4/SM7。**非对称**：RSA、ECC；国产 SM2/SM9（SM2 椭圆曲线、SM9 基于标识）。
- **杂凑**：SM3、MD5、SHA 系列；用于完整性、摘要。

## 四、认证技术：数字签名与数字证书（★★★★★）

- **Hash 特性**：易压缩、单向、高灵敏、抗碰撞；SHA-256、SM3 等。
- **数字签名**：对**摘要**用私钥加密；验证时用公钥解密摘要，与对原文 Hash 结果比对。**只签摘要不签全文**。
- **数字证书**：CA 用私钥对“持有者信息+公钥”等签名，形成证书；对方用 CA 公钥验证证书，获得可信公钥。解决**公钥真实性**问题。
- **场景**：信源验证（私钥签名、公钥验签）；密钥交换（公钥加密会话密钥，私钥解密）。

## 五、密钥管理体制（★★★☆☆）

| 体制 | 适用 | 特点 |
|------|------|------|
| **KMI** | 封闭网 | KDC 分发，静态/动态，秘密信道 |
| **PKI** | 开放网 | CA、证书，标准平台 |
| **SPK** | 规模化专用网 | LPK/CPK 等 |

## 六、安全协议（★★★★★）

| 协议 | 层次/类型 | 要点 |
|------|-----------|------|
| **SSL** | 传输层 | 握手/记录/警报；身份认证、加密、完整性；HTTPS 即 HTTP+SSL |
| **HTTPS** | 应用层+SSL | 安全通道、防窃听、X.509 |
| **IPSec** | 网络层，IPv4/6 | IP 层加密与认证，端到端 |
| **PGP** | 应用，邮件 | 基于 RSA，邮件加密与签名 |

## 七、访问控制（★★★★★）

| 类型 | 思想 | 适用 |
|------|------|------|
| **DAC** | 主体自主授权，依身份与组 | 多用户 |
| **MAC** | 主体与客体安全属性，强制规则 | 高安全 |
| **RBAC** | 用户—角色—权限 | 大型系统如 DBMS |
| **TBAC** | 权限随任务上下文，动态 | 工作流 |
| **OBAC** | 对象 ACL，可继承、派生 | 对象多、变化频繁 |

### 7.1 访问控制模型（★★★☆☆）

- **BLP**：保密性；**上读 no read up**、**下写 no write down**。
- **Biba**：完整性；**下读 no read down**、**上写 no write up**。

## 八、单点登录（★★★☆☆）

- **Kerberos**：信任第三方，票据认证。**外壳脚本**：一次认证激活多系统，口令同步等需配合。

---

### （案例分析向补充）

---

## 一、安全架构三性（★★★☆☆）

| 特性 | 定义 | 典型例子 |
|------|------|----------|
| **可用性** | 确保系统资源**在需要时可正常访问**，防止故障或攻击导致服务中断 | 数据备份与恢复、负载均衡、双机热备 |
| **完整性** | 保护数据**不被未经授权修改或破坏**，确保数据一致性和准确性 | 文件哈希校验（MD5/SHA）、**数字签名**、数据库事务回滚 |
| **机密性** | **防止敏感信息在未授权情况下被泄露或获取** | 数据加密存储、HTTPS、访问控制列表（ACL） |

**辨析**：**完整性** = 防篡改、保一致；**机密性** = 防泄露、保密。二者不同，答题时分别写清概念即可。

## 二、数据加密技术（★★★★★）

### 2.1 对称加密

- **原理**：**同一密钥**既加密又解密；发送方用密钥把明文变密文，接收方用**同一密钥**还原。
- **优点**：加解密**速度快、效率高**，适合**大量数据**。
- **典型算法**：AES、DES、3DES。
- **数学基础**：替代/置换等运算。

---

### 2.2 非对称加密

- **原理**：**一对密钥**——**公钥**（可公开，用于加密）、**私钥**（用户自持，用于解密）。发送方用**接收方公钥**加密，接收方用**自己私钥**解密。
- **优点**：**密钥管理方便**，无需在通信双方间传递私钥；安全性基于**数学难题**（如 RSA 大整数分解）。
- **典型算法**：RSA、ECC、DH。
- **用途**：数字签名、密钥交换、身份验证等。

---

### 2.3 非对称的两种应用场景

| 场景 | 谁加密 / 谁解密 | 传递内容 | 典型用途 |
|------|-----------------|----------|----------|
| **数字签名（信源验证）** | **私钥加密，公钥解密** | 明文 + 签名（对摘要签名） | 验证来源与完整性，防抵赖 |
| **数据加密** | **公钥加密，私钥解密** | 密文 | 只有拥有私钥的接收方能解密；也可用于**交换对称密钥** |

**记忆**：签名 = 私钥签、公钥验；加密 = 公钥加密、私钥解密。

**密钥交换**：非对称加密慢，适合小数据；实际大量数据用对称加密。双方可用**公钥加密“对称密钥”**传给对方，对方用私钥解密得到密钥，后续用对称密钥通信。

### 2.4 对称 vs 非对称对比

| 对比项 | 对称加密 | 非对称加密 |
|--------|----------|------------|
| **密钥** | **单一密钥** | **密钥对**（公钥 + 私钥） |
| **速度** | **快**，适合大数据 | **慢**，适合小数据 |
| **密钥管理** | 需**安全共享**密钥 | 公钥公开，私钥保密 |
| **主要用途** | 数据加密 | 密钥交换、数字签名、身份验证 |
| **数学基础** | 替代/置换 | 数论难题（分解、离散对数等） |
| **典型算法** | AES、DES、3DES | RSA、ECC、DH |

## 三、认证技术（★★★★★）

### 3.1 Hash（杂凑）算法

- **特性**：**易压缩**（任意长→固定长）、**单向**（不能由哈希反推原文）、**高灵敏**（输入微变输出大变）、**抗碰撞**（不同输入极难同哈希）。
- **用途**：数据**完整性校验**，是数字签名的重要组成部分。
- **常用算法**：SHA-1（已不推荐）、SHA-256、SHA-384、国产 **SM3**。

---

### 3.2 数字签名原理

**创建签名**：  
1. 对文件做 **Hash** 得到**消息摘要**。  
2. 用**签名者私钥**对摘要加密，得到**数字签名**。  
3. 可加时间戳。  
4. 发送：**原文 + 数字签名**（注意：只对摘要签名，不对整个文件，省资源、提效率）。

**验证签名**：  
1. 接收方对**原文**再做 Hash，得**本地摘要**。  
2. 用**签名者公钥**解密数字签名，得**发送时摘要**。  
3. **两摘要一致** → 文件完整且签名有效；不一致 → 被篡改或签名无效。

**示意**：
```
  发送方：  原文 → Hash → 摘要 → 私钥加密 → 签名  ； 发送 [原文 + 签名]
  接收方：  原文 → Hash → 摘要A ； 签名 → 公钥解密 → 摘要B ； 比较 摘要A 与 摘要B
```

---

### 3.3 数字证书

- **问题**：公钥如何保证属于真正的发送方？若公钥被篡改，验证会失效。
- **解决**：**数字证书**——由 **CA（证书颁发机构）** 签发，对**公钥及其持有者信息**做认证；CA 用**自己的私钥**对“公钥+信息”签名，形成证书。
- **验证**：接收方用 **CA 的公钥**解密证书，验证证书有效性，即可信任其中的公钥。
- **标准**：X.509；CA 维护**证书吊销列表（CRL）**；不同 CA 间通过**证书链**建立信任。
- **典型流程**：客户端请求 → 服务器返回**数字证书** → 客户端用 CA 公钥验证证书 → 用服务器公钥加密后续数据（或交换对称密钥）。

### 3.4 身份认证方式简表

| 方式 | 特点 |
| **口令认证** | 用户名/密码，简单但不安全，易泄露、被猜、被截获 |
| **动态口令** | 按时间或次数变化，一次一密，较安全；可能同步问题、输入不便 |
| **生物特征** | 身体/行为特征，分身体特征、行为特征及不同层级 |

## 四、网络安全体系（★★★☆☆）

### 4.1 安全服务与安全机制

| 安全服务 | 对应安全机制 | 作用 |
|----------|----------------|------|
| **鉴别服务** | 认证机制、数字签名机制 | 验证实体身份真实性，防身份伪造 |
| **访问控制服务** | 访问控制机制、路由控制机制 | 管理访问权限，仅授权主体可访问资源 |
| **数据机密服务** | 加密机制、业务流填充机制 | 保护保密性；业务流填充可防流量分析 |
| **数据完整性服务** | 数据完整性机制 | 防篡改、删除、插入，保证准确一致 |
| **抗否认性服务** | 公证机制 | 记录与验证操作，防抵赖，可追溯 |

**关系**：安全服务 = “要实现什么”；安全机制 = “用什么技术实现”。

### 4.2 典型架构的脆弱性（★★★☆☆）

| 架构 | 脆弱性梳理 |
|------|------------|
| **分层架构** | 层间依赖强，单层故障易波及整体；层间通信易成性能瓶颈 |
| **C/S 架构** | 客户端易藏漏洞；网络开放、易受攻击；数据易遭破坏 |
| **B/S 架构** | 易受病毒、恶意代码攻击，安全性较弱 |
| **事件驱动架构** | 组件控制力不足；数据交互逻辑复杂；易死循环、高并发难题；固定流程存在安全漏洞 |
| **MVC 架构** | 设计复杂；视图与控制器耦合高，影响复用；视图读模型效率低 |
| **微内核结构** | 整体优化难；进程间通信开销大，效率下降 |
| **微服务架构** | 开发逻辑复杂；需额外通信与容错设计；管理维护复杂度高 |

**说明**：给定架构并要求分析脆弱性或安全风险时，可从表中对应条目展开。

## 软件测试

---

## 一、测试分类

### 1.1 按程序是否可见

| 类型 | 要点 |
|------|------|
| **黑盒** | 功能测试；不看内部，基于输入输出；等价类划分等；系统/确认测试常用 |
| **白盒** | 结构测试；了解内部逻辑，覆盖所有路径；逻辑/循环/基本路径覆盖；军工、航天等高可靠领域 |
| **灰盒** | 介于两者；多用于集成测试；既看输入输出也看内部表征 |

### 1.2 按是否执行程序

| 类型 | 要点 |
|------|------|
| **静态测试** | 不运行程序；分析语法、结构、接口等；代码检查、静态结构分析、质量度量；发现缺陷早，耗时长 |
| **动态测试** | 运行程序；比较结果与预期；构造用例、执行、分析输出 |

### 1.3 按测试对象

功能测试、性能测试、安全测试、兼容性测试、界面测试、易用性测试、稳定性测试等。

### 1.4 按测试阶段

| 阶段 | 对象 | 方法 | 要点 |
|------|------|------|------|
| **单元测试** | 最小可测单元 | 白盒为主、黑盒为辅 | 模块接口、局部数据；开发人员在组长监督下 |
| **集成测试** | 模块/子系统 | — | 模块间数据传输、高层设计；单元测试之后 |
| **系统测试** | 整个系统 | 黑盒 | 功能、性能、恢复、安全、压力等；集成测试之后 |

## 二、白盒覆盖标准（★★★★★）

**强度从弱到强**：语句 < 判定 ≈ 条件 < 判定条件 < 条件组合 < 路径。

| 覆盖类型 | 描述 | 发现错误能力 |
|----------|------|--------------|
| 语句覆盖 | 每条语句至少执行一次 | 最弱 |
| 判定覆盖 | 每个判定的每个分支至少一次 | 2.5 |
| 条件覆盖 | 每个条件取到各种可能值，**不考虑组合** | 2.5 |
| 判定条件覆盖 | 同时满足判定覆盖与条件覆盖 | 4 |
| 条件组合覆盖 | 每个判定中各条件每一种组合至少一次 | 5 |
| 路径覆盖 | 所有可能路径都执行过 | 最强 |

## 三、功能测试与性能测试细分

### 3.1 功能测试

链接测试、表单测试、数据校验、Cookies 测试、数据库测试等；概念辨识即可。

### 3.2 性能测试辨析（★★★★★）

| 类型 | 要点 |
|------|------|
| **负载测试** | 在**已达一项或几项性能瓶颈**时，让系统继续运行一段时间，观察各项指标 |
| **压力测试** | **不断加压**（增加负载），直到某些指标达瓶颈（如 CPU 100%）；**过程性、渐进加压** |
| **强度测试** | 制造**极限状态**（超负荷或最差环境），看系统抵抗能力与性能下降幅度；侧重**极限状态**，非渐进过程 |
| **并发测试** | 负载+压力结合；逐渐增加负载至瓶颈或不可接受点，综合分析交易与资源指标 |
| **容量测试** | 预先分析某指标极限值（如最大并发用户数），在极限状态下测试是否故障或仍保持主要功能 |
| **可靠性测试** | 为满足可靠性要求的设计、分析、测试；系统测试阶段提高可靠性、发现对可靠性影响大的缺陷 |
| **配置测试** | 找系统资源最优分配，为调优提供依据；选典型配置测试 |
| **速度测试** | 网络连接速度、业务处理速度 |

**压力 vs 强度**：压力=慢慢加压、过程性；强度=制造极限状态（超负荷或环境）。

## 四、安全测试类型

数据加密、用户身份验证、日志文件、Session、备份与恢复、访问控制、安全漏洞（XSS、SQL 注入等）、TCP 端口、服务器端脚本、防火墙等；选择题概念辨识。

## 五、测试模型 W/H/X（★★★☆☆）

| 模型 | 特点 | 与开发关系 |
|------|------|------------|
| **W 模型** | 在 V 模型基础上发展；测试与开发**同步** | 同步 |
| **H 模型** | 测试活动**独立**，贯穿整个生命周期 | 独立 |
| **X 模型** | 针对**单独程序片段**编码与测试，**频繁交接**集成 | 交错 |

## 系统运行与维护

---

## 一、运维技术指标（★★★★★）

| 指标 | 含义 | 计算 |
|------|------|------|
| **MTTR** | 平均故障修复时间：故障发生到恢复正常所花时间的平均值 | 修复总时间 / 维修次数 |
| **MTBF** | 平均故障间隔时间：两次故障之间的平均运行时间 | 总运行时间 / 故障总数 |
| **MTTF** | 平均无故障时间：从开始运行到出现故障的平均持续时间（可更换/不可修系统） | 可正常运行总时间 / 故障总数 |
| **MTTA** | 平均应答时间：运维团队响应投诉/中断/异常的平均时间 | 告警到确认的累计时间 / 事件总数 |

## 二、软件维护分类（★★★★★）

| 类型 | 原因 | 定义 |
|------|------|------|
| **改正性维护** | 识别并纠正错误、性能缺陷、误用 | 诊断和改正错误（修 Bug）；上线后发现的故障修复 |
| **适应性维护** | **外部环境**变化（新硬件/软件配置、数据库、数据格式等） | 为使软件适应这些变化而修改；如 OS 升级、硬件更新、迁移 |
| **完善性维护** | **用户提出**新功能与性能要求 | 为满足新要求而修改或再开发；功能增强、性能优化、体验改进等，非修故障、非环境变化 |
| **预防性维护** | 预先提高可维护性、可靠性 | 用先进方法对需维护部分重新设计、编码与测试；“用今天的方法学满足明天的需要” |

**适应性 vs 完善性**：变化来自**外部环境**→适应性；来自**用户要更多/更好**→完善性。

### 2.1 提高可维护性（论文可用）

- 需求：明确未来改进与可修改部分、跨平台可移植方案。
- 设计：高内聚低耦合、灵活可扩充、可复用构件。
- 编码：规范、注释、可复用构件。
- 测试：完善测试与测试文档。
- 维护：配置管理、文档同步更新、用户培训。

## 三、遗留系统处置（★★★★★）

**遗留系统**：基本不能通过修改和演化满足新业务需求的信息系统。

**四象限**：横轴**业务价值**（低→高），纵轴**技术含量**（低→高）。按逆时针：一象限→二象限→三象限→四象限。

| 象限 | 技术 | 业务 | 策略 | 说明 |
|------|------|------|------|------|
| **第一象限** | 高 | 高 | **改造** | 系统功能增强、数据模型改造 |
| **第二象限** | 高 | 低 | **集成** | 对遗留系统进行集成 |
| **第三象限** | 低 | 低 | **淘汰** | 全面重新开发新系统替代；业务根本变化、维护与文档丢失、新系统更合算等 |
| **第四象限** | 低 | 高 | **继承** | 新系统完全兼容遗留系统功能与数据模型；新老并行后逐步切换 |

## 四、新旧系统转换策略（★★★★★）

| 策略 | 定义 | 适用场景 |
|------|------|----------|
| **直接转换** | 原系统停止某一时刻，新系统立即投入，**无过渡** | 新系统不太复杂或现有系统完全不可用 |
| **并行转换** | 新系统与现有系统**并行运行**一段时间，试运行后正式替换 | 较大系统、处理复杂、数据重要 |
| **分段转换** | 直接与并行的结合，**分期分批**逐步转换 | 较大系统、现有系统稳定、转换风险大 |

## 系统可靠性

---

## 一、可靠性设计技术（★★★☆☆）

| 分类 | 技术 | 要点 |
|------|------|------|
| **容错** | 恢复块设计 | 程序划分为恢复块，每块含多个功能相同、设计不同的程序块；故障时切换备用块 |
| **容错** | N 版本程序设计 | 多版本软件，同一输入多数表决；不同算法/语言/测试保证独立 |
| **容错** | 冗余设计 | 主系统外设备用模块，主故障时切换 |
| **检错** | 检错技术 | 检测点与检测机制，监测返回结果、运行时间等，发现故障后停止并报警 |
| **降复杂度** | 降低复杂度设计 | 简化结构、优化数据流，降低模块内与模块间复杂度 |
| **系统配置** | 双机热备 | 主备服务器，心跳检测切换 |
| **系统配置** | 服务器集群 | 多台服务器一体，单台故障时其他接管 |

## 二、可靠性定量指标（★★★★★）

| 指标 | 英文（记末尾词义） | 定义 |
|------|--------------------|------|
| **MTBF** | Mean Time **Between** Failures | 两次相邻故障之间的平均工作时间；**含**检测时间与修复时间 |
| **MTTR** | Mean Time to **Repair/Recover** | 故障发生到恢复正常所需的平均时间 |
| **MTTF** | Mean Time to **Failure** | 从开始使用到出现故障的平均时间（**不含**检测与修复） |
| **MTTA** | Mean Time to **Acknowledge** | 从故障警报到相关人员确认故障的平均时间 |
| **MTTD** | Mean Time to **Detect** | 从故障发生到故障被检测到的平均时间 |

**关系**：MTTR、MTTD 通常较小，故**MTBF ≈ MTTF**（选择题考过）。

- **失效概率 F(t)**：初始为 0，随运行时间增加趋近 1。
- **可靠度 R(t) = 1 - F(t)**：规定条件、规定时间内不失效的概率；R(0)=1，R(+∞)=0。
- **规定时间**：CPU 执行程序指令所用时间总和；用执行时间度量最准确。

## 三、影响可靠性的因素（了解）

## 四、McCabe 环形复杂度（★★★☆☆）

- **流图**：结点=语句或顺序语句块，边=控制转移；由边和结点围成的封闭区域+图外部=区域数。
- **V(G) 三种算法**：
  1. **区域数**：流图划分成的线性无关区域数（含图外）。
  2. **V(G) = E - N + 2**：E=边数，N=结点数。
  3. **V(G) = P + 1**：P=判定结点数（具判断条件的结点）。

## 五、串并联系统（★★★★★）

### 5.1 串联系统

- 所有组件依次连接，**全部正常**系统才正常；任一组件失效则系统失效。
- **R 串联 = R1 × R2 × … × Rn**

### 5.2 并联系统

- 多组件冗余，**至少一个正常**系统即正常；全部失效才失效。
- **R 并联 = 1 - (1-R1)(1-R2)…(1-Rn)**

真题可能出现串并联混合，先按局部串联/并联化简再计算。

## 项目管理

---

## 一、范围管理（★★★☆☆）

**五过程**（软考无“收集需求”）：

| 过程 | 描述 |
|------|------|
| 编制范围管理计划 | 描述如何定义、确认和控制范围 |
| 定义范围 | 详细描述产品与项目范围，编制范围说明书 |
| **创建工作分解结构 WBS** | 将项目工作分解为较小、易管理部分，自上而下至工作包，其总和即项目范围 |
| 确认范围 | 正式验收可交付成果 |
| 范围控制 | 监督范围状态，管理范围基准变更 |

## 二、进度管理（★★★★★）

### 2.1 过程

活动定义→活动排序→活动资源估算→活动历时估算→进度计划编制→进度控制。

### 2.2 网络图

- **AOV 网（单代号）**：顶点=活动，弧=先后关系；单代号以**节点**代表工作，箭线=逻辑关系。
- **AOE 网（双代号）**：顶点=事件，弧=活动；双代号以**箭线及两端节点编号**表示工作。求**最短/关键路径**时常用双代号。
- **PERT 图**：即网络图。

### 2.3 三点估算法

- **乐观时间 a**：一切顺利时完成时间。
- **最可能时间 m**：正常情况下完成时间。
- **悲观时间 b**：最不利情况下完成时间。
- **期望时间**：**(a + 4m + b) / 6**

### 2.4 关键路径与时差

- **关键路径**：从开始到结束的**最长路径**，决定项目最短工期。
- **总时差**：该活动可推迟而不影响总工期的最大时间。
- **自由时差**：该活动可推迟而不影响紧后活动最早开始的最大时间。真题常考总时差计算。

## 三、成本管理（★★★☆☆）

| 过程 | 定义 | 要点 |
|------|------|------|
| 成本估算 | 对完成项目所需成本的估计与计划 | 自顶向下、自底向上、差别估算法 |
| 成本预算 | 将总成本分配到各工作包，建立成本基准 | 形成成本基准计划 |
| 成本控制 | 保证各项工作在预算内进行 | — |

## 四、配置管理（★★★☆☆）

| 内容 | 描述 |
|------|------|
| 编制配置管理计划 | 项目启动时制订，为开发工作基础 |
| 配置标识 | 确定配置项、命名与描述，是配置管理前提 |
| 变更管理与配置控制 | 对配置项变更加以控制，防止多次变更下失控 |
| 配置状态说明（配置状态报告） | 记录、报告配置信息，给出配置项当前状况 |
| 配置审核 | 验证配置项与配置标识一致，确保管理有效 |

## 五、风险管理（★★★☆☆）

**六过程**：风险管理计划编制→风险识别→风险定性分析→风险定量分析→风险应对计划编制→风险监控。

| 过程 | 要点 |
|------|------|
| **风险管理计划编制** | 如何安排与实施风险管理；角色与职责、预算、风险类别、概率与影响定义、汇报格式、跟踪；应急计划与应急储备 |
| **风险识别** | 系统化识别已知与可预测风险；反复进行；确定风险来源、条件、特征；收集资料→估计风险形势→识别潜在风险 |
| 风险定性分析 | 优先级排序；可能性与影响、风险类型 |
| 风险定量分析 | 灵敏度分析、期望货币价值、决策树、**蒙特卡罗**（多元函数→参数概率→随机数→模拟）等 |
| 风险应对 | **防范策略**（发生前：避免、转移、减轻）；**响应策略**（发生后执行）；正向风险：开拓、分享、提高 |
| 风险监控 | 跟踪已识别风险、残余风险，识别新风险，评价计划有效性 |

## 计算机组成原理

---

## 一、系统层次结构（低→高）

硬联逻辑级→微程序级（微指令集）→传统机器级（指令集）→操作系统级→汇编语言级→高级语言级→应用语言级。

## 二、Flynn 分类

| 类型 | 描述 | 代表 |
|------|------|------|
| **SISD** | 单指令流单数据流 | 传统单处理器 |
| **SIMD** | 单指令流多数据流 | 并行处理机、矩阵机 |
| **MISD** | 多指令流单数据流 | 少见；流水线有时视为此类 |
| **MIMD** | 多指令流多数据流 | 高性能服务器、超级计算机 |

**常考**：SISD=传统单机，MIMD=多机并行。

## 三、冯·诺依曼机

- **存储程序**：程序与数据同存于存储器，控制器自动取指执行。
- **五部件**：运算器、控制器（合为 CPU）、主存、输入设备、输出设备、辅助存储器（外存）。
- **数据流**：输入/外存→主存→CPU 运算→主存→输出；**控制流**：控制器指挥各部件。

## 四、存储器系统

- **速度与容量**：寄存器最快（比 Cache 还快）> Cache > 主存 > 辅存（CPU 不可直接访问）。
- **Cache 依据**：**局部性原理**——时间局部性（刚访问的很快再访）、空间局部性（邻近单元很快被访问）。

### 4.1 RAID（★★★☆☆）

| 级别 | 盘数 | 利用率 | 校验 | 容错 | 读写性能 |
|------|------|--------|------|------|----------|
| RAID 0 | N | N | 无 | 无 | 最好（并行） |
| RAID 1 | 偶数 | N/2 | 无 | 允许 1 块故障 | 读同单盘，写写两边 |
| RAID 5 | ≥3 | (N-1)/N | 有 | 允许 1 块故障 | 读近 RAID0，写较慢 |
| RAID 6 | ≥4 | (N-2)/N | 有 | 允许 2 块故障 | 写慢于 RAID5 |
| RAID 1+0 | ≥4 偶数 | N/2 | 无 | 两组各坏 1 块 | 读同 RAID0，写同 RAID1 |

### 4.2 磁盘调度

- **移臂调度**：如 SSTF（最短寻道时间优先）；**旋转调度**：同一磁道内按扇区顺序。先移臂后旋转。
- **优化分布**：使处理完一块后磁头刚好在下一块起始，减少旋转等待。

### 4.3 网络存储

- **DAS**：直连服务器，SCSI；难扩展。**NAS**：存储设备连网络，文件级共享。**SAN**：专用网络、块级，FC/iSCSI 等，高性能高可用。

## 五、I/O 与总线

- **数据传送方式**：程序控制（CPU 参与多、效率低）→程序中断→**DMA**（主存与外设批量交换，DMAC 控制）→通道→I/O 处理机。
- **总线**：分时共享；**总线带宽** = 总线宽度 × 总线频率；总线周期 = 时钟周期数（例：2 个时钟周期则频率=时钟频率/2）。

## 六、指令系统：CISC vs RISC

| 类型 | 指令 | 寻址 | 实现 | 其他 |
|------|------|------|------|------|
| **CISC** | 数量多、变长、频率差别大 | 多种 | 微程序（微码） | 研制周期长 |
| **RISC** | 数量少、定长、单周期多、仅 Load/Store 访存 | 少 | 通用寄存器多、硬布线、流水线 | 优化编译、支持高级语言 |

## 七、流水线（★★★★★）

- **执行时长**（无特殊并行）：**第一条指令顺序执行时间 + (n-1) × 周期**，周期=各段中最长的那段时间。
- **吞吐率 TP**：单位时间完成的任务数；TP = n / Tk；各段不等时，**最大 TP 由最长的段决定**。
- **加速比**：S = T0 / Tk；若 k 段、各段时间为 t1…tk，n 个任务：S = (n×Σti) / (Σti + (n-1)×max(ti))；**最大加速比** = k（段数）。
- **超标量**：多条流水线同时执行；若度为 4，12 条指令则每条流水线约 3 条，按 3 条指令的流水线公式算再取时间。

## 八、阿姆达尔定律

- 某功能占系统时间比例 p，加速 k 倍后，总加速比 = 1 / ((1-p) + p/k)。例：p=50%，k=10 → 总时间比 = 1/(0.5+0.05)=1/0.55≈1.82。

## 九、系统性能评估

- **基准程序准确度**（高→低）：真实程序 > 核心程序 > 小型基准程序 > 合成基准程序。

## 计算机网络

---

## 一、传输方式与码元

- **单工**：数据只能单方向传输（如广播）。**半双工**：双方可收发但不能同时（如对讲机）。**全双工**：可同时双向（电话、以太网）。
- **码元**：代表不同离散值的基本波形；V 进制码元可携带 log2(V) 比特。

## 二、奈氏准则与香农公式（★★★★★）

- **奈氏准则**（理想低通、无噪声）：极限码元速率 = 2W（码元/秒）；**极限数据传输率 = 2W × log2(V)**（b/s），V=码元状态数。
- **香农公式**（有噪声）：**C = W × log2(1 + S/N)**（b/s）；信噪比 dB = 10×log10(S/N)。实际速率低于 C 才可能无差错。

## 三、数据编码（了解）

- **模拟**：AM/ASK、FM/FSK、PM/PSK；抗干扰 PM 最强。
- **数字**：不归零（无同步）、**曼彻斯特**（每比特中间跳变，高→低=1）、差分曼彻斯特（中间跳变同步，起始无跳变=1）、4B/5B 等。

## 四、OSI 七层与协议（★★★★★）

| 层 | 名称 | 常见协议 |
|----|------|----------|
| 7 应用 | 为应用提供网络服务 | HTTP、FTP、SMTP、POP3、DNS、Telnet、SNMP |
| 6 表示 | 格式转换、加密压缩 | SSL/TLS |
| 5 会话 | 建立、管理、终止会话 | NetBIOS、RPC |
| 4 传输 | 端到端传输 | TCP（可靠、面向连接）、UDP（无连接） |
| 3 网络 | 路由与寻址 | IP、ICMP、IGMP、ARP、RARP |
| 2 数据链路 | 帧、差错、流量控制 | PPP、HDLC、Ethernet、VLAN、MAC |
| 1 物理 | 物理标准与信号 | RS-232、RJ-45、IEEE 802.3 |

## 五、TCP/IP 四层与常见端口

| 传输层 | 协议 | 端口 | 说明 |
|--------|------|------|------|
| TCP | FTP | 20 数据、21 控制 | 文件传输 |
| TCP | Telnet | 23 | 远程登录 |
| TCP | SMTP | 25 | 发邮件 |
| TCP | HTTP | 80 | Web |
| TCP | POP3 | 110 | 收邮件 |
| UDP | DNS | 53 | 域名解析（也可 TCP） |
| UDP | DHCP | 67/68 | 自动分配 IP |
| UDP | TFTP | 69 | 简单文件传输 |
| UDP | SNMP | 161/162 | 网络管理 |

**TCP**：可靠、面向连接、重发；**UDP**：无连接、不可靠、速率高。

## 六、IP 地址与子网（★★★★★）

- **A 类**：首字节 0–127；**B 类**：128–191；**C 类**：192–223；**D 类**：224–239 组播；**E 类**：240–255 保留。
- **网络地址**：主机位全 0。**广播地址**：主机位全 1。**回环地址**：127.0.0.1。
- **子网**：从主机位借 K 位作子网号，可划分 2^K 个子网。**子网掩码**：网络+子网位为 1，主机位为 0；两 IP 与掩码按位与结果相同则在同子网。
- **CIDR**：无类；格式 A.B.C.D/**前缀长度**；网络地址=IP 与掩码按位与。最长前缀匹配：选掩码位数最多的匹配项。

## 七、IPv4 vs IPv6

- IPv4：32 位、点分十进制；IPv6：128 位、冒号十六进制。
- IPv6：固定 40 字节报头、无校验和、内置 IPsec、无状态地址自动配置（SLAAC）、无需 NAT。

## 八、局域网拓扑与以太网（★★★★★）

| 拓扑 | 特点 |
|------|------|
| **星形** | 以枢纽为中心，依赖枢纽 |
| **总线** | 共享线路，线路故障影响全网 |
| **环形** | 闭合环，单点故障影响环路 |
| **网状** | 任意两点直连，可靠但成本高 |

**以太网**：CSMA/CD（载波监听、冲突检测）；监听算法：非坚持、1-坚持、P-坚持；冲突时边发边听、停发、发拥塞信号、随机退避重试。

## 九、无线局域网

- Wi-Fi 4（802.11n）~0.6Gbps；Wi-Fi 5（ac）~6Gbps；Wi-Fi 6（ax）~9.6Gbps；Wi-Fi 7（be）~46Gbps。

## 十、网络设备与交换（★★★★★）

| 设备 | 工作层次 | 功能 |
|------|----------|------|
| 中继器、集线器 | **物理层** | 再生信号；集线器=多口中继器 |
| 网桥、二层交换机 | **数据链路层** | 按 MAC 转发；交换机=多口网桥 |
| 路由器、三层交换机 | **网络层** | 按 IP 路由；三层交换机=带路由的二层交换 |
| 网关 | 高层（4–7） | 不同协议子网互连 |

**交换**：**电路交换**（独占通路）；**报文交换**（存储转发）；**分组交换**（数据报/虚电路，最常用）。

**路由协议**：**内部网关** RIP、OSPF 等；**外部网关** BGP；距离向量 vs 链路状态。

## 操作系统

---

## 一、进程状态（★★★☆☆）

- **三态**：**就绪**（只差 CPU，等调度）、**运行**（正占用 CPU）、**阻塞/等待**（等事件，有 CPU 也不能运行）。
- **五态**：新建态→就绪→运行→阻塞→终止态。

## 二、同步与互斥、PV 操作（★★★★★）

- **同步**：按既定顺序协调执行（如读等写满、写等读完）。
- **互斥**：同一时刻仅一进程访问临界资源（如打印机）。
- **P(S)**：S 减 1；若 S≥0 继续，若 S<0 阻塞并入阻塞队列。
- **V(S)**：S 加 1；若 S>0 继续，若 S≤0 从阻塞队列唤醒一进程，本进程继续。
- **计数信号量**：S≥0 表示可用资源数；**S<0 时 |S| 为阻塞进程数**（如 S=-3 表示 3 个进程阻塞）。
- **前趋图**：有向无环图，边表示前驱关系；前驱完成处写 V，后继开始处写 P；需为每条边设一信号量（初值 0）。

## 三、死锁（★★★★★）

**四必要条件**：① **互斥**（资源独占）；② **不剥夺**（已占资源不被强夺）；③ **请求与保持**（占着资源再申请其他）；④ **环路**（等待形成环）。

**预防**：破坏上述任一条件（如一次性分配、可剥夺、有序申请等）。

## 四、内存管理（★★★★★）

### 4.1 页式管理

- 逻辑地址 = **页号 + 页内地址**；页表：页号→物理块号；物理地址 = 物理块号×页大小 + 页内地址。
- 例：逻辑空间 64KB、页大小 1KB → 页内 10 位、页号 6 位；给定逻辑地址先拆出页号与页内地址，查页表得块号再算物理地址。

### 4.2 段式管理

- 逻辑地址 = **段号 + 段内地址**；段表：段号→基址等；物理地址 = 段基址 + 段内地址。

### 4.3 虚拟内存与页面置换

- **虚拟存储**：部分装入、请求调入、置换；大程序小内存、多进程并发。
- **置换算法**：**FIFO**（淘汰最早进入）、**LRU**（淘汰最久未用）、**Clock**（环形+访问位，扫到 0 淘汰并置 0）、NUR、最优（理论）。FIFO 可能 Belady 异常。
- **缺页次数**：按访问序列模拟，缺页时按算法选一页淘汰再装入新页，统计缺页次数。

## 五、文件系统（★★★★★）

### 5.1 文件索引

- **直接地址**：索引节点中直接放数据块号，逻辑块号 0～k-1（k=直接项数）。
- **一级间接**：一项指向一个索引块，索引块内放数据块号；每块可存 块大小/地址项大小 个地址；逻辑块号范围 k～k+n-1（n=一索引块项数）。
- **二级间接**：一项指向一级索引块，一级索引块再指向数据块；范围再往后推。
- 例：5 直接、2 个一级、1 个二级；块 1KB、地址项 4B → 一索引块 256 项；块号 1 用直接，块号 518 用二级间接。

### 5.2 位示图

- 每位表示一物理块使用情况；**字数 = 物理块总数 / 字长**（如 300GB/4MB=76800 块，字长 32 则 76800/32=2400 字）。

## 数据库系统

---

## 一、三级模式与两级独立性（★★★★★）

- **外模式**：用户视图，局部数据逻辑结构。**概念模式**：全体数据逻辑结构，DBA。**内模式**：存储结构、物理顺序、索引，系统程序员。
- **物理独立性**：应用程序与磁盘数据独立。**逻辑独立性**：应用程序与逻辑结构独立，逻辑结构变时应用可不变。

## 二、关系运算（★★★☆☆）

- **并 R∪S**、**差 R-S**、**交 R∩S**、**笛卡尔积 R×S**、**投影 π**、**选择 σ**、**θ 连接/自然连接 R⊳⊲S**。选择=WHERE，投影=SELECT 列。

## 三、E-R 与逻辑设计（★★★★★）

### 3.1 E-R 三要素

- **实体**：矩形。**属性**：椭圆（单值）；双线椭圆多值；虚线椭圆派生）。**联系**：菱形，标注 1:1、1:n、m:n。

### 3.2 E-R 转关系模式

- **1:1**：独立关系模式（两实体主码+联系属性）或与任一端合并（另一端主码+联系属性放入）。
- **1:n**：独立关系模式（n 端主码）或与 **n 端合并**（1 端主码+联系属性放入 n 端）。
- **m:n**：联系**单独成表**，属性=两实体主码+联系属性，主码为两主码组合。

### 3.3 合并 E-R：合并→消除冲突（属性/命名/结构）→消除冗余。

## 四、函数依赖与范式（★★★★★）

- **完全函数依赖**：X→Y 且 X 的任一真子集不能决定 Y。**部分函数依赖**：X→Y 但 Y 不完全依赖于 X（存在真子集决定 Y）。**传递函数依赖**：X→Y，Y→Z，Y↛X，Z⊈Y。
- **候选键**：图示法——依赖作图，入度为 0 的属性集为起点，能遍历全图即为候选键；若不能则加入部分中间顶点再试。
- **无损分解**：ρ={R1,R2} 对 F 无损 ⇔ (R1∩R2)→(R1−R2) 或 (R1∩R2)→(R2−R1)。

| 范式 | 关键 |
|------|------|
| **1NF** | 属性原子，不可再分 |
| **2NF** | 1NF + 无非主属性对候选码的**部分**函数依赖 |
| **3NF** | 1NF + 无非主属性对候选码的**传递**函数依赖 |
| **BCNF** | 任一非平凡依赖左侧均为**候选码** |
| **4NF** | BCNF + 无多值依赖（或多值依赖拆开） |

- **不规范问题**：**绒绣茶山**——冗余、修改异常、插入异常、删除异常。

## 五、事务与并发控制（★★★★★）

### 5.1 ACID

- **原子性**：全做或全不做。**一致性**：从一个一致状态到另一一致状态。**隔离性**：并发事务互不干扰，可串行化。**持久性**：提交后永久。

### 5.2 并发问题

| 问题 | 描述 |
|------|------|
| **丢失修改** | 两事务同读同改，后提交覆盖前提交 |
| **读脏数据** | 读到他事务未提交的修改，他事务回滚后数据无效 |
| **不可重复读** | 同一事务内两次读同一数据，中间被他事务更新，结果不同 |
| **幻读** | 同一事务内两次按条件读，中间他事务插入/删除，结果集不同 |

### 5.3 封锁

- **X 锁（排他）**：可读可写；其他事务不能加任何锁。**S 锁（共享）**：可读不可写；其他可加 S，不能加 X；全部 S 释放后才能加 X。
- **一级**：修改前加 X 直到结束；防丢失修改，不防脏读、不可重复读。**二级**：一级 + 读前加 S 读后即释；防丢失、脏读，不防不可重复读。**三级**：一级 + 读前加 S 直到结束；防丢失、脏读、不可重复读。**两段锁**：扩展阶段只能加锁、收缩阶段只能解锁；可串行化，可能死锁。
- **粒度**：粒度小并发高、开销大；粒度大并发低、开销小。

## 六、完整性、触发器、反规范化与索引（★★★★★）

- **实体完整性**：主键不为空。**参照完整性**：外键取被参照表主键值或空；级联/受限/置空等。**用户定义完整性**：业务规则。
- **触发器**：BEFORE/AFTER × INSERT/UPDATE/DELETE；INSERT 用 NEW，DELETE 用 OLD，UPDATE 用 NEW 与 OLD。
- **反规范化**：增加冗余以提升性能；带来冗余、修改/插入/删除异常。
- **索引准则**：常查询、少更新的属性建索引；索引过多影响增删改；小表可不建。

## 七、备份、OLAP 与分片（★★★☆☆）

- **物理备份**：拷贝数据文件、日志。**逻辑备份**：导出为 SQL 等逻辑对象。**完全**：全量。**增量**：自上次备份以来变化。**差异**：自上次完全备份以来变化。
- **OLTP**：日常事务、当前细节、面向应用。**OLAP**：分析决策、历史聚集、面向主题、多维。
- **钻取**：变粒度；**切片/切块**：部分维定值；**旋转**：行列互换。
- **水平分片**：元组划分，**并**恢复。**垂直分片**：属性划分，**连接**恢复；关键字各分片均需保留。

---

### （案例侧合并补充）

---

## 一、数据库性能优化（★★★★★）

### 1.1 索引建立注意事项

| 准则 | 说明 |
| 选**常查少改**的属性建索引 | 常作为查询条件、不常更新的属性适合建索引；**常更新的属性**建索引会严重影响性能 |
| **索引不宜过多** | 一个关系上索引过多会拉低 UPDATE、INSERT、DELETE 性能，因为更新时所有索引都要调整 |
| **按查询频度优化** | 分析重要查询的使用频度，优先优化使用最多的索引 |
| **小表可不建索引** | 数据量很小的关系不必建索引，全表扫描往往更快、耗资源更少 |

### 1.2 数据分片（Shard）

- **目的**：**横向扩展**（Scale Out），突破单节点 I/O 与扩展性限制；把“整库”打碎成多块，每块存到不同物理节点。
- **分片分类**（按拆分维度）：

| 类型 | 原理 | 重构方式 |
|------|------|----------|
| **水平分片** | 按**行**划分，元组分成多个子集，每个子集为一个片段；条件由**关系属性值**表示 | **并**操作重构全局关系 |
| **垂直分片** | 按**列（属性）**划分，满足不相交 | **连接**运算重构 |
| **导出分片** | 水平分片条件**来自其他关系**的属性（如按学生表的性别对选课表分片） | — |
| **混合分片** | 水平 + 垂直混合 | — |

### 1.3 分片算法

| 算法 | 原理 | 适用数据 |
| **哈希分片** | 对键（如 id）哈希，再对节点数**取模**，余数对应存储节点 | 分布均匀、无强顺序/时间相关性、关联弱、可独立读写 |
| **范围分片** | 按**范围区间**（时间、ID 等）划分，同一范围放一起 | 有明显顺序（时间、编号）、可划分区间 |
| **地理位置分片** | 按**地理位置**（城市、地域）分配 | 数据与地理位置强相关，不同地域访问模式不同 |
| **融合算法** | 组合多种分片算法 | 同时有随机读写、范围查询或多维特征 |

**中间件常用**（如 MyCat）：**取模**（随机均匀）、**范围**（范围查询）、**列表**（枚举值指定分片）、**一致性哈希**（节点动态增删时只影响相邻数据）。

### 1.4 数据分区

- **含义**：单表内按规则（范围、哈希、列表等）划分为多个**逻辑子集**（分区）；查询时优化器只访问相关分区。
- **好处**：**查询**—减少 I/O、加快查询；**管理**—大表拆成小块，便于维护、备份、恢复；**隔离**—按类型或时间分隔，便于权限与生命周期管理。
- **分区算法**：与分片类似——**范围分区**（时间、数值区间）、**列表分区**（离散枚举值）、**哈希分区**（均匀、随机读写）、**复合分区**（先一种再另一种）。

### 1.5 分片 vs 分区（★★★★★）

| 特征 | 分片 | 分区 |
|------|------|------|
| **范围** | **跨多个数据库实例/服务器** | **单实例内** |
| **目的** | 提高**可扩展性、并发** | 优化**查询性能** |
| **实现** | 多个**物理表**（多节点） | **PARTITION BY** 子句（单库内） |
| **管理难度** | 相对复杂 | 相对简单 |

### 1.6 分库分表（★★★★★）

- **动机**：单机连接数、I/O、网络瓶颈；单表过大导致 B+ 树变高、I/O 增多；热点集中拖慢整体。
- **目的**：**分库**—分散请求到多台服务器，突破单机能力；**分表**—减小单表数据量，查询在更小范围完成。
- **分库分表是分片的物理实现**：“做了分片”的实现手段 = 分库 + 分表。

| 类型 | 定义 | 举例 |
| **水平分表** | 同一张表按**行**划分到多张表 | 订单表按订单 ID 取模拆成 orders_0、orders_1、orders_2 |
| **垂直分表** | 同一张表按**列**拆到多张表，减少单表字段 | 商品表：product_base（基本信息）、product_detail（描述、图片等大字段） |
| **水平分库** | 相同表结构分布到**多个库**，分摊压力 | 消息表按用户 ID 拆到 message_db1、message_db2 |
| **垂直分库** | **不同业务**的数据放到**不同库**，按模块拆 | 用户库 user_db、课程库 course_db、支付库 payment_db |

## 二、数据库高可用（★★★★★）

### 2.1 读写分离

- **目的**：把**读、写**分散到不同节点；**小幅提升写、大幅提升读**；符合写少读多。
- **常见**：**一主多从**—主库写，从库读；主从之间**数据同步**保证从库数据正确。
- **实现**：应用层如 **Shardingsphere**；数据库层中间件如 **MyCat**。

**示意**：
```
  写请求 → 主库  ←── 同步 ──→  从库  ← 读请求
```

### 2.2 主从复制

- **手段**：利用**日志**（如 MySQL 的 **binlog**）；主库写操作记入 binlog，推给从库，从库**重放**完成复制。
- **流程（MySQL）**：① 主库把数据变化写入 **binlog**；② 从库连接主库请求复制；③ 从库 **I/O 线程**向主库请求 binlog；④ 主库 **dump 线程**发送 binlog，从库 I/O 线程接收；⑤ 从库 I/O 线程将 binlog 写入**中继日志（relay log）**；⑥ 从库 **SQL 线程**从 relay log 读事件并应用到本地库。

| 优势 | 描述 |
| **数据冗余** | 多副本，主库故障时减少数据丢失风险 |
| **负载均衡** | 读分发到从库，减轻主库负载 |
| **高可用** | 主库故障时可切换到从库 |

### 2.3 主从一致性问题（主从延迟）（★★★★★）

- **现象**：主库写入后，同步到从库有**时间差**，读从库可能拿到**旧数据**。
- **解决思路**：减少延迟或延迟窗口内**强制读主**。三种方式对比：

| 方式 | 原理 | 优点 | 缺点 |
|------|------|------|------|
| **半同步复制** | 主库提交事务后，等**至少一个从库**把事务 binlog 写入 relay log 并**回 ACK**，主库才提交并返回客户端 | 用数据库**原生**能力，实现简单 | 主库**写时延增加**，**吞吐下降** |
| **数据库中间件** | 读写都走中间件；写走主库，读走从库；**记录写库的 key**，在**主从同步时间窗**（如 500ms）内对该 key 的**读请求走主库**，过后再走从库（如 Canal、Otter） | **强一致** | 中间件**成本高** |
| **缓存记录** | 写前把 **key 写入 cache** 并设超时（如 500ms），再写主库；读时**先查 cache**，命中则说明刚写过，**读主库**，未命中则读从库 | 比中间件**成本低** | 多 **cache** 组件，读写多一步缓存操作 |

**key**：能唯一标识写操作的主键或业务键（如主键 ID、用户 ID、订单号），用于在延迟窗口内把“刚写过”的读请求路由到主库。

**示意**：
```
  半同步：  主库 --等从库 ACK--> 提交
  中间件：  写 key 记录 → 时间窗内该 key 读主库
  缓存：    写前 key 入 cache(500ms) → 读时命中则主库
```

## 三、数据库选型（★★★★★）

| 分类 | 典型场景 | 结构/特点 | 产品 |
|------|----------|-----------|------|
| **键值数据库** | 内容缓存、高访问负载、日志等 | Key-Value，常用 hashtable | **Redis** |
| **列存储数据库** | 分布式文件、海量存储与分析、大数据 | 列簇存储，同列放一起 | **Cassandra、HBase** |
| **文档型数据库** | Web 应用、灵活结构（如电商商品信息） | Key-Value，Value 为结构化文档 | **MongoDB** |
| **图数据库** | 社交网络、推荐、知识图谱、关系链分析 | 图结构（节点、边、属性） | **Neo4J** |
| **时序数据库** | 物联网监控、设备状态、金融时序分析 | 以**时间戳**为核心，按时间序列组织 | **InfluxDB、TDengine** |

## Redis

---

## 一、数据类型（★★★★★）

| 类型 | 含义 | 适用场景 |
|------|------|----------|
| **String** | 字符串 | 配置、缓存对象、统计、限流（时间内请求次数） |
| **Hash** | 键值对（field-value） | 用户/商品/文章/购物车等对象信息 |
| **List** | 列表 | 最新文章/动态、简单消息队列 |
| **Set** | 集合，不重复，可交并差、随机取 | UV 统计、点赞、共同好友、抽奖、随机点名 |
| **Zset** | 有序集合，按权重排序 | 排行榜（礼物、步数、段位、热度） |
| **Bitmap** | 位图，0/1 表示状态 | 签到、活跃用户、行为统计 |
| **HyperLogLog** | 基数计数 | 海量计数（百万/千万级 IP、UV 统计） |
| **Geospatial** | 地理空间 | 附近的人、地理位置 |

**String vs Hash 存对象**：  
- **String**：存**序列化后的整对象**，更省内存（约 Hash 一半），适合多层嵌套复杂对象；  
- **Hash**：可**单独存、取、改**部分字段，省网络流量，适合字段常变或需单独查询的场景。

## 二、持久化机制（★★★★★）

| 方式 | 含义 | 特点 |
|------|------|------|
| **RDB** | Redis DataBase，**快照** | 按时间间隔将数据**快照**写入磁盘；**恢复快**；宕机可能丢一段数据；**默认**开启 |
| **AOF** | Append-Only File，**追加日志** | 每个**写命令**追加到文件；可做到**秒级**持久化；**恢复慢**；可重写压缩 |

**AOF 同步策略**：**always**（每次写都刷盘）、**everysec**（每秒）、**no**（由操作系统决定）。  
**AOF 重写**：压缩文件，减小占用；Redis 7.0 引入 Multi-Part AOF 优化。

## 三、Redis 集群（★★★★★）

### 3.1 三种模式概览

| 模式 | 作用 | 优点 | 缺点 | 适用 |
|------|------|------|------|------|
| **主从复制** | 主节点数据复制到从节点；主写从读 | 配置简单、数据冗余、读写分离 | **不能自动故障转移**，受单机内存限制 | 备份、读写分离、在线升级 |
| **哨兵（Sentinel）** | 在主从基础上加哨兵，**监控+自动故障转移** | 高可用，具备主从优点 | 配置管理较复杂 | 高可用要求高 |
| **Cluster** | **数据分片（slot）+ 故障转移**，多主多从 | 水平扩展、高可用、突破单机容量 | 架构与运维更复杂 | 大数据量、高并发、需分片 |

---

### 3.2 主从复制流程

**全量同步**（从库首次或需全量时）：  
① **握手**：从库发 `PSYNC -1`，主库回 `FULLRESYNC <runid> <offset>`（runid=主库身份，offset=复制进度）；  
② 主库 **BGSAVE** 生成 RDB，期间新写命令进 **replication buffer**；  
③ 主库把 **RDB** 发给从库，从库清空旧数据、加载 RDB；  
④ **补齐**：主库把 replication buffer 中的命令发给从库，从库执行后与主库一致；  
⑤ **常态复制**：后续写命令通过长连接实时同步。

**增量同步（部分重同步）**（断线重连后）：  
① 从库带 `runid + offset` 发 `PSYNC <runid> <offset>`；  
② 主库校验 runid 是否一致、缺口数据是否还在 **repl_backlog_buffer**；在则返回 CONTINUE，执行部分重同步；不在则全量；  
③ 主库从 backlog 中取“>offset”的日志发给从库执行；  
④ 进入常态复制。

---

### 3.3 哨兵（Sentinel）

- **作用**：**监控**主从健康；主节点下线时**自动故障转移**（选新主、通知从库与客户端）；**通知**管理员或其它服务。
- **建议**：至少 **3 个**哨兵节点，奇数便于投票。

**故障转移流程**：  
① **选 Sentinel Leader**：主节点客观下线的哨兵发起选举，得**过半数**票者成为 Leader；  
② **选新 Master**：从存活从节点中按 **slave-priority → 复制 offset → 实例 ID** 选数据最完整者为新主；  
③ **重建主从**：其它从节点复制新主；通过**发布/订阅**把新主地址通知客户端；  
④ **旧主恢复**：旧主上线后执行 `slaveof` 降级为新主的从节点。

---

### 3.4 Cluster 模式

- **目的**：解决单机内存与性能上限、主从+哨兵无法**水平分片**的问题；通过**数据分片（slot）+ 自动故障转移**实现扩展与高可用。
- **槽位**：键空间划分为 **16384** 个槽；每个主节点负责一部分槽；键定位：**CRC16(key) % 16384**；扩容缩容时迁移对应槽数据。
- **结构**：多个 Master（各负责一部分槽），每个 Master 可有多个 Slave；主库故障时从库可被提升为主库。

---

### 3.5 哨兵 vs Cluster

| 对比 | 哨兵 | Cluster |
|------|------|--------|
| **数据分片** | **不分片**，所有数据在一个主库 | **分片**，数据分布到多主 |
| **主要作用** | 监控 + **主从故障转移** | **分片 + 故障转移** |
| **选型** | 单 Redis 高可用、读写分离 | **横向扩展、大数据量、高并发** |

## 四、常见生产问题（★★★★★）

| 问题 | 原因 | 解决方案（简） |
|------|------|----------------|
| **缓存穿透** | 请求的 key 在**缓存和库都不存在**，请求直接打到库 | **校验**非法参数；**缓存空 key**（短过期）；**布隆过滤器**先判存在再查 |
| **缓存击穿** | **热点 key 过期瞬间**大量请求打到库 | 热点**永不过期**或长过期；**互斥锁**（只有一个请求查库）；**预热**热点数据 |
| **缓存雪崩** | **大量 key 同时失效**，请求压垮库 | **集群**避免单点；**限流**；**多级缓存**；**错峰过期**；**预热** |

**布隆过滤器**：多位数组 + k 个哈希函数；添加时把对应位标 1；判断时若有一位为 0 则**一定不存在**，全为 1 则**可能存在**（有假阳性，无假阴性）。

## 五、缓存与数据库一致性（★★★★★）

**旁路模式**：读先查缓存，没有则查库并回写缓存；**写先更新库，再删除缓存**，下次读时从库加载到缓存。

**主从延迟导致读旧缓存**：写库后删缓存前，其它请求可能读到旧缓存。常用做法：

| 方式 | 思路 | 一致性 | 备注 |
|------|------|--------|------|
| **延迟双删** | 删缓存 → 更新库 → **延迟后再删一次缓存** | 最终一致 | 延迟时间需根据业务权衡 |
| **先更新库再删缓存** | 更新库 → 删缓存 | 最终一致，概率上偶发不一致 | **最常用、最简单** |
| **分布式锁** | 写时加写锁（更新库+删缓存），读时加读锁，串行化 | **强一致** | 性能差 |
| **MQ 串行化** | 同一 key 的写请求进同一队列，顺序消费更新库与缓存 | 最终一致 | 无乱序覆盖 |
| **订阅 binlog** | 库更新后通过 **Canal** 等把 binlog 发 MQ，消费后更新缓存 | 最终一致 | 顺序消费保证最终一致 |

## 六、过期删除与内存淘汰（★★★★★）

**过期删除策略**：  
- **惰性删除**：**取 key 时**才检查是否过期；对 CPU 友好，可能堆积大量过期 key。  
- **定期删除**：**周期性**抽一批 key 删过期；通过限制执行时长和频率降低对 CPU 影响。

**内存淘汰策略**（达 **maxmemory** 时）：

| 策略 | 描述 | 适用 |
|------|------|------|
| **noeviction**（默认） | 内存满时**拒绝写入** | 要数据完整、对可用性要求不高 |
| **volatile-lru** | 在**设了过期**的 key 中淘汰**最近最少使用** | 缓存，优先淘汰不常用 |
| **allkeys-lru** | **全部** key 中淘汰最近最少使用 | **通用缓存** |
| **volatile-random** | 设了过期的 key 中**随机**淘汰 | 对顺序无要求 |
| **allkeys-random** | 全部 key 中**随机**淘汰 | 均匀淘汰 |
| **volatile-ttl** | 设了过期的 key 中淘汰**剩余 TTL 最小** | 优先淘汰即将过期 |

## 七、大 Key 与热 Key（★★★★★）

### 7.1 大 Key

- **定义**：Key 对应的 **Value 大**（如 String 5MB、Zset 成员上万、Hash 成员 value 总和大）。
- **危害**：命令变慢、易触发 maxmemory 淘汰或阻塞、集群下某分片内存倾斜、删除易阻塞引发主从中断。
- **处理**：**拆分**大 Key（如大 Hash 拆成多个小 Key）；**清理**不适于 Redis 的数据；**定期清理**过期数据防堆积。

### 7.2 热 Key

- **定义**：**QPS/带宽/CPU** 集中在一个或少量 key（如单 key QPS 远高于其它）。
- **危害**：占用大量 CPU、集群下某分片被打满、易引发击穿。
- **处理**：**多级缓存**（本地缓存分担）、**热 key 备份**（多节点备份分散读）、**热 key 拆分**（带后缀分散到多实例）、**业务隔离**（热点业务独立集群）。

## 八、分布式锁（★★★★★）

- **目标**：分布式环境下，保证同一时刻**只有一个进程**能访问共享资源。
- **三种实现**：

| 实现 | 做法 | 优点 | 缺点 |
|------|------|------|------|
| **数据库表** | 锁表插入一条记录，释放删记录 | 简单 | 依赖库可用性、无失效时间、非阻塞、非重入 |
| **Redisson** | 基于 Redis，支持单机/主从/Sentinel/Cluster；**看门狗**定时续期防提前过期；**可重入**（hset 记录加锁次数） | 性能好、可重入、续期 | 非阻塞需自旋 |
| **Zookeeper** | 在 /lock 下建**临时有序**子节点，序号最小者获锁；释放删节点；监听前序节点 | 无单点、可重入、阻塞等待、锁可释放 | 高并发下性能不如 Redis（ZAB 协议） |

## 九、Redis 事务（★★★★★）

- **用法**：**MULTI** 开启事务 → 命令入队 → **EXEC** 顺序执行；**DISCARD** 放弃事务；**WATCH** 监视 key，被改则事务失败。
- **特点**：**不保证原子性**—EXEC 后某条失败**不回滚**，后续仍执行；**持久性**依赖 RDB/AOF 配置。适合**批量命令串行执行**，而非严格 ACID 事务。

## 十、常用指令速查（★★★★★）

| 类型 | 常用指令 |
|------|----------|
| **String** | SET/GET、INCR/DECR、APPEND |
| **List** | LPUSH/RPUSH、LPOP/RPOP、LRANGE |
| **Set** | SADD、SREM、SPOP、SCARD |
| **Zset** | ZADD、ZREM、ZRANGE（WITHSCORES） |
| **Hash** | HSET、HGET、HGETALL |
| **Bitmap** | SETBIT、GETBIT |
| **HyperLogLog** | PFADD、PFCOUNT |
| **Geospatial** | GEOADD、GEODIST、GEORADIUS |
| **键** | KEYS、DEL、EXPIRE |
| **事务** | MULTI、EXEC、DISCARD、WATCH |
| **持久化** | SAVE、BGSAVE |

---

## Elasticsearch

---

## 一、ES 概述（★★★★★）

- **特点**：以**全文检索**为基础；使用**分析器、分词**处理文本，支持词干、停用词、同义词等；建立索引时**分词**，写入**倒排索引**；查询时对查询语句分词，在倒排索引中找文档，再按**相关性评分**（如 BM25、TF-IDF）排序。
- **流程**：文档 → **分词** → 词项 → **倒排索引**；查询 → 分词 → 查倒排索引 → **相关性打分** → 排序返回。

## 二、分词原理（★★★★★）

### 2.1 分析器（Analyzer）三部分

分析器是一条**流水线**，依次为：

| 环节 | 作用 |
|------|------|
| **字符过滤器（Character Filters）** | 对**原始文本**预处理：去/换特殊字符、HTML 转可读等；**可选** |
| **分词器（Tokenizer）** | **核心**：按规则把文本切成**词元（Token）**；如标准分词器按空格、标点切分 |
| **标记过滤器（Token Filters）** | 对词元再加工：**小写**、去**停用词**、**词干提取**等，便于索引与搜索 |

**示意**：
```
  原始文本 → [字符过滤器] → [分词器] → [标记过滤器] → 词元序列
```

---

### 2.2 常用 Tokenizer（分词器）

| 类型 | 输入示例 | 输出示例 | 特点 |
|------|----------|----------|------|
| **standard**（标准） | Hello, this is Elasticsearch! | ["hello","this","is","elasticsearch"] | 去标点、转小写，适合自然语言 |
| **whitespace**（空白） | Hello World, 你好 Elasticsearch | ["Hello","World,","你好","Elasticsearch"] | **仅按空格**切，标点保留 |
| **keyword**（关键词） | OrderID-12345 | ["OrderID-12345"] | **不切分**，整段作为一个词元，适合 **ID、手机号**等精确匹配 |
| **uax_url_email** | 请联系 support@example.com 或 https://cheko.cc | 邮箱和 URL 为**整体** | 识别 URL、邮箱，保持完整 |

**记忆**：standard 自然语言，whitespace 只看空格，keyword 不切（精确），uax_url_email 保 URL/邮箱。

## 三、倒排索引（★★★★★）

- **定义**：**反向索引**；先对文档**分词**得到词项，再建立「**词项 → 文档 ID 列表**」的索引；查词即可得包含该词的文档。
- **与正排**：正排是 **key（文档）→ value（内容）**；倒排是 **value（词项）→ key（文档列表）**。
- **优势**：无需扫描全部文档，通过**词典 + 倒排表**快速定位包含某词的文档，**全文检索效率高**。

**示例**：  
文档 1：苹果是一种水果。 文档 2：香蕉是一种热带水果。 文档 3：苹果和香蕉都是水果。  
倒排索引：**苹果 → [1, 3]**；**香蕉 → [2, 3]**。搜「苹果」即得文档 1、3。

**示意**：
```
  正排： 文档ID → 内容
  倒排： 词项 → [文档ID1, 文档ID2, ...]
```

## 四、相关性算法（★★★★★）

- **作用**：**相关性得分**决定排序，得分越高越符合查询意图。
- **算法**：由 **Lucene** 计算；常用 **TF-IDF**、**BM25**（ES 默认）。BM25 为 TF-IDF 改进版，长文档与参数可调性更好。

### 4.1 影响相关性的主要因素

| 因素 | 含义 | 影响 | 举例 |
|------|------|------|------|
| **查询类型** | 不同查询方式对应不同打分规则 | **match** 会算相关性；**term** 偏精确匹配，不参与复杂评分 | match「手机」考虑词频与相关性；term 只匹配完全一致的「手机」 |
| **词频（TF）** | 词在**当前文档**中出现次数 | 出现越多，得分越高 | 文档 A 出现 10 次「手机」比 B 出现 1 次得分高 |
| **逆文档频率（IDF）** | 词在**所有文档**中的稀有程度 | 越稀有，贡献得分越高 | 「iPhone17」比「手机」更稀有，区分度更强 |
| **文档长度归一化（Norms）** | 关键词在**短文档 vs 长文档**中的代表性 | 短文档中出现一次更有代表性 | 标题里出现一次「iPhone」比长文中一次更突出 |
| **字段权重（Boost）** | 手动提高某字段重要性 | 被 Boost 的字段匹配得分更高 | title boost=2，优先于 description |

## 五、ES 与传统关系型数据库对比（★★★★★）

| 对比维度 | Elasticsearch（ES） | 传统关系型数据库（如 MySQL） |
|----------|---------------------|------------------------------|
| **类型** | **分布式搜索引擎**，**文档型**存储 | **关系型**，**表结构**存储 |
| **核心功能** | **全文搜索**、复杂查询、日志分析、**实时检索** | 结构化存储、**事务**、**数据一致性** |
| **应用场景** | 全文检索（电商搜索）、日志分析、监控查询 | 订单、财务、用户信息等**强事务**场景 |
| **优点** | 搜索性能高、实时搜索、**分布式扩容方便**、全文检索强 | **ACID 事务**、一致性强、生态成熟、SQL 标准 |
| **缺点** | 部署运维复杂、查询学习成本高、**一致性弱** | 实时搜索弱、大数据扩展难、全文能力有限 |
| **数据结构** | **文档（JSON）**，半结构化 | **二维表**，结构化 |
| **分布式** | **原生**分布式集群，**横向扩展**能力强 | 需**分库分表**，架构复杂 |
| **事务** | 仅**文档级**简单操作，**事务弱** | **完整 ACID**，原子性、一致性有保障 |

## MongoDB

---

## 一、文档模型要点

- **文档**：一条记录 = 一个文档，结构为「字段-值」对，可嵌套文档与数组。
- **优势**：与编程语言中对象类型对应自然；**嵌入式文档和数组**减少多表连接，适合半结构化、嵌套数据。

## 二、数据类型（★★★★★）

### 2.1 BSON（Binary JSON）

- **定义**：**二进制**形式的类 JSON 存储格式；支持内嵌文档和数组，并扩展了 JSON 没有的类型（如 **Date、BinData** 等）。
- **与 JSON 对比**：

| 对比项 | BSON | JSON |
|--------|------|------|
| **数据表示** | **二进制** | **纯文本** |
| **类型支持** | 更多原生类型（日期、整型、浮点、**二进制**等） | 类型相对有限 |
| **存储与速度** | **更紧凑、读写更快**；体积可能略大 | 相对不紧凑、较慢 |
| **易读性** | 需专门工具查看 | **人类可读**，便于调试与日志 |
| **适用场景** | **数据库存储**，要求读写速度、复杂结构及扩展类型 | **网络传输**、开发调试 |
| **压缩** | 紧凑，压缩优化不如文本明显 | 文本压缩算法优化效果更好 |

### 2.2 GeoJSON（★★★★★）

- **定义**：描述**地理数据**的开放标准格式，**非 MongoDB 独有**；MongoDB **原生支持** GeoJSON，可存储与查询地理空间数据。
- **用途**：地理空间**索引与查询**，实现**位置服务、位置分析**等（如附近的人、范围查询）。

## 三、分片集群（★★★★★）

- **目的**：解决**海量数据**存储与读写扩展——单机磁盘、CPU、内存、网卡等瓶颈；通过**分片**将数据分布到多节点。
- **两种分片策略**：

| 分片类型 | 原理 | 适用场景 |
|----------|------|----------|
| **哈希分片** | 对**片键**字段做**哈希**，用哈希值在分片间划分数据 | **随机访问**为主，无明确范围；数据均匀分布 |
| **范围分片** | 按片键的**连续范围**划分，同一范围的数据在同一分片 | 数据**按某属性连续增长**（如**时间顺序**）；**范围查询**高效；片键选择不当会导致热点与性能下降 |

**记忆**：哈希→随机、均匀；范围→连续、范围查询（如时间）。

**示意**：
```
  哈希分片：  key → hash(key) → 分片索引 → 落在某分片
  范围分片：  key 在 [A,B) → 分片1；[B,C) → 分片2；...
```

## 四、高可用：副本集（Replica Set）（★★★★★）

- **组成**：一组 MongoDB 实例，包含 **1 个 Primary** 和 **多个 Secondary**。
- **读写**：**客户端（Driver）写只到 Primary**；Secondary 从 Primary **同步数据**，使副本集内数据一致，提供**高可用**。
- **同步机制**：通过 **oplog**（类似 MySQL 的 **binlog**）。Primary 完成写操作后，向 **local.oplog.rs** 集合写入一条 oplog；Secondary **持续从 Primary 拉取并应用** oplog，保持与 Primary 数据一致。

**示意**：
```
  客户端  →  写  →  Primary  →  oplog  →  Secondary 1
                        ↓                    Secondary 2
                  local.oplog.rs
```

**要点**：一主多从、写主读可从从、oplog 同步、local.oplog.rs。

## 大数据架构

---

## 一、系统架构原则（★★★☆☆）

| 原则 | 要点 | 解释（简） |
|------|------|------------|
| **可扩展** | 分布式环境 | 分布式运行，任务分配、资源管理、负载均衡，**横向扩展**计算与存储 |
| **可扩展** | 模块化设计 | 拆分为独立模块，便于设计、开发、维护与扩展 |
| **可管理** | — | 自动化管理、统一管理平台、可视化管理，提升效率与可靠性 |
| **数据安全** | 加密、访问控制、备份恢复、审计日志、持续监控 | 存储/传输/处理加密；权限控制；备份与恢复；操作审计；监控日志与访问模式 |
| **高性能** | 数据分区、分布式计算、并行处理、内存计算 | 数据分块并行；多节点计算合并；子任务同时执行；内存计算减 I/O 延迟 |
| **高可用** | 数据冗余、自动故障转移、负载均衡 | 多节点/设备复制；ZooKeeper 等自动切换；Haproxy 等负载均衡 |
| **稳定性** | 监控预警、性能调优、故障排除、升级维护 | 实时监控与告警；找瓶颈优化；快速排障；及时升级与维护 |

## 二、批处理 vs 流处理 vs 混合（★★★★★）

| 架构类型 | 概念 | 优势 | 局限 | 应用场景 |
|----------|------|------|------|----------|
| **批处理** | 大规模数据**批量**任务，**离线**执行；含采集、存储、处理、输出；按**时间间隔或触发**处理 | 可处理大规模数据，**吞吐高、稳定**，充分利用计算资源 | **实时性差**，无法满足实时分析 | 对实时性要求不高的大规模数据处理 |
| **流处理** | 数据**连续、无界**流式处理，**新数据到达即处理**；由数据流、运算符组成，流处理引擎管理 | **实时**处理，快速响应，适应数据快速变化 | 无法处理历史数据、可能漏数据，需考虑并发与一致性 | 实时分析、监控、推荐等 |
| **混合架构** | 同时使用批处理与流处理，数据在两种模式间按需转换 | 兼具批与流优点，可处理历史+实时，快速响应 | 需权衡架构与数据处理，管理维护成本高 | 既有历史又有实时需求的场景 |

## 三、Lambda 架构（★★★★★）

### 3.1 三层结构

| 层次 | 概念 | 支撑技术 |
|------|------|----------|
| **批处理层** | 负责**所有批处理**，存储**全量数据集**并计算**批处理视图** | Hive、Spark-SQL、MapReduce 等 |
| **加速层/速度层** | 用**流式计算**实时处理**当前数据**，弥补批处理高延迟 | Storm、Spark Streaming、Flink 等 |
| **服务层** | 以批处理层与加速层结果为数据源，提供**低延时查询与即席查询** | 关系型库，或 Kylin、Presto、Impala、Druid 等 OLAP |

**示意**：
```
  数据源 → [批处理层] → BatchView ──┐
       ↘ [加速层] → RealtimeView ──┼→ [服务层] → 查询
```

### 3.2 批处理层与加速层分开设计的原因

| 原因 | 说明 |
|------|------|
| **容错性** | 加速层数据持续写入批处理层；批处理层**重算全量**后可覆盖加速层数据，**修正加速层错误** |
| **复杂性隔离** | 加速层用**增量算法**处理实时数据，复杂度高于批处理层，需隔离 |
| **横向扩容** | 数据量或负载增大时，分层便于**增加机器**维持性能 |

**二者对比**：加速层处理**最近增量数据流**，不断更新 RealtimeView；批处理层处理**全量数据集**，得到 BatchView。

### 3.3 Lambda 实现中各技术角色

| 层次 | 技术 | 功能 |
|------|------|------|
| **批处理层** | Hadoop | 大规模**批量**数据处理；从 Kafka 读数据进行批处理 |
| **加速层** | Spark / Storm | Spark：微批准实时、吞吐与复杂计算（ML、图）；Storm：**纯流**、亚秒级、低延迟与可靠性 |
| **服务层** | HBase / Cassandra | 实时访问与更新；HBase 基于 HDFS 列存；Cassandra 宽列、高吞吐 |
| **服务层** | Hive | 在 Hadoop 上建**可查询视图**，HiveQL→MapReduce，便于离线查询分析 |

**Spark vs Storm**：Spark 偏吞吐与复杂逻辑，Storm 偏**极致实时**。

### 3.4 Lambda 优缺点

| 类型 | 描述 |
|------|------|
| **优点** | **容错性好**：可修复算法或重算视图；**查询灵活**：批/实时存不同存储，多接口（SQL、REST 等）；**易伸缩**：分布式、弹性调度；**易扩展**：分层解耦、插件化 |
| **缺点** | 技术栈复杂，**两套系统**开发维护成本高、代码重复；全场景覆盖编码开销大；重部署与迁移成本高 |

## 四、Kappa 架构（★★★★★）

### 4.1 核心思想与组件

- **思想**：**去掉批处理层**，数据通道用**消息队列**替代；以**流处理为主**，数据存数据湖，需离线或重算时从消息队列**重放**数据即可。
- **数据流**：数据视为**持续事件流** → 进入 **Kafka/Pulsar**（高吞吐、持久化、可重放）→ **Flink/Kafka Streams/Spark Structured Streaming** 流处理（窗口、聚合、状态管理）→ 结果写入 **Redis/ClickHouse/ES** 或数据湖（Hudi、Iceberg）。
- **可回放**：算法或程序变更时，**重置消费位点、重放历史流**即可重新出结果，无需单独批处理程序。

| 核心组件 | 内容 |
|----------|------|
| **消息传输层** | Kafka 等，高吞吐、低延迟、**持久化与重放** |
| **流处理层** | Flink、Storm 等，实时计算，低延迟结果输出至服务层 |
| **数据存储** | HBase、Cassandra 等，支持快速查询 |

### 4.2 Kappa 优缺点

| 类型 | 描述 |
|------|------|
| **优点** | **实时与离线代码统一**，维护方便，**统一数据口径**，避免两套逻辑不一致 |
| **缺点** | 消息中间件**缓存与回溯**有性能瓶颈；多实时流**关联时顺序**问题易导致数据丢失；**抛弃离线模块**，失去离线计算更稳定的特点 |

---

### 4.3 Lambda vs Kappa（★★★★★ 必背）

| 对比内容 | Lambda | Kappa |
|----------|--------|-------|
| **复杂度与维护成本** | **两套系统**（批+流），复杂度高，开发维护成本高 | **一套系统**（流），复杂度低，成本低 |
| **计算开销** | 批处理和实时**同时常开**，计算开销大 | **必要时**全量计算（重放），计算开销相对小 |
| **实时性** | 满足实时性 | 满足实时性 |
| **历史数据处理能力** | **批式全量**，吞吐大，**历史数据处理能力强** | **流式全量**，吞吐相对低，历史数据处理能力**较弱** |

## 五、IOTA 架构（★★★☆☆）

- **特点**：**边缘计算** + **统一数据模型**；数据流连续、一致。
- **思路**：设定**标准数据模型**（如“主-谓-宾”“对象-事件”），在**数据产生、计算、查询**各环节分散计算，**统一模型贯穿**，支持即时查询。
- **要点**：Edge SDK 做校验、格式转换、边缘聚合；实时缓存（Kudu/HBase）+ 历史沉淀（Dumper→HDFS）。
- **优点**：简化架构（无批处理层）、低延迟（边缘计算）、统一数据模型。**缺点**：历史分析依赖流式重放，效率瓶颈；乱序、延迟、幂等处理难；全量日志存储成本高。

## 六、典型架构与技术归属（★★★★★）

### 6.1 某网奥运架构

| 层级 | 技术 | 用处 |
|------|------|------|
| **数据采集/集成** | Flume | 实时收集、聚合、传输日志 → Kafka |
| | Nginx | 负载均衡，保障采集稳定 |
| | Kafka | 消息队列，缓存实时数据，解耦生产消费 |
| | Zookeeper | 为 Kafka 等提供协调，管理集群状态与配置 |
| | ETL | 离线数据提取、转换、加载 |
| | Sqoop | 关系库与 Hadoop 间**批量**导入导出 |
| **数据存储** | MemSQL | 内存库，实时结果或热数据 |
| | HBase | 分布式 NoSQL，海量半/非结构化，高并发读写 |
| | HDFS | 分布式文件系统，大规模离线数据 |
| **数据计算** | Spark Streaming | 流计算，实时统计、分析 |
| | Spark | 通用分布式计算，批量与复杂逻辑 |
| | Impala | 实时交互式 SQL 查询（HDFS/HBase） |
| | Hive | HQL→MapReduce，离线分析 |
| | MapReduce | 离线批量 Map/Reduce 模型 |
| **数据展现** | 当日概览、赛事回顾 | 可视化呈现 |

---

### 6.2 某网络广告平台（Lambda 形态）

| 层级 | 技术 | 用处 |
|------|------|------|
| **批处理层** | HDFS | 存原始数据 |
| | Hive | 离线分析 HDFS，HQL 处理 |
| | MySQL | 存 Hive 处理后的结构化结果 |
| **服务层** | Kafka | 消息队列，缓存数据 |
| | View1–View3 | 批处理视图 + 实时视图，封装查询 |
| | JavaQuery | 业务查询接口，调视图 |
| **加速层** | Spark Streaming | 实时处理 Kafka 数据 |
| | Redis | 缓存加速层结果 |

---

### 6.3 某证券日志大数据系统

| 层级 | 技术 | 用处 |
|------|------|------|
| **逻辑处理层** | Filebeat | 轻量级**日志收集** → Kafka |
| | Kafka | 缓存日志，解耦收集与处理 |
| **存储计算层** | CDH | 大数据计算集群（Hadoop 生态） |
| | ElasticSearch | **日志库**，检索与分析 |
| | OpenTSDB | **指标库**，时序数据 |

---

### 6.4 某电商智能决策系统

| 层级 | 技术 | 用处 |
|------|------|------|
| **逻辑处理层** | Kafka | 缓存 B 端实时流 |
| | Flink | 实时流计算、作业提交 |
| **存储计算层** | CDH | 计算集群 |
| | Hive | **日志库**，批量存储与分析 |
| | Tair | **实时计算结果**存储 |

## 领域驱动设计（DDD）

---

## 一、DDD 三层边界总览

```
  大的边界（业务版图）   →  领域、子域（核心/支撑/通用）
  小的边界（团队协作）   →  统一语言、限界上下文、上下文映射（防腐层等）
  代码边界               →  实体、值对象、聚合/聚合根、领域服务、领域事件、仓储与工厂、应用层编排
```

**出处**：Eric Evans《领域驱动设计：软件核心复杂性应对之道》——在软件核心处应对复杂性。

## 二、大的边界：领域与子域（★★★☆☆）

### 2.1 领域与子域

| 概念 | 含义 |
|------|------|
| **领域** | 系统要解决的**业务问题空间**，如“电商交易” |
| **子域** | 领域中**可相对独立**的问题区域，如“商品管理”“交易下单”“支付结算”“库存管理”“营销优惠”“售后服务” |

**目的**：先划子域，把复杂度拆小，把精力集中在**最能体现业务价值**的部分。

---

### 2.2 子域分类（★★★★★）

| 类型 | 英文 | 含义 | 策略 | 电商示例 |
|------|------|------|------|----------|
| **核心子域** | Core | **企业差异化竞争力**所在 | 做深模型、持续演进 | 优惠计算引擎、库存与履约协同、售后规则 |
| **支撑子域** | Supporting | 业务需要但非核心竞争力 | 介于核心与通用之间 | 内容运营、活动页搭建 |
| **通用子域** | Generic | 通用能力、可替代 | **尽量复用**成熟方案，少占研发资源 | 统一登录、短信邮件、通用支付渠道接入 |

## 三、小的边界：统一语言与限界上下文（★★★★★）

### 3.1 统一语言（Ubiquitous Language）

- **定义**：业务与研发**共用**的一套业务术语与定义，并落实到**代码命名、接口字段、测试、文档、监控**中。
- **目的**：保证“**讨论的概念**”和“**实现的概念**”一致，减少误解与口径漂移；用语言约束模型一致性。
- **示例**：“取消订单”与“关闭订单”口语相近，规则不同——取消（用户主动）、超时关闭（系统自动）、风控关闭（风险判定）；代码中应体现为 `OrderStatus`、`CloseReason`、`CancelReason` 等，而非混为一谈。

### 3.2 限界上下文（Bounded Context）

- **定义**：在**明确边界内**，模型与语言一致；**跨边界**可同名不同义，通过**契约**协作。
- **作用**：避免不同业务语义混在一起导致模型“污染”（例如把营销规则写进订单模型，营销一变就要改订单逻辑）。

**电商示例（若干限界上下文）**：

| 上下文 | 典型概念 |
|--------|----------|
| **商品上下文** | 商品、SKU、类目、上下架、图文详情 |
| **交易上下文** | 订单、订单项、价格快照、收货地址、订单状态机 |
| **营销上下文** | 优惠券、活动、满减规则、凑单、会员价 |
| **库存上下文** | 可售库存、预占、扣减、回补、库存流水 |
| **支付上下文** | 支付单、渠道流水、退款单、对账 |
| **履约物流上下文** | 发货单、包裹、物流轨迹、签收 |

**污染示例**：把“优惠计算”直接写进交易上下文的订单类 → 营销规则（活动级、常变）会倒逼频繁改订单算价逻辑，即**订单被营销污染**。

### 3.3 上下文映射与防腐层（★★★☆☆）

**上下文映射**：描述多个限界上下文之间的**集成方式与依赖关系**，使跨团队协作与演进成本可控。

| 关系类型 | 含义 |
|----------|------|
| **客户/供应商** | 下游提需求，上游配合 |
| **顺从者** | 下游被迫适配上游 |
| **防腐层（ACL）** | **翻译与适配层**，隔离外部模型，防止外部模型污染核心领域 |
| **共享内核** | 少量共享模型由多上下文共同维护 |

**防腐层**：对接外部系统或遗留系统时，外部模型与本系统语言不一致。防腐层把外部模型**翻译**成本系统的领域概念，保证核心领域不被污染。例如第三方支付渠道返回渠道特有状态码、错误码 → 支付上下文的 ACL 翻译为统一的“支付成功/支付失败/待确认”等领域概念再向交易上下文发布事件；更换渠道时只需改 ACL，不动交易模型。

## 四、代码边界（★★★★★）

### 4.1 实体（Entity）

- **定义**：以**身份标识**区分；同一实体在生命周期内**属性可变**，**身份不变**；通常承载业务行为与状态演化。
- **示例**：订单（Order）有 `orderId`；地址、状态、备注可改，仍是同一订单。商品（Product）有 `productId`；**交易侧**宜用“价格快照/商品快照”，避免下单后商品改价影响历史订单。

### 4.2 值对象（Value Object）

- **定义**：由**属性**描述，**无独立身份**，通常**不可变**；相等性由属性值决定。
- **示例**：价格快照、收货地址（某次下单时的快照）、金额等。

### 4.3 聚合与聚合根（Aggregate & Aggregate Root）

- **聚合**：**一致性边界**。聚合内的业务不变式（强约束）应在**一次事务或一次一致性操作**内维护；**外部不能直接改内部对象**，必须通过**聚合根**统一入口修改。
- **聚合根**：聚合对外的唯一入口，负责保证聚合内规则不被绕开。
- **示例**：订单聚合（Order 为聚合根）含订单项、价格明细、收货信息。不变式示例：已支付后不许改应付金额；关闭后不许发货；同一订单同一时刻只能一个状态。应由聚合根方法如 `pay()`、`close(reason)`、`ship()` 保证，而非在控制器里直接改字段。

**示意**：
```
  [外部]  →  只能通过聚合根  →  [Order]  →  内部  OrderItem、PriceDetail、Address 等
```

### 4.4 领域服务（Domain Service）

- **定义**：当业务操作**涉及多个聚合**，或**难以归属**于单一实体/值对象时，用领域服务承载；仍在**领域层**，表达**业务规则**而非技术流程。
- **示例**：优惠分摊——订单多订单项，会员折扣后需把优惠分摊到各订单项（便于按项退款）；分摊规则不属于单个订单项，也不宜放在应用层脚本，适合做成领域服务如 `PricingService` / `PromotionApportionService`。

### 4.5 领域事件（Domain Event）

- **定义**：表示“**领域内发生的重要业务事实**”，用于在边界内外**传播变化、解耦协作**；强调**业务语义**，而非技术日志。
- **用法**：上游**发布**事件，下游**订阅**后执行自己的业务反应；常与限界上下文协作结合。

### 4.6 仓储与工厂（Repository & Factory）

| 概念 | 作用 |
|------|------|
| **仓储（Repository）** | 以“**集合**”方式**获取与保存聚合**，隐藏持久化细节；领域层不直接写 SQL |
| **工厂（Factory）** | **创建复杂聚合**，隐藏构造过程与校验逻辑 |

**目的**：领域模型不被数据库结构牵着走，保持领域层业务表达的纯粹性。

**示例**：`OrderRepository` 提供 `findById`、`save` 等；`OrderFactory` 在创建订单时完成：生成订单号、固化价格快照、校验库存可售、写入初始状态等，使“创建订单”不散落在控制器与多个服务类中。

### 4.7 应用层编排（Application Layer）

- **应用层**：负责**用例编排**——把一次业务请求串起来：校验权限、加载聚合、调用领域方法、持久化、发布事件。
- **领域层**：负责**业务规则本身**（“能不能做、做了以后状态怎么变”）。

**示例**：“支付订单”流程：读取订单聚合 → 调用 `order.pay()` → 生成支付单 → 调支付网关 → 收到回调后发布 `PaymentSucceeded` 事件。**编排**在应用层；**订单能否支付、支付后状态如何变化**在领域层。

## 五、小结

**DDD 三步**：  
1）大边界：领域、子域（核心/支撑/通用）；  
2）小边界：统一语言、限界上下文、上下文映射与防腐层；  
3）代码边界：实体、值对象、聚合/聚合根、领域服务、领域事件、仓储与工厂、应用层编排。

---

---

## 分布式系统

---

## 一、CAP 理论（★★★★★）

### 1.1 三性定义

**结论**：分布式系统**最多同时满足两个**，无法三者兼得；实践中**分区容错必然存在**，只能在**一致性 C** 与**可用性 A** 之间权衡。

| 概念 | 定义 |
|------|------|
| **一致性（Consistency）** | 写请求后**各相关节点数据均变化**，读请求能读到**变化后的数据**；多副本一致 |
| **可用性（Availability）** | 节点**收到请求就要处理并返回结果**，即使数据可能有问题（如返回旧数据）也不能拒绝 |
| **分区容错性（Partition Tolerance）** | 节点间**通信出现故障形成分区**时，系统仍能继续运行，**各分区可独立对外服务** |

**一致性**：一次写后，所有节点都更新，读能拿到新数据 → 称“发生了一致性变化”。  
**可用性**：能收请求就必须响应，哪怕返回的是过期数据（如三天前的数据）。  
**分区**：网络不可靠，节点间通信出问题即“分区”；**分区容错** = 出现分区后系统仍运行、分区可独立服务。  
**分区容错 vs 可用性**：分区容错强调“**通信故障导致分区**后各分区独立服务”；可用性强调“**非通信故障下**系统整体对外服务”。

---

### 1.2 CAP 组合与系统实现

| 组合 | 含义 | 典型例子 |
|------|------|----------|
| **CP**（一致性+分区容错） | 分区期间**宁可不可用**，也要保证数据一致 | **Zookeeper**：写经 Leader、多数派确认；Leader 失联或不足多数则**暂停服务**，直到重新选主 |
| **AP**（可用性+分区容错） | 分区期间**允许数据不一致**，保证继续可用 | **Eureka**：心跳丢失时**自我保护**，不剔除实例、不删注册信息，仍可查询，保持可用 |
| **CA**（一致性+可用性） | **分布式环境下无法实现**（无法避免分区） | — |

**示意**：
```
  分区发生时：
  CP → 暂停部分服务，保证一致（如 ZK 停写）
  AP → 继续服务，允许各分区数据不一致（如 Eureka 保护）
```

## 二、BASE 理论（★★★★★）

- **来源**：eBay 架构师 Dan Pritchett，2008 年 ACM；基于 CAP 的**一致性与可用性权衡**，牺牲强一致换可用性，允许**一段时间不一致**，**最终一致**。
- **思路**：通过业务设计，牺牲强一致性获得可用性，允许中间状态，最终达到一致。

| 概念 | 定义 |
|------|------|
| **基本可用（Basically Available）** | 遇不可预知故障时，**允许损失部分可用性**（如响应变慢、功能降级） |
| **软状态（Soft State）** | 允许系统存在**中间状态**，且该状态不影响整体可用性；**不同节点副本同步可有延迟** |
| **最终一致性（Eventually Consistent）** | 所有数据副本**经一段时间同步后**，**最终达到一致**，不要求实时强一致 |

## 三、分布式事务（★★★★★）

### 3.1 什么是分布式事务

- **定义**：事务的参与者、资源、事务管理器等分布在**不同节点**；一次业务涉及**多个服务/多库**，要么**全成功**，要么**全失败**。
- **典型场景**：下单 = 扣库存 + 扣优惠 + 生成订单，跨订单服务、库存服务、优惠服务等；需保证这些操作的整体一致性。
- **本质**：保证**不同数据库/服务**间的**数据一致性**。

### 3.2 刚性事务：2PC（二阶段提交）（★★★★★）

**两阶段**：**投票阶段** → **提交/回滚阶段**。

| 阶段 | 内容 |
|------|------|
| **阶段一（投票/预处理）** | ① 协调者向所有参与者发送事务内容，**询问**能否提交；② 各参与者**执行事务**，写 Undo/Redo 日志；③ 参与者向协调者反馈 **Yes/No** |
| **阶段二（提交/执行）** | 协调者根据反馈决定：**全 Yes** → 发 **Commit**，参与者提交并释放资源，回 ACK，协调者完成事务；**有 No 或超时** → 发 **Rollback**，参与者用 Undo 回滚并释放资源，回 ACK，协调者中断事务 |

**示意**：
```
  协调者          参与者1    参与者2
     | 询问         |          |
     |------------->| 执行+日志 |
     |------------->| 执行+日志 |
     |<--Yes--------|          |
     |<--Yes-------------------|
     | Commit/Rollback         |
     |------------->|          |
     |------------->|          |
     |<--ACK--------|          |
     |<--ACK-------------------|
```

**2PC 优缺点**：

| 优点 | 缺点 |
|------|------|
| 实现原理简单 | **性能**：两阶段内参与者**阻塞**，占资源 |
| | **单点故障**：协调者挂了，参与者无法释放资源或完成事务 |
| | **数据不一致**：发 Commit 后若网络异常或协调者崩溃，可能只有部分参与者提交，导致不一致 |
| | **保守**：依赖超时判断，容错差，难适应动态网络 |

### 3.3 柔性事务：TCC（Try-Confirm-Cancel）（★★★★★）

- **来源**：Pat Helland 2007 年论文《Life beyond Distributed Transactions: an Apostate's Opinion》。
- **与 2PC 对比**：① **无单点**，由主业务方发起，业务活动管理器可**集群**；② **同步阻塞**减轻，引入超时与补偿，资源以业务逻辑形式预留，**粒度更小**；③ **补偿机制**由业务活动管理器控制一致性。

| 阶段 | 含义 | 要点 |
|------|------|------|
| **Try** | **尝试执行** | 完成**业务检查**（一致性），**预留**必需业务资源（准隔离）；如库存检查+预留“可用库存” |
| **Confirm** | **确认执行** | **真正执行业务**，不再做检查，只使用 Try 预留的资源；**幂等**，失败需重试 |
| **Cancel** | **取消执行** | **释放** Try 阶段预留的资源；**幂等**；异常处理同 Confirm |

**示例**：订单+库存。Try：检查库存是否够、预留可用库存（如扣减“可用数量”）；Confirm：真正扣减库存、生成订单；Cancel：释放预留库存。

**缺点**：需把原一个接口拆成 **Try、Confirm、Cancel 三个接口**，**开发复杂度高**。

### 3.4 柔性事务：本地消息表（★★★★★）

- **来源**：eBay Dan Pritchett，2008 年 ACM；通过**消息**异步确保分布式任务执行，实现**最终一致性**。
- **核心**：**本地库中消息表**记录发送状态 + **MQ** 异步投递；业务数据与消息表**同一库、同一本地事务**写入。

**发送方**：  
① 本地事务内：写**业务数据** + 写**消息表**；② 投递消息到 **MQ**；③ 若消费方 **ACK**，可删除消息表记录；④ 未 ACK 则**重试**；消费方需**幂等**防重复处理。

**消费方**：  
① 消费 MQ 消息，执行业务逻辑；② 本地事务成功即处理成功；③ 失败则重试；④ 业务失败可向生产方发**补偿消息**（回滚等）。

**优缺点**：

| 优点 | 缺点 |
|------|------|
| 实现简单，开发成本低 | 与业务**高耦合**，占业务库资源，量大可能影响库性能 |
| | 消息**异步**，存在**延迟**；需处理重复消息（幂等） |

## 大模型应用

---

## 一、检索增强生成（RAG）（★★★★★）

### 1.1 核心思想与流程

- **思想**：**“检索 + 生成”**——用大模型生成能力，同时用**外部知识**弥补模型记忆与时效局限。
- **三阶段**：**索引阶段** → **检索阶段** → **生成阶段**。
- **五环节**：**数据准备** → **向量化** → **索引存储** → **检索增强** → **生成答案**。

**示意**：
```
  数据准备 → 向量化 → 索引存储（向量库）
       ↑                    ↓
  用户问题 → 检索增强（向量检索/重排）→ 问题+文档 → 大模型 → 生成答案
```

### 1.2 数据准备

- **目标**：把原始文档变成**结构化、可处理**的文本，质量直接影响检索与回答。
- **常见步骤**：
  1. **清洗**：去掉 HTML、PDF 页眉页脚等多余格式。
  2. **分块**：长文档切成小块再向量化与检索；三种策略：
     - **语义切分**：每块**语义独立**（如问答对、完整段落）。
     - **结构切分**：按**标题层级**切，保留文档结构。
     - **递归切分**：文本过长时按**字符数**递归切，兼顾连贯与长度。

### 1.3 向量化与索引存储

- **向量化**：用 **Embedding 模型**（如 BERT、Sentence-BERT、BGE）把**文本块**转成**向量**（高维语义表示）；语义相近的文本向量接近（如“苹果手机”与“iPhone”）。
- **索引存储**：向量存入**向量数据库**（如 Milvus、Faiss、Elasticsearch、Pinecone），支持**相似度检索**；通常同时存**原始文本**，便于检索后回溯内容。

### 1.4 检索增强

- **步骤**：① **问题向量化**—用户问题转为向量；② **向量检索**—在库中找相似文本块；③ **重排序（Reranker）**—用 Reranker 模型（如 Cohere Reranker）对初筛结果精排，把最相关的放前面。
- **增强手段**：
  - **多阶段检索**：先用 **BM25** 等**关键词**粗筛，再用**向量检索**精排。
  - **混合检索**：关键词 + 向量检索结果用 **RRF（倒数排名融合）** 合并，兼顾“语义相似但缺词”和“有词但语义偏”的问题。

### 1.5 生成答案

- **输入**：**用户问题 + 检索到的相关文档** 一起交给大模型。
- **关键**：**Prompt 设计**——明确“仅依据文档作答、不知道就说不知道、标注来源、处理冲突”等；示例：先结论后依据、用[编号]标来源、冲突时分别说明、无关则说明无法回答并建议更精确提问。

## 二、模型上下文协议（MCP）（★★★★★）

- **提出**：**Anthropic**，2024 年 11 月。
- **定位**：为 **LLM 与外部世界**提供统一的**“通用接口”**（类似 USB-C），让模型**即插即用**地访问各类数据源与工具。
- **机制**：符合 MCP 标准的外部资源（数据库、API、文档库、本地文件等）可直接接入；模型**不需关心底层实现**，通过 MCP 即可理解和使用，从而**实时获取数据、调用工具、执行任务**，减少对静态知识的依赖。
- **价值**：① **标准化接入**—一次集成多场景复用；② **增强模型能力**—如从 GitHub 拉代码、读日历、操作本地文件；③ **架构模块化**—协议统一后扩展与维护更简单。

## 三、智能体（Agent）（★★★☆☆）

### 3.1 智能体简介

- **定义**：以**目标为导向**的智能系统；能**感知环境**，基于观测**推理与规划**，通过**行动**（调用工具/接口）影响环境，使任务向目标收敛；本质是**“决策—行动—反馈”**闭环。
- **与 Chatbot、Workflow 对比**：

| 类型 | 特点 | 局限 |
|------|------|------|
| **Chatbot** | 以**对话生成与交互**为主，可拟人、可聪明 | 不必调用外部系统，不必对结果负责 |
| **Workflow** | **步骤可复现、路径可控**，适合稳定流程与合规 | **适应性弱**，输入/约束/目标一变常需改流程 |
| **Agent** | **目标牵引**，在循环中选工具、执行、读反馈、修正计划，**自适应、可迭代** | 依赖工具质量与观测可靠性，需成本与回合约束 |

**示意**：Agent = 感知 → 推理/规划 → 行动（工具/接口）→ 反馈 → 再决策（闭环）。

### 3.2 智能体架构

- **含义**：为让 LLM 系统像“会做事的人”一样完成目标，在**模型外**搭建的**结构与组件组织**；解决的是“系统如何感知、决策、调用工具、记忆与修正、何时交付”，模型是其中的“大脑”。
- **两种常见模式**：**ReAct**、**规划-执行**。

---

### 3.3 ReAct 模式（Reason + Act）

- **思想**：**“推理”与“行动”交替**；每步从环境获取反馈，再根据反馈做下一步决策，形成**闭环**，而不是一次性生成答案。
- **过程**：模型通过**工具调用、检索、计算**等得到“地面事实”，再据此一步步推进直到完成。
- **劣势**：每步都可能回模型决策，**调用次数与时延**易偏高；可能**循环失控**（不停检索/尝试）；依赖**工具质量与观测可靠**，否则易在错误反馈上越走越偏；工程上需**预算约束**（最大回合、最大工具调用、超时降级）。
- **适用**：**信息不完整、需边做边看**的任务（多源检索综合、按日志排障、试探性改代码并观察、需计算与验证的问答）。**不适合**：流程固定、强一致性、低成本的场景（更适合预编排工作流）。

### 3.4 规划-执行模式（Plan-and-Execute）

- **思想**：把任务明确分为**规划阶段**和**执行阶段**。规划阶段根据目标生成**可执行计划**（步骤顺序、依赖、资源与工具、每步验收标准）；执行阶段**按计划逐步执行**，每步产出结果作为下一步输入，把复杂任务拆成可管理、可检查的子任务链。
- **劣势**：**计划与现实易偏差**——信息不全、环境变化时，规划易理想化，执行中易遇依赖不可用、约束改变，导致偏航与返工；若死守计划反而拖慢。需配套**动态重规划**：观测不满足假设时，可调整后续步骤或回到规划阶段重做，并保留已验证成果。
- **适用**：**可拆解、可验收、目标明确**的多步骤任务（如先列提纲与资料再写报告）。**不适合**：**强探索式**、目标模糊、路径不确定、需频繁试错的场景（易变成“计划—推翻—重来”），此类更适合 ReAct 或执行阶段嵌入 ReAct。

## 法律法规

---

## 一、保护对象与是否需要申请

| 法律法规 | 保护对象 | 是否需申请 |
|----------|----------|------------|
| **著作权法** | 文学、艺术、科学领域内独创并能以一定形式表现的智力成果 | 不需要，作品完成即保护 |
| **计算机软件保护条例** | 计算机程序及其有关文档（软件作品） | 不需要；登记便于举证 |
| **专利法** | 发明创造：发明、实用新型、外观设计 | **需要申请**；有效期从**申请日**起算 |
| **商标法** | 商标，区别商品或服务来源 | **需要申请**；核准之日起保护 |
| **反不正当竞争法** | 经营者合法权益、竞争秩序；**商业秘密**（技术+经营，须有保密措施） | 商业秘密无期限，公开后公众可用 |

## 二、保护期限（★★★★★）

| 客体 | 权利类型 | 期限 |
|------|----------|------|
| 公民作品 | 署名权、修改权、保护作品完整权 | **无限制** |
| 公民作品 | 发表权、使用权、获得报酬权 | 作者终生及死后 **50 年**（第 50 年 12 月 31 日） |
| 单位作品 | 发表权、使用权、报酬权 | 首次发表后 **50 年** |
| 公民软件作品 | 署名权、修改权 | **无限制** |
| 公民软件作品 | 其他权利 | 终生及死后 50 年；合作开发以最后死亡作者为准 |
| 注册商标 | 注册商标专用权 | **10 年**；期满前 6 个月内续展；注册人死亡或倒闭 1 年后未转移可注销 |
| 发明专利 | 发明专利权 | **20 年**（申请日起） |
| 实用新型 | 实用新型专利权 | **10 年**（申请日起） |
| 外观设计 | 外观设计专利权 | **15 年**（申请日起） |
| 商业秘密 | — | 不确定，公开后公众可用 |

## 三、职务作品 / 职务软件 / 职务发明归属（★★★★★）

- **作品**：利用单位技术条件或合同约定归单位 → 著作权归**单位**（署名权除外）；其他 → 归**作者**，单位有权在业务范围使用。
- **软件**：本职工作明确开发目标、属本职工作结果、使用单位资源并由单位承担责任 → 归**单位**（署名权除外）。
- **专利**：执行本单位任务、利用单位物质技术条件、**离职/退休/调动 1 年内**与原单位工作相关 → 归**单位**（署名权除外）。

**署名权**：永久归开发者/作者，**不因离职而丧失**；擅自替换署名侵犯署名权。

## 四、委托与合作、商标与专利归属

- **委托创作/软件**：未约定则归**创作方/开发方**，可约定归委托方。
- **合作创作/开发**：**共同享有**。
- **商标**：**先申请**先得（知名商标抢注除外）；同一天申请则**先使用**（须证据）；无法证明则协商，协商不成抽签。
- **专利**：**先申请**先得；同一天申请则**协商**，不能同时驳回双方。

## 五、侵权判定（★★★★★）

- **不侵权**：个人学习研究欣赏、合理引用、教学科研、馆藏复制、免费表演、室外艺术品临摹拍摄、翻译成少数民族/盲文等。
- **侵权**：未经许可发表、未经合作作者同意将合作作品当个人作品发表、未参与创作而署名、歪曲篡改、剽窃、未付报酬、未经许可使用版式设计等。

## 六、专利法（★★★★★）

- **客体**：**发明**（产品/方法及其改进）、**实用新型**（产品形状、构造及其结合）、**外观设计**（形状、图案、色彩及其结合，适于工业应用）。
- **发明与实用新型授权条件**：**新颖性**（申请前国内外无同样）、**创造性**（相对现有技术有突出特点和显著进步）、**实用性**（能制造或使用且有积极效果）。**外观设计**：与国内外已发表的不相同、不相近似。
- **不授予专利权**：科学发现、智力活动规则和方法、疾病诊断治疗方法、动植物品种、用原子核变换方法获得的物质等。
- **不视为侵权**：专利权人售出产品后的使用等；申请日前已制造相同产品等在**原有范围**继续；临时过境；专为科研和实验。
- **终止**：未缴年费、书面放弃、复审未通过等。
