# 数据库系统 · 考点速记（易背版）

> **分卡 + 完整真题**：[12-数据库系统/总目录.md](./12-数据库系统/总目录.md)（`build_exams_12.py` 合并 [数据库系统.pdf](../选择真题-分类/数据库系统.pdf)）。  
> 基于《12-数据库系统》整理。**超级重点**；选择与案例常考；三级模式与两级独立性、关系运算、**E-R 与转换**、**函数依赖与范式**、**并发问题与封锁**、完整性、触发器、反规范化与索引、OLAP、分片需熟记。各节 **案例（航空运营智能管理平台）** 与《考纲/航空运营智能管理平台项目素材-核心.md》对齐，把数据库理论与 **强一致票务、旅客主数据、PB 级分析、高并发** 结合表述。

---

## 一、章节定位与考法

| 项目 | 内容 |
|------|------|
| **章节** | 数据库系统（超级重点 ★★★★★） |
| **核心** | **三级模式与两级映射**、关系代数（并/差/交/笛卡尔积/选择/投影/连接）、**E-R 三要素与联系转换**、**函数依赖与无损/保持依赖**、**1NF～4NF 与 BCNF**、**ACID 与并发问题**、**X/S 锁与封锁协议**、完整性、触发器、反规范化与索引准则、OLAP、水平/垂直分片 |
| **考法** | 模式归属、E-R 转关系、范式判断与分解、候选键图示法、丢失修改/脏读/不可重复读、封锁协议、案例默写 |

**案例（航空运营智能管理平台）**：数据层要支撑 **订单/库存/行程/旅客/航班/检修/分析宽表** 等；**OLTP** 侧重购票事务与一致性，**OLAP/湖仓** 侧重 T+1 与 PB 级查询；下文用同一项目串起模式、设计、范式与并发。

---

## 二、三级模式与两级独立性（★★★★★）

- **外模式**：用户视图，局部数据逻辑结构。**概念模式**：全体数据逻辑结构，DBA。**内模式**：存储结构、物理顺序、索引，系统程序员。
- **口诀**：外局改全内务——外（外模式）局（局部）改（概念）全（全体）内（内模式）务（物理）。
- **物理独立性**：应用程序与磁盘数据独立。**逻辑独立性**：应用程序与逻辑结构独立，逻辑结构变时应用可不变。

**案例（航空运营智能管理平台）**：

- **外模式**：旅客端仅见 **可脱敏行程**；运营端见 **统计视图**；不同角色 **视图 + 权限**。
- **概念模式**：全库 **航班、舱位、订单、旅客、设备** 等全局关系与约束。
- **内模式**：**分库分表、索引、分区、列存**（分析侧）等物理设计。
- **独立性**：索引调整、分片迁移（**物理独立**）；增加字段通过视图兼容（**逻辑独立**）。

---

## 三、关系运算（★★★☆☆）

- **并 R∪S**、**差 R-S**、**交 R∩S**、**笛卡尔积 R×S**、**投影 π**、**选择 σ**、**θ 连接/自然连接 R⊳⊲S**。选择=WHERE，投影=SELECT 列。

**案例（航空运营智能管理平台）**：**σ** 筛选 **某航班、某日期可售票**；**π** 取 **航班号、起降时间** 投影；**⋈** 将 **订单、旅客、航班** 按外键连接生成行程单；**差** 可做 **两期报表差异** 分析。

---

## 四、E-R 与逻辑设计（★★★★★）

### 4.1 E-R 三要素

- **实体**：矩形。**属性**：椭圆（单值）；双线椭圆多值；虚线椭圆派生）。**联系**：菱形，标注 1:1、1:n、m:n。

### 4.2 E-R 转关系模式

- **1:1**：独立关系模式（两实体主码+联系属性）或与任一端合并（另一端主码+联系属性放入）。
- **1:n**：独立关系模式（n 端主码）或与 **n 端合并**（1 端主码+联系属性放入 n 端）。
- **m:n**：联系**单独成表**，属性=两实体主码+联系属性，主码为两主码组合。

### 4.3 合并 E-R：合并→消除冲突（属性/命名/结构）→消除冗余。

**案例（航空运营智能管理平台）**：

- **实体**：**旅客、航班、舱位、订单、设备、检修工单** 等。
- **联系**：**旅客—订单** 1:n；**旅客—航班** 经 **订单/行程** m:n；**航班—飞机** n:1；**订单—支付流水** 1:n 等。
- **转换**：**旅客↔航班** 购票为 **m:n** → **订单明细/行程表**（含票价、状态）；**航班—机场** 多出现在 **航班表** 或 **航段表**。
- **合并**：整合旧系统时 **消除同名异义**（如“航班号”格式统一）。

---

## 五、函数依赖与范式（★★★★★）

- **完全函数依赖**：X→Y 且 X 的任一真子集不能决定 Y。**部分函数依赖**：X→Y 但 Y 不完全依赖于 X（存在真子集决定 Y）。**传递函数依赖**：X→Y，Y→Z，Y↛X，Z⊈Y。
- **候选键**：图示法——依赖作图，入度为 0 的属性集为起点，能遍历全图即为候选键；若不能则加入部分中间顶点再试。
- **无损分解**：ρ={R1,R2} 对 F 无损 ⇔ (R1∩R2)→(R1−R2) 或 (R1∩R2)→(R2−R1)。
- **范式口诀**：一范式无重复，二范式主键独，三范式无传递，BCNF 传递无，四范式多值除。

| 范式 | 关键 |
|------|------|
| **1NF** | 属性原子，不可再分 |
| **2NF** | 1NF + 无非主属性对候选码的**部分**函数依赖 |
| **3NF** | 1NF + 无非主属性对候选码的**传递**函数依赖 |
| **BCNF** | 任一非平凡依赖左侧均为**候选码** |
| **4NF** | BCNF + 无多值依赖（或多值依赖拆开） |

- **不规范问题**：**绒绣茶山**——冗余、修改异常、插入异常、删除异常。

**案例（航空运营智能管理平台）**：

- **候选键**：订单表 **(订单号)** 或复合键（视业务）；库存行 **(航班, 舱位, 日期)** 等需画图验证。
- **范式**：旅客姓名、证件号 **1NF 原子**；订单明细避免 **部分依赖**（拆出票价规则表）；费用、税费避免 **传递依赖** 到非码属性；促销规则若破坏 BCNF 需分解或接受约束。
- **反例**：把 **旅客手机号** 冗余在每张票里省连接 → **绒绣茶山**，可用 **冗余 + 触发器同步** 或 **读侧宽表** 折中。

---

## 六、事务与并发控制（★★★★★）

### 6.1 ACID

- **原子性**：全做或全不做。**一致性**：从一个一致状态到另一一致状态。**隔离性**：并发事务互不干扰，可串行化。**持久性**：提交后永久。

### 6.2 并发问题

| 问题 | 描述 |
|------|------|
| **丢失修改** | 两事务同读同改，后提交覆盖前提交 |
| **读脏数据** | 读到他事务未提交的修改，他事务回滚后数据无效 |
| **不可重复读** | 同一事务内两次读同一数据，中间被他事务更新，结果不同 |
| **幻读** | 同一事务内两次按条件读，中间他事务插入/删除，结果集不同 |

### 6.3 封锁

- **X 锁（排他）**：可读可写；其他事务不能加任何锁。**S 锁（共享）**：可读不可写；其他可加 S，不能加 X；全部 S 释放后才能加 X。
- **一级**：修改前加 X 直到结束；防丢失修改，不防脏读、不可重复读。**二级**：一级 + 读前加 S 读后即释；防丢失、脏读，不防不可重复读。**三级**：一级 + 读前加 S 直到结束；防丢失、脏读、不可重复读。**两段锁**：扩展阶段只能加锁、收缩阶段只能解锁；可串行化，可能死锁。
- **粒度**：粒度小并发高、开销大；粒度大并发低、开销小。

**案例（航空运营智能管理平台）**：

- **ACID**：购票事务 **原子** 扣库存+写订单；**一致** 不超售、金额平衡；**隔离** 防春运并发 **丢失修改**；**持久** 提交后账务可追溯。
- **并发问题**：两笔同时改 **余票** → **丢失修改**；读到未提交支付 → **脏读**；舱位中途调价 → **不可重复读**；统计行数时他方退票 → **幻读**。
- **封锁**：热点舱位行 **X 锁** 串行化更新；报表用 **MVCC/S 锁** 与业务隔离级别配合；**行级锁** 粒度契合 **5000 TPS** 与素材 **分布式事务**。

---

## 七、完整性、触发器、反规范化与索引（★★★★★）

- **实体完整性**：主键不为空。**参照完整性**：外键取被参照表主键值或空；级联/受限/置空等。**用户定义完整性**：业务规则。
- **触发器**：BEFORE/AFTER × INSERT/UPDATE/DELETE；INSERT 用 NEW，DELETE 用 OLD，UPDATE 用 NEW 与 OLD。
- **反规范化**：增加冗余以提升性能；带来冗余、修改/插入/删除异常。
- **索引准则**：常查询、少更新的属性建索引；索引过多影响增删改；小表可不建。

**案例（航空运营智能管理平台）**：

- **完整性**：**订单号主键**；**航班+日期** 外键关联 **航班表**；**退票** 时 **级联/限制** 依业务；**身份证、银行卡** 符合 **用户定义完整性** 与脱敏规则。
- **触发器**：**出票后** 写行程、**写审计**；**退票** 回补库存并记日志（**NEW/OLD**）。
- **反规范化**：看板 **冗余汇总字段**（日销量）换 **查询性能**，靠 **批处理或触发器** 维护一致。
- **索引**：**航班日期、旅客证件哈希** 等高频条件；写多读少的流水表慎加索引。

---

## 八、备份、OLAP 与分片（★★★☆☆）

- **物理备份**：拷贝数据文件、日志。**逻辑备份**：导出为 SQL 等逻辑对象。**完全**：全量。**增量**：自上次备份以来变化。**差异**：自上次完全备份以来变化。
- **OLTP**：日常事务、当前细节、面向应用。**OLAP**：分析决策、历史聚集、面向主题、多维。
- **钻取**：变粒度；**切片/切块**：部分维定值；**旋转**：行列互换。
- **水平分片**：元组划分，**并**恢复。**垂直分片**：属性划分，**连接**恢复；关键字各分片均需保留。

**案例（航空运营智能管理平台）**：

- **备份**：**核心订单库** 全量+日志，**RPO/RTO** 对齐 **99.99%** 与素材 **备份保留≥3 年**；分析库 **增量** 同步。
- **OLTP vs OLAP**：**在线购票** 为 OLTP；**航线需求预测、设备故障模型、旅客偏好** 为 OLAP（主题域、钻取切片）。
- **分片**：订单按 **旅客 ID/时间** **水平分片** 撑 **单表 50 亿行**；**旅客敏感列** **垂直分片** 到加密库，查询 **连接** 回业务库。

---

## 九、速记清单（考前扫一眼）

1. **三级模式**：外局改全内务；物理/逻辑独立性。
2. **E-R 转关系**：1:1 独立或合并一端；1:n 独立或并 n 端；m:n 单独成表。
3. **范式**：1NF 原子；2NF 无部分依赖；3NF 无传递；BCNF 左侧皆码；4NF 多值除。绒绣茶山。
4. **候选键**：图示法入度 0 起点遍历。
5. **无损**：(R1∩R2)→(R1−R2) 或 (R1∩R2)→(R2−R1)。
6. **并发**：丢失修改、脏读、不可重复读、幻读；X/S 锁；一二三级封锁协议；两段锁可串行化。
7. **触发器**：BEFORE/AFTER、INSERT/UPDATE/DELETE；NEW/OLD。
8. **分片**：水平并、垂直连。

---

*文档由《12-数据库系统》提炼，保留必要表格与图形含义，便于背诵与考场回忆；案例与《考纲/航空运营智能管理平台项目素材-核心.md》一致，可按需裁剪写入论文。*