# 常见应用层 · 传输层 · 网络层协议

> 源讲义：[../10-计算机网络-raw.md](../10-计算机网络-raw.md) · 10.3

---

10.3.常见的应用层协议（次重点★★★☆☆)

10.3.1.常见的应用层协议（次重点★★★☆☆)


| 应用层是最高层， 为用户提供各种网络应用服务， 如 HTTP、FTP、SMTP  等。TCP/IP  模型中也有应用层，包含了类似的各种应用协议，如 HTTP  用于网页浏览、SMTP  用于电子邮件传输等。 常见的应用层协议如下 ，选择题主要考察这些协议占用的端 口 以及对应的传输层协议是 TCP还是 UDP。 |
| --- |



| 传输层 | 协议名称 | 占用端 口 | 功能描述 |
| --- | --- | --- | --- |
| TCP | POP3 | 110 | 用于从邮件服务器将邮件下载到本地客户端，允许用户访问并管理存储在服务器上的电子邮件。 |
| TCP | FTP | 20（数据端 口 ）、 21（控制端 口 ） | 用于在网络上进行文件传输，20 端 口传输数据 ，21 端 口传输控制命令 ，实现文件的上传和下载。 |
| TCP | Telnet | 23 | 提供远程登录服务 ，允许用户通过网络远程连接到其他计算机系统 ，像在本地一样操作远程计算机。 |
| TCP | HTTP | 80 | 用于在 Web 浏览器和 Web 服务器之间传输超文本，是万维网数据通信的基础 ，实现网页内容的请求和响应。 |
| TCP | SMTP | 25 | 用于发送电子邮件，将邮件从发件人的邮件客户端传输到邮件服务器 ， 以及在不同邮件服务器之间传输邮件。 |
| UDP | DHCP | 67（服务器端）、 68（客户端） | 用于自动分配  IP 地址等网络配置参数 ，减少手动配置的工作量，提高网络管理效率。 |
| UDP | SNMP | 161（管理端）、 162（代理端） | 用于网络管理 ，管理端通过此协议收集网络设备（如路由器、交换机等） 的信息 ，进行监控和管理。 |


|  | TFTP | 69 | 用于简单的文件传输 ，常用于设备初始化时的文件传输等场景 ，相对 FTP 更简单、 占用资源少。 |
| --- | --- | --- | --- |
|  | DNS | 53 | 负责将域名解析为对应的  IP 地址 ，或者反向解析 ，是实现网络访问中域名与  IP 地址转换的关键服务。 |

10.3.2.传输层协议（次重点★★★☆☆)


| 传输层负责在不同主机上的进程之间提供可靠的端到端通信服务 ，主要协议有 TCP  和 UDP。这一章节主要是要知道 TCP 和 UDP 的特点 ，选择题会辨析就可以。 |
| --- |


TCP： 在 IP  协议提供的不可靠数据服务的基础上，采用了重发技术 ，为应用程序提供了一个可靠的、面向连接的、全双工的数据传输服务。TCP  协议一般用于传输数据量比较少 ，且对可靠性要求高的场合。

UDP：是一种不可靠的、无连接的协议，可以保证应用程序进程间的通信，与 TCP  相比，UDP是一种无连接的协议，它的错误检测功能要弱得多。可以这样说，TCP  有助于提高可靠性，而 UDP则有助于提高传输速率。UDP 协议一般用于传输数据量大 ，对可靠性要求不是很高 ，但要求速度快的场合。

10.3.3.网络层协议（次重点★★★☆☆)


| 网络层主要负责将分组从源节点传输到 目 的节点 ，实现网络间的路由选择 ，主要协议是 IP 。 TCP/IP  模型的网络层也承担着类似的功能 ，核心协议也是 IP ，负责处理网络地址、路由选择等，在这一层上两者的功能和核心协议基本相同。这里有哪些协议需要掌握 ，选择题会考。 |
| --- |



| 协议名称 | 功能描述 |
| --- | --- |
| IP | 负责网络层核心功能 ，包含数据包的路由与传输 ，不保障可靠性 |
| ARP/RARP | 承担  IP 地址与物理地址相互转换的工作 |
| ICMP | 处理网络中的错误检测与报告 ，辅助  IP 协议运行 |
| IGMP | 负责多播组的管理以及组播数据的传输 |

## 系统架构设计师 · 真题汇编

> 来源：[计算机网络.pdf](../../选择真题-分类/计算机网络.pdf)。**完整题干、选项、正确答案与解析**。

### （2022年11月·第13题）

以下关于 HTTPS 和 HTTP协议的描述中，不正确的是（）
问题（1）
（A）HTTPS协议使用加密传输
（B）HTTPS协议默认服务端口号是443
（C）HTTP协议默认服务端口是80
（D）电子支付类网站应使用 HTTP协议

**正确答案：** D

**解析**

本题考察的是HTTP 与 HTTPS 协议的基础知识和区别，这是网络通信安全中的基础内容。
A选项HTTPS协议使用加密传输：正确。HTTPS是在HTTP协议基础上加入SSL/TLS层，实现数据传输的
加密与安全认证，能够有效防止数据被窃听和篡改。
B选项HTTPS协议默认服务端口号是443：正确。HTTPS 通信默认使用 TCP 的 443 端口进行连接。
C选项HTTP协议默认服务端口是80：正确。HTTP 协议默认使用 TCP 的 80 端口，是 Web 通信的基
础端口。
D选项电子支付类网站应使用 HTTP协议：错误。电子支付涉及用户敏感信息的传输（如银行卡信息
、密码等），必须保证数据传输的加密与完整性，因此应使用 HTTPS 协议，而非明文传输的 HTTP
协议。
 因此，选项 D 正确。

### （2022年11月·第14题）

电子邮件客户端通过发起对（）服务器的（）端口的 TCP 连接来进行邮件发送。
问题（1）
（A）POP3
（B）SMTP
（C）HTTP
（D）IMAP
问题（2）
（A）23
（B）25
（C）110
（D）143

**正确答案：** B,B

**解析**

本题考察的是电子邮件协议及其对应端口号的基本知识。
 电子邮件系统中常用的协议包括 SMTP、POP3 和 IMAP 等，其中 SMTP 用于邮件的发送，POP3 和
IMAP 用于邮件的接收。
问题 1：SMTP（Simple Mail Transfer Protocol，简单邮件传输协议）专门用于邮件发送，客户端
通过与 SMTP 服务器建立 TCP 连接将邮件投递到邮件服务器。其他选项：POP3（Post Office
Protocol 3）用于从服务器接收邮件；HTTP主要用于网页浏览；IMAP（Internet Message Access
Protocol）用于邮件的在线访问与管理。
 因此，选择SMTP协议（B）。
问题 2：SMTP 默认使用 TCP 25 端口进行通信，是邮件发送的标准端口。其他选项：23 端口是
Telnet 服务端口；110 端口是 POP3 接收邮件的端口；143 端口是 IMAP 接收邮件的端口。
 因此，使用25端口（B）。

### （2021年11月·第10题）

Internet 网络核心采取的交换方式为（）。
问题（1）
（A）分组交换
（B）电路交换
（C）虚电路交换
（D）消息交换

**正确答案：** A

**解析**

本题考察的是Internet 网络核心的交换方式。
 Internet 核心网使用的是分组交换（Packet Switching），它将数据分为一个个分组（数据包
）进行传输，每个分组独立路由并在目的地重新组装。
A选项：正确，分组交换又称包交换，采用存储-转发方式，适用于突发性数据通信，是 Internet
的核心交换方式。
B选项：错误，电路交换需要为通信双方建立一条固定的物理通道（如传统电话网络），不适合数据
网络的突发性和多路复用需求。
C选项：错误，虚电路交换是一种逻辑电路建立方式，更多应用于早期的 X.25、帧中继等网络，不
是 Internet 核心的主要方式。
D选项：错误，消息交换一次传输整个报文，且需要中间节点缓存整个消息后再转发，延迟较高，不
适合实时通信。
 因此，选项 A 正确。

### （2020年11月·第12题）

在Linux系统中，DNS的配置文件是（），它包含了主机的域名搜索顺序和DNS服务器的地址。
问题（1）
（A）/etc/hostname
（B）/dev/host.conf
（C）/etc/resolv.conf
（D）/dev/name .conf

**正确答案：** C

**解析**

本题考察的是Linux 系统中 DNS 配置文件的位置与作用。
 在 Linux 和大多数 UNIX 系统中，DNS 查询需要参考一个指定的配置文件，该文件用于定义域名
解析时的 DNS 服务器地址、默认域名以及搜索顺序。
A选项 /etc/hostname：该文件用于存储当前主机的主机名，而不是 DNS 服务器信息，错误。
B选项 /dev/host.conf：不存在该路径，且 host.conf 文件的正确路径为 /etc/host.conf，它用
于设置主机名解析顺序，但不用于直接配置 DNS 服务器地址，错误。
C选项 /etc/resolv.conf：是 DNS 的主要配置文件，包含 nameserver、domain、search 等指令
，指定 DNS 服务器 IP、默认域名和搜索域名顺序，正确。
D选项 /dev/name.conf：不存在该文件路径和用途，错误。
 因此选择 C。

### （2020年11月·第31题）

下面关于 Kerberos 认证的说法中，错误的是（）。
问题（1）
（A）Kerberos 是在开放的网络中为用户提供身份认证的一种方式
（B）系统中的用户要相互访问必须首先向 CA 申请票据
（C）KDC 中保存着所有用户的账号和密码
（D）Kerberos 使用时间戳来防止重放攻击

**正确答案：** B

**解析**

本题考察的是Kerberos 认证协议的基本原理与特点。
 Kerberos 是一种基于对称密钥加密的网络认证协议，主要通过可信的第三方——密钥分发中心
（KDC）来完成身份认证。KDC 由认证服务器（AS）和票据授予服务器（TGS）组成，能够实现单点
登录并防止重放攻击。
A选项 Kerberos 是在开放的网络中为用户提供身份认证的一种方式：这是 Kerberos 的核心设计目
标，它能够在不安全的网络环境中确保身份认证的安全性，正确。
B选项 系统中的用户要相互访问必须首先向 CA 申请票据：Kerberos 不使用 CA（Certificate
Authority，证书颁发机构），而是依赖 KDC 分发票据，因此该描述错误。
C选项 KDC 中保存着所有用户的账号和密码：在 Kerberos 系统中，KDC 确实保存着用户的账户信
息和经过加密保护的密码，正确。
D选项 Kerberos 使用时间戳来防止重放攻击：这是 Kerberos 防御重放攻击的机制之一，正确。
 因此，选项 B 错误。

### （2019年11月·第11题）

TCP端口号的作用是（）。
问题（1）
（A）流量控制
（B）ACL过滤
（C）建立连接
（D）对应用层进程的寻址

**正确答案：** D

**解析**

本题考察的是TCP协议中端口号的作用，属于传输层相关的基础知识点。
A选项流量控制：是TCP协议的一项功能，用于控制数据发送速率，防止接收方处理不过来，但这与
端口号无关，因此错误。
B选项ACL过滤：ACL（访问控制列表）用于网络设备上的数据包过滤，通常基于IP地址和端口等条件
进行控制，本质是设备配置策略，与端口号本身作用无直接关系，因此错误。
C选项建立连接：虽然TCP协议确实需要建立连接，但这指的是三次握手过程，与“端口号”的作用
无直接对应，因此该选项错误。
D选项对应用层进程的寻址：端口号的核心作用就是用于标识主机上的具体应用进程，传输层协议通
过端口号将接收到的数据交付给对应的应用程序，这是正确选项。
 因此，选项 D 正确。

### （2019年11月·第12题）

Web 页面访问过程中，在浏览器发出 HTTP 请求报文之前不可能执行的操作是（）。
问题（1）
（A）查询本机 DNS 缓存，获取主机名对应的 IP 地址
（B）发起 DNS 请求，获取主机名对应的 IP 地址
（C）发送请求信息，获取将要访问的 Web 应用
（D）发送 ARP 协议广播数据包，请求网关的 MAC 地址

**正确答案：** C

**解析**

本题考察的是网络通信过程及协议工作顺序的基本概念。
 浏览器访问 Web 页面时，会先进行域名解析（查询本机缓存或向 DNS 服务器请求），接着在本地
网络中可能会通过 ARP 获取网关 MAC 地址，然后建立 TCP 连接，最后才会发出 HTTP 请求报文。
因此，在发出 HTTP 请求报文之前，与 HTTP 层直接交互的操作是无法进行的。
A选项 查询本机 DNS 缓存：这是域名解析的第一步，可以在 HTTP 请求之前执行，正确。
B选项 发起 DNS 请求：如果本地缓存没有命中，会向 DNS 服务器查询 IP 地址，这个过程也在
HTTP 请求之前执行，正确。
C选项 发送 HTTP 请求信息：HTTP 请求报文是在建立 TCP 连接并完成网络底层准备后才会发送的
，因此在发出 HTTP 请求报文之前，不可能提前执行此操作，正确答案。
D选项 发送 ARP 协议广播数据包：当目标 IP 在本地网络中或需要找到网关时，ARP 请求会在
HTTP 报文发送前进行，以便获取 MAC 地址，正确。
 因此，选项 C 正确。

### （2019年11月·第13题）

以下关于 DHCP 服务的说法中，正确的是（）。
问题（1）
（A）在一个园区网中可以存在多台 DHCP 服务器
（B）默认情况下，客户端要使用 DHCP 服务需指定 DHCP 服务器地址
（C）默认情况下，DHCP 客户端选择本网段内的 IP 地址作为本地地址
（D）在 DHCP 服务器上，DHCP 服务功能默认开启

**正确答案：** A

**解析**

本题考察的是DHCP（动态主机配置协议）的工作原理与配置特性。
 DHCP 是一种在 TCP/IP 网络中自动分配 IP 地址及其他网络配置参数的协议，减少人工配置的复
杂性。
A选项 在一个园区网中可以存在多台 DHCP 服务器：在一个网络中可以部署多台 DHCP 服务器，以
实现冗余和负载分担，但需要确保它们的地址池不冲突，正确。
B选项 默认情况下，客户端要使用 DHCP 服务需指定 DHCP 服务器地址：DHCP 客户端通过广播
DHCP Discover 消息自动寻找可用的 DHCP 服务器，不需要手动指定地址，错误。
C选项 默认情况下，DHCP 客户端选择本网段内的 IP 地址作为本地地址：IP 地址的分配由 DHCP
服务器决定，不是客户端自行选择，错误。
D选项 在 DHCP 服务器上，DHCP 服务功能默认开启：大多数操作系统或网络设备中，DHCP 功能需
要手动启用，默认并不开启，错误。
 因此，选项 A 正确。

### （2019年11月·第34题）

下列协议中与电子邮箱安全无关的是（）。
问题（1）
（A）SSL
（B）HTTPS
（C）MIME
（D）PGP

**正确答案：** C

**解析**

本题考察的是电子邮箱安全相关协议。
A选项 SSL：安全套接字层协议，提供网络通信的加密与身份验证，常用于邮件传输过程的安全防护
，属于与电子邮箱安全相关的协议，因此不选。
B选项 HTTPS：基于 SSL/TLS 的 HTTP 协议，也可用于保护 Web 端邮件收发的安全，因此与电子邮
箱安全相关，不选。
C选项 MIME：多用途互联网邮件扩展标准，用于支持电子邮件中的非 ASCII 文本、附件、多部分消
息等功能，与安全无关。与安全电子邮件相关的是 S/MIME（安全/多用途互联网邮件扩展），而不
是 MIME，因此此选项正确。
D选项 PGP：Pretty Good Privacy，一套用于加密与数字签名的应用，可用于加密电子邮件内容
，属于电子邮箱安全协议，因此不选。
 正确答案是 C。

### （2018年11月·第12题）

在客户机上运行 nslookup 查询某服务器名称时能解析出 IP 地址，查询IP地址时却不能解析出服
务器名称，解决这一问题的方法是（）。
问题（1）
（A）清除 DNS 缓存
（B）刷新 DNS 缓存
（C）为该服务器创建 PTR 记录
（D）重启 DNS 服务

**正确答案：** C

**解析**

本题考察的是DNS 正向解析与反向解析的原理。
 在 DNS 系统中，正向解析是根据域名查找对应的 IP 地址，这依赖于 A 记录；反向解析是根据
IP 地址查找对应的域名，这依赖于 PTR（Pointer）记录。
A选项 清除 DNS 缓存：只能清除本地缓存记录，不会解决服务器没有反向记录的问题，错误。
B选项 刷新 DNS 缓存：与清除缓存类似，仅更新缓存内容，无法生成不存在的反向解析记录，错误
。
C选项 为该服务器创建 PTR 记录：PTR 记录是反向 DNS 查询所需的记录，没有此记录就无法通过
IP 查询出域名，因此这是根本解决办法，正确。
D选项 重启 DNS 服务：只能重启 DNS 进程，不会自动添加缺失的 PTR 记录，错误。
 因此，本题答案为 C。

### （2018年11月·第13题）

如果发送给 DHCP 客户端的地址已经被其他 DHCP 客户端使用，客户端会向服务器发送（）信息包
拒绝接受已经分配的地址信息。
问题（1）
（A）DhcpAck
（B）DhcpOffer
（C）DhcpDecline
（D）DhcpNack

**正确答案：** C

**解析**

本题考察的是DHCP 协议中的报文类型与作用。
 DHCP（Dynamic Host Configuration Protocol）在为客户端分配 IP 地址的过程中，会使用多个
不同类型的消息进行交互。
A选项 DhcpAck：由 DHCP 服务器发送，用于确认客户端请求并分配 IP 地址，表示地址分配成功
，错误。
B选项 DhcpOffer：由 DHCP 服务器发送，向客户端提供可用 IP 地址的“报价”，错误。
C选项 DhcpDecline：由 DHCP 客户端发送，如果客户端在收到 DHCP Ack 后通过冲突检测发现 IP
已被占用，就会向服务器发送 DhcpDecline 报文拒绝使用该地址，这是正确答案。
D选项 DhcpNack：由 DHCP 服务器发送，用于拒绝客户端的请求或通知客户端租约已失效，与客户
端拒绝地址的场景不同，错误。
 因此，本题答案为 C。

### （2017年11月·第10题）

下面可提供安全电子邮件服务的是（）。
问题（1）
（A）RSA
（B）SSL
（C）SET
（D）S/MIME

**正确答案：** D

**解析**

本题考察的是安全电子邮件服务与 S/MIME 的概念。
A选项 RSA：一种非对称加密算法，可用于加密和数字签名，但它本身不是一种电子邮件服务协议。
B选项 SSL：安全套接层协议，用于保障网络通信安全，主要应用于网页浏览、VPN等场景，不是专
门的电子邮件安全协议。
C选项 SET：Secure Electronic Transaction，安全电子交易协议，主要用于电子商务支付，不是
电子邮件安全协议。
D选项 S/MIME：Secure/Multipurpose Internet Mail Extensions，对 MIME 在安全方面的扩展
，可实现邮件主体加密、数字签名、完整性保护、身份认证等安全功能，是专门用于安全电子邮件
服务的协议。
 因此，选项 D 正确。

### （2013年11月·第11题）

主机PC对某个域名进行查询，最终由该域名的授权域名服务器解析并返回结果，查询过程如下图所
示。这种查询方式中不合理的是（）。
问题（1）
（A）根域名服务器采用递归查询，影响了性能
（B）根域名服务器采用迭代查询，影响了性能
（C）中介域名服务器采用迭代查询，加重了根域名服务器负担
（D）中介域名服务器采用递归查询，加重了根域名服务器负担

**正确答案：** A

**解析**

本题考察的是DNS 解析原理。
 在DNS解析过程中，主要有两种查询方式：递归查询和迭代查询。
递归查询：由发起查询的DNS服务器（如本地域名服务器）完全处理查询过程。如果它没有所需
的结果，它会代表客户端发起后续查询，直到得到最终答案或者返回失败。此方式会增加该
DNS服务器的负担，因为它需要继续向其他服务器发起查询请求。
迭代查询：如果查询的DNS服务器无法提供结果，它会返回一个指向下一个合适DNS服务器的地址
，查询过程由请求方继续完成。这种方式通常会降低服务器负担，因为每个DNS服务器只需要提
供其职责范围内的信息，并不负责完成整个查询过程。
此图中中介域名服务器采用递归查询，根域名也是根域名服务器采用递归查询。但是假如根域名服
务器如果采用递归查询，会代替客户端继续查询并最终返回结果，这会导致根域名服务器的负担增
加。根域名服务器主要负责顶级域名的解析，不应该在此过程中参与递归查询，通常采用迭代查询
。
B选项：根域名服务器采用迭代查询，影响了性能：根域名服务器通常采用迭代查询，不会影响性能
，所以不选。
C选项：中介域名服务器采用迭代查询，加重了根域名服务器负担：中介域名服务器在此图中采用递
归查询，而不是采用迭代查询，描述错误，所以不选。
D选项：中介域名服务器采用递归查询，加重了根域名服务器负担：如果中介域名服务器采用递归查
询，它将代表客户端完成查询，最终返回结果。这会将中介服务器的负担增加，同时减少根域名服
务器的负担。描述错误，所以不选。
 因此，选择选项 A 。

### （2014年11月·第43题）

下列安全协议中（）是应用层安全协议。
问题（1）
（A）IPSec
（B）L2TP
（C）PAP
（D）HTTPS

**正确答案：** D

**解析**

本题考察的是不同安全协议在OSI模型中的所属层级。
A选项 IPSec：属于网络层（第三层）安全协议，为IP层提供加密、身份认证、数据完整性和防重放
等安全服务，因此不是应用层协议。
B选项 L2TP：属于会话层（第五层）协议，可结合传输层和数据链路层技术实现虚拟专线服务，也
不是应用层协议。
C选项 PAP：属于数据链路层（第二层）协议，是一种简单的基于用户名和密码的身份验证协议，多
用于PPP连接的认证过程，不是应用层协议。
D选项 HTTPS：基于HTTP并通过SSL/TLS加密实现的安全通信协议，运行在应用层，典型的应用层安
全协议，因此该说法正确。
 因此，正确答案是 D。

### （2023年11月·第7题）

下列（）不是SSL的特性。
问题（1）
（A）保密性
（B）可靠性
（C）完整性
（D）不可抵赖性

**正确答案：** D

**解析**

本题考察的是SSL协议的特性。
A选项保密性：SSL协议提供保密性，确保数据在传输过程中不被窃取或篡改。SSL通过加密算法实现
数据的加密，确保只有通信双方能够解密和读取数据内容。
B选项可靠性：SSL协议运行在传输层之上，通常基于TCP协议。由于TCP协议本身提供了可靠的数据
传输，SSL也间接继承了这种可靠性，确保数据可靠送达。
C选项完整性：SSL协议保证数据的完整性。在SSL通信过程中，数据在传输时会使用哈希算法（如
SHA）计算消息认证码（MAC），以检测数据是否被篡改。
D选项不可抵赖性：不可抵赖性通常由数字签名提供，确保发送方不能否认已发送的信息。而SSL协
议本身并不直接提供不可抵赖性功能，虽然它提供身份认证和数据加密，但不涉及不可抵赖性的保
证。
 因此，选项 D 正确。

### （2024年5月·第35题）

OSI定义了7层协议，其中除（）外，每一层均能提供相应的安全服务。
问题（1）
（A）应用层
（B）表达层
（C）会话层
（D）物理层

**正确答案：** C

**解析**

本题考察的是OSI安全架构中各层安全服务的适用性。
OSI参考模型将网络通信划分为七层：物理层、数据链路层、网络层、传输层、会话层、表示层和应
用层。根据安全架构的定义，几乎每一层都能配置适当的安全机制，如加密、认证、访问控制等
，但会话层是唯一在OSI安全体系中未专门定义安全服务的层次。
A选项应用层：支持如HTTPS、S/MIME等应用级加密与认证服务。
B选项表达层（应为“表示层”）：可处理数据加密、压缩等，支持安全编码。
C选项会话层：正确，在OSI安全架构中未指定特定的安全机制，主要负责会话控制而非数据安全。
D选项物理层：虽级别较低，但可通过如物理隔离、电磁屏蔽等方式增强安全性。
 因此，正确答案是 C。

### （2024年11月·第8题）

属于计算机传输层的协议是（）。
问题（1）
（A）TCP和UDP
（B）IP和ICMP
（C）FTP和SMTP
（D）TELNET和NFS

**正确答案：** A

**解析**

本题考察的是计算机网络分层模型中传输层的协议。
 掌握OSI七层模型及TCP/IP四层模型对应协议是重点内容。
A选项 TCP 和 UDP：正确。TCP（传输控制协议）和UDP（用户数据报协议）是传输层的两种核心协
议。TCP提供可靠、有序、面向连接的通信；UDP提供无连接、不可靠的通信，但效率高。二者都是
传输层的标准协议。
B选项 IP 和 ICMP：错误。IP（互联网协议）和ICMP（Internet Control Message Protocol）均属
于网络层协议。IP负责数据包的寻址和路由；ICMP用于发送控制消息，比如“目标不可达”等。
C选项 FTP 和 SMTP：错误。FTP（文件传输协议）和SMTP（简单邮件传输协议）都是应用层协议
，它们运行在TCP之上，提供文件传输和电子邮件服务。
D选项 TELNET 和 NFS：错误。TELNET（远程登录协议）和NFS（网络文件系统）也属于应用层协议
，分别用于远程终端访问和文件系统共享。
 因此，正确答案是 A。建议复习的时候看下图即可。

### （2024年11月·第20题）

路由器工作在 OSI 模型的（）。
问题（1）
（A）物理层
（B）数据链路层
（C）传输层
（D）网络层

**正确答案：** D

**解析**

本题考察的是计算机网络中OSI七层模型与网络设备的对应关系。
路由器（Router） 是一种连接多个网络、实现数据包路由选择与转发的设备。它的核心功能包括：
依据 IP地址 进行路径选择
转发数据包到目标网络
实现多个子网之间或不同网络之间的通信
这些功能明确对应的是 OSI模型的第三层：网络层（Network Layer）。
A选项物理层：主要负责物理信号的传输，如电压、电缆连接等，对应设备如中继器（Repeater）、
集线器（Hub）。
B选项数据链路层：负责节点之间的可靠传输，如帧的封装和MAC地址识别，对应设备如网桥
（Bridge）、交换机（Switch）。
C选项传输层：负责端到端的数据传输和错误恢复，如TCP、UDP协议，不是路由器的工作层次。
D选项网络层：是路由器的工作层次，它处理逻辑寻址、路由选择等核心功能。
 因此，选项 D 正确。

### （2025年11月·第28题）

物理层接口描述中，用于说明各项功能在时间上发生顺序的部分是（）。
问题（1）
（A）功能
（B）机械
（C）过程
（D）电气

**正确答案：** C

**解析**

本题考察的是物理层接口特性四要素的基本概念。
 在数据通信的物理层中，接口规范通常从以下四个方面进行描述：
机械特性（Mechanical Characteristics）：描述通信设备之间物理连接的机械结构，如接口形
状、引脚数量、尺寸、插头类型等。
电气特性（Electrical Characteristics）：描述信号的电气属性，如电压、电流、阻抗、信号
电平等。
功能特性（Functional Characteristics）：描述各个信号线或电路的功能，例如哪一条线传数
据信号、哪一条线表示控制信号。
过程特性（Procedural Characteristics）：描述信号在时间上的变化顺序，即各项功能在时间
维度上是如何协调、交互、传输的，用于规定通信双方的时序关系。
根据定义，过程特性用于规定“功能的发生顺序”，即信号何时发送、何时接收、握手时序如何。
A选项 功能：说明信号线的用途，不涉及时间顺序。
B选项 机械：描述接口的物理形态，与时间无关。
C选项 过程：描述信号发生的时序和步骤，正确。
D选项 电气：描述电压电平和传输特性，与时间顺序无关。
 因此，正确答案是 C. 过程。
