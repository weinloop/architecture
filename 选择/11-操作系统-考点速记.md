# 操作系统 · 考点速记（易背版）

> 基于《11-操作系统》整理。**次重点**；选择概念与计算；进程三态/五态、**PV 操作与信号量**、**死锁四条件与预防**、**页式/段式**、**虚拟内存与页面置换**（FIFO/LRU/Clock）、**文件索引**（直接/一级/二级间接）、**位示图**需熟记。各节 **案例（航空运营智能管理平台）** 与《考纲/航空运营智能管理平台项目素材-核心.md》对齐，把 OS 概念映射到 **容器化微服务、高并发票务、分布式事务、PB 级存储** 等工程语境。

---

## 一、章节定位与考法

| 项目 | 内容 |
|------|------|
| **章节** | 操作系统（次重点 ★★★☆☆） |
| **核心** | **进程三态/五态**、**同步与互斥**、**PV 操作**与前趋图、**死锁四条件**与预防、**页式地址**、**段式**、**页面置换算法**（FIFO/LRU/Clock）、**文件索引**（直接/一二级间接）、**位示图**计算 |
| **考法** | 状态辨析、S=-3 时阻塞进程数、前趋图 PV 填空、死锁条件、逻辑地址→物理地址、缺页次数、索引方式、位示图字数 |

**案例（航空运营智能管理平台）**：线上服务运行在 **Linux + 容器** 中，**进程/线程** 处理购票与流式任务；**调度、同步、虚拟内存、文件 I/O** 直接影响 **≤1 秒响应、5000 TPS、PB 级读写** 是否达标；下文用同一项目做直观映射（考场仍以教材定义与计算为准）。

---

## 二、进程状态（★★★☆☆）

- **三态**：**就绪**（只差 CPU，等调度）、**运行**（正占用 CPU）、**阻塞/等待**（等事件，有 CPU 也不能运行）。**口诀**：舅运祖宗。
- **五态**：新建态→就绪→运行→阻塞→终止态。**口诀**：新舅运祖宗。

**案例（航空运营智能管理平台）**：**工作线程** 就绪等待 **CPU**；运行中处理请求；**阻塞** 于 **磁盘 I/O、网络 read、锁等待、epoll 等事件**；高并发下大量线程阻塞在外部 **核验/支付 RPC** 时，表现为 **连接堆积与延迟上升**（与素材峰值场景相关）。

---

## 三、同步与互斥、PV 操作（★★★★★）

- **同步**：按既定顺序协调执行（如读等写满、写等读完）。
- **互斥**：同一时刻仅一进程访问临界资源（如打印机）。
- **P(S)**：S 减 1；若 S≥0 继续，若 S<0 阻塞并入阻塞队列。
- **V(S)**：S 加 1；若 S>0 继续，若 S≤0 从阻塞队列唤醒一进程，本进程继续。
- **计数信号量**：S≥0 表示可用资源数；**S<0 时 |S| 为阻塞进程数**（如 S=-3 表示 3 个进程阻塞）。
- **前趋图**：有向无环图，边表示前驱关系；前驱完成处写 V，后继开始处写 P；需为每条边设一信号量（初值 0）。

**案例（航空运营智能管理平台）**：**互斥** 体现在 **同一航班舱位库存**、**同一订单记录** 的临界区；**信号量/分布式锁** 控制并发扣减。**前趋图** 可对应 **购票流水线**：**运力校验完成 → 锁库存 → 支付 → 出票**，每步 **P/V** 协调顺序。注意：**跨服务** 还需 **分布式协调**（教材 PV 为单机经典模型）。

---

## 四、死锁（★★★★★）

**四必要条件**：① **互斥**（资源独占）；② **不剥夺**（已占资源不被强夺）；③ **请求与保持**（占着资源再申请其他）；④ **环路**（等待形成环）。

**预防**：破坏上述任一条件（如一次性分配、可剥夺、有序申请等）。

**案例（航空运营智能管理平台）**：**分布式事务** 若 **按不同顺序** 锁 **订单表与库存表**，可能 **环路等待**；预防可采用 **统一加锁顺序**、**超时回滚**、**避免持有锁再调外部慢接口**。数据库连接池耗尽也会出现 **类死锁** 的 **饥饿/阻塞**，需与素材中 **强一致票务** 设计一并考虑。

---

## 五、内存管理（★★★★★）

### 5.1 页式管理

- 逻辑地址 = **页号 + 页内地址**；页表：页号→物理块号；物理地址 = 物理块号×页大小 + 页内地址。
- 例：逻辑空间 64KB、页大小 1KB → 页内 10 位、页号 6 位；给定逻辑地址先拆出页号与页内地址，查页表得块号再算物理地址。

**案例（航空运营智能管理平台）**：**页式** 是 **JVM/进程堆、mmap 大文件** 的常见底层模型；定位 **逻辑地址→物理帧** 与 **容器内存限额** 排查相关。

### 5.2 段式管理

- 逻辑地址 = **段号 + 段内地址**；段表：段号→基址等；物理地址 = 段基址 + 段内地址。

**案例（航空运营智能管理平台）**：**段式** 强调 **逻辑模块**（代码段、数据段、栈）；业务上可类比 **不同微服务进程地址空间隔离**，越界访问触发 **保护错误**（与 **旅客敏感数据隔离** 的安全目标一致）。

### 5.3 虚拟内存与页面置换

- **虚拟存储**：部分装入、请求调入、置换；大程序小内存、多进程并发。
- **置换算法**：**FIFO**（淘汰最早进入）、**LRU**（淘汰最久未用）、**Clock**（环形+访问位，扫到 0 淘汰并置 0）、NUR、最优（理论）。FIFO 可能 Belady 异常。
- **缺页次数**：按访问序列模拟，缺页时按算法选一页淘汰再装入新页，统计缺页次数。

**案例（航空运营智能管理平台）**：**页式** 对应 **进程虚拟地址空间**；容器 **cgroup 内存上限** 触发 **OOM 或换页** 会影响延迟。**LRU/Clock** 贴近 **操作系统页缓存** 与 **Redis 等缓存淘汰** 思想；**T+1 扫描大表** 若访问模式 **随机** 易 **缺页**，需 **顺序读/分区** 改善局部性（与 **10PB+、单表 50 亿行** 场景相关）。

---

## 六、文件系统（★★★★★）

### 6.1 文件索引

- **直接地址**：索引节点中直接放数据块号，逻辑块号 0～k-1（k=直接项数）。
- **一级间接**：一项指向一个索引块，索引块内放数据块号；每块可存 块大小/地址项大小 个地址；逻辑块号范围 k～k+n-1（n=一索引块项数）。
- **二级间接**：一项指向一级索引块，一级索引块再指向数据块；范围再往后推。
- 例：5 直接、2 个一级、1 个二级；块 1KB、地址项 4B → 一索引块 256 项；块号 1 用直接，块号 518 用二级间接。

**案例（航空运营智能管理平台）**：传统 **inode 多级索引** 与 **分布式对象/块存储**（如大文件分块、元数据服务）思想相通；**PB 级湖仓** 中单表 **海量小文件 vs 大文件分块** 影响 **NameNode/元数据压力** 与 **查询延迟**（素材：PB 查询 ≤5～10 秒）。

### 6.2 位示图

- 每位表示一物理块使用情况；**字数 = 物理块总数 / 字长**（如 300GB/4MB=76800 块，字长 32 则 76800/32=2400 字）。

**案例（航空运营智能管理平台）**：**分布式存储集群** 管理海量 **物理块/对象**，**分配与回收** 可用 **位示图或等价结构** 跟踪空闲；考点计算与 **卷容量、块大小、字长** 公式一致即可。

---

## 七、速记清单（考前扫一眼）

1. **三态**：舅运祖宗；就绪=等 CPU，阻塞=等事件。
2. **PV**：P 减 1、S<0 阻塞；V 加 1、S≤0 唤醒；|S| 为阻塞数；前趋图前驱完 V、后继始 P。
3. **死锁**：互斥、不剥夺、请求保持、环路；预防=破其一。
4. **页式**：逻辑=页号+页内；物理=块号×页大小+页内；段式=段号+段内。
5. **置换**：FIFO 最早、LRU 最久未用、Clock 环形+访问位。
6. **索引**：直接 0～k-1；一级 k～k+256-1（1KB/4B）；二级再往后。
7. **位示图**：字数=总块数/字长。

---

*文档由《11-操作系统》提炼，保留必要表格与图形含义，便于背诵与考场回忆；案例与《考纲/航空运营智能管理平台项目素材-核心.md》一致，可按需裁剪写入论文。*
