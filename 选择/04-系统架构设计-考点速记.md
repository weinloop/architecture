# 系统架构设计 · 考点速记（易背版）

> 基于《04-系统架构设计》整理。**超级重点**；选择、案例、论文均考察；架构风格、ABSD、质量属性与场景、ATAM、敏感点/权衡点需熟记。各节 **案例（航空运营智能管理平台）** 与《考纲/航空运营智能管理平台项目素材-核心.md》对齐，用于论文与综合应用表述。

---

## 一、章节定位与考法

| 项目 | 内容 |
|------|------|
| **章节** | 系统架构设计（超级重点 ★★★★★） |
| **核心** | 4+1 视图、**ABSD** 三基础与六阶段、**架构风格**（数据流/调用返回/以数据为中心/虚拟机/独立构件/C2/过程控制）、架构复用与 DSSA、**九大质量属性**、**质量属性场景六要素**、**敏感点/权衡点**、**ATAM** 与效用树 |
| **考法** | 风格辨析、ABSD 阶段与质量场景、质量属性对策、场景六要素填空、敏感点 vs 权衡点、ATAM 阶段与效用树结构 |

**案例（航空运营智能管理平台）**：架构设计需同时回答 **“云原生微服务 + 分布式事务/缓存 + 实时流式 + 离线 PB 级数仓 + AI 检修 + 多终端 + 安全合规”** 如何组织，并与 **≥10 万并发、5000 TPS、≤1 秒购票响应、可视化 PB 查询≤5～10 秒、可用性≥99.99%** 等指标论证可追溯；下文用同一项目串起视图、ABSD、风格、复用与质量评估。

---

## 二、4+1 视图

| 模型 | 五视图口诀 |
|------|------------|
| 架构 4+1 | **罗凯进五常**——逻辑、开发、进程、物理、场景 |
| RUP/UML 4+1 | **落实进步用**——逻辑、实现、进程、部署、用例 |
| ABSD 4+1 | **逻实进配**（+ 用例） |

**案例（航空运营智能管理平台）**：

- **逻辑视图**：七大业务域（基础支撑、航信、旅客、票务、检修、数据服务、辅助管理）及领域关系、购票联动链路。
- **开发/实现视图**：微服务仓库划分、共享 SDK、CI/CD、网关与 BFF。
- **进程视图**：购票链路上的同步调用、异步消息、流式作业、分布式事务与批处理 T+1 的并发与同步关系。
- **物理/部署视图**：容器集群、多 AZ、负载均衡、缓存、消息、分布式存储与离线分析集群映射。
- **场景/用例视图**：旅客购票、航变批量退改签、实时告警、运营看板查询等驱动架构验证。

---

## 三、基于架构的软件开发 ABSD（★★★★★）

### 3.1 三个基础

1. **功能分解**：使用基于模块的内聚与耦合技术。
2. **选择体系结构风格**：实现质量与商业需求。
3. **软件模板**：利用已有系统结构。

### 3.2 特点

- 由**商业、质量、功能需求**组合驱动；**递归、迭代**；自顶向下细化到构件与类。
- **用例**捕获功能需求；**质量场景**捕获质量需求（变更、性能、可靠性、交互性）；含预期与非预期场景。

**案例（航空运营智能管理平台）**：**三基础**——功能分解到可测试的微服务与有界上下文；风格选择为 **数据流（实时/批处理管道）+ 调用返回（分层网关与服务）+ 以数据为中心（湖仓一体）+ 独立构件（消息/RPC）** 的组合；模板复用 **云原生脚手架、监控与日志套件**。商业目标含效率提升 30%、智慧民航政策；质量目标含强一致票务、99.99% 可用、≤1 秒响应；非预期场景如核验接口限流、单 AZ 故障、集中退票洪峰。

### 3.3 六阶段

**口诀**：**许设温敷实验**——许（需求）设（设计）温（文档化）敷（复审）实（实现）验（演化）。

| 阶段 | 要点 |
|------|------|
| 体系结构需求 | 需求来源：质量目标、商业目标；需求过程、需求评审；标识构件 |
| 体系结构设计 | 需求激发设计，多视图表达，迭代 |
| 体系结构文档化 | 输出：体系结构规格说明、测试质量设计说明书；完整、高质量、从使用者角度、**保证最新** |
| 体系结构复审 | 外部人员（用户代表、领域专家）；最小化可运行系统；标识风险、发现缺陷 |
| 体系结构实现 | 按文档组装构件，连接合成，构件与整体测试 |
| 体系结构演化 | 需求变化归类→演化计划→修改/增删构件→更新交互→组装测试→技术评审 |

**案例（航空运营智能管理平台）**：

- **体系结构需求**：标识构件（票务、库存、支付适配、流式告警、离线建模等）与质量场景（峰值 TPS、RTO/RPO、合规审计）。
- **设计**：多视图迭代定稿网关、事务边界、数据分层。
- **文档化**：输出架构规格说明 + 测试与质量设计（压测模型、降级预案），**随版本更新**。
- **复审**：邀请业务/领域专家走查春运峰值与航变预案，最小可运行集可为“购票闭环 + 监控”。
- **实现**：按文档装配容器与中间件，契约测试。
- **演化**：例如新增行李子域或调整检修 AI 阈值→评估影响→增删服务与主题→回归与评审。

---

## 四、软件架构风格（★★★★★）

### 4.1 数据流

- **批处理**：每步独立程序，前一步**整体**结束并**整体传递**数据后下一步才开始；**可并行**弱。
- **管道-过滤器**：前一步**输出即后一步输入**，数据流连接；过滤器+管道；**可并行**。

**区别**：批处理整体传递、前后不一定数据直接关联；管道-过滤器流式、前后输入输出直接相连。

**案例（航空运营智能管理平台）**：**T+1 离线** 日批可视为 **批处理**（日切片落盘后再跑特征与报表）；**实时风控/超售告警** 适合 **管道-过滤器**（采集→清洗→规则/模型→告警→推送），数据在管道中逐步变换。

### 4.2 调用/返回

- **主程序/子程序**：单线程、层次调用，过程调用为连接件。
- **面向对象**：数据与操作封装在对象中。
- **层次型**：每层为上层服务、作下层客户；**相邻层**交互；接口一致可替换。
- **客户端/服务器**：资源不对等；二层=客户应用+数据库服务器+网络；三层=表示层+**应用服务器**（功能层）+数据层。

**案例（航空运营智能管理平台）**：整体为 **多层 C/S 演进**：多终端 + API 网关（表示/接入）→ 应用微服务（业务）→ 分布式数据与检索；购票链为 **同步层次调用**；领域对象封装票价规则与行程状态。

### 4.3 以数据为中心

- **仓库**：中央数据结构 + 对之操作的独立构件；如 IDE、企业数据管理。
- **黑板**：解空间**分级结构**，多知识源协同；如语音识别、模式识别。

**案例（航空运营智能管理平台）**：**仓库**——企业级 **航信主数据、统一订单与指标湖** 作为中心，各服务读写经治理 API；**黑板** 可用于 **复杂航变处置** 或多模型协同的 **调度/排故**（多知识源写入黑板、逐步收敛方案），答题时强调“分级解空间、协同”。

### 4.4 虚拟机

- **解释器**：解释引擎 + 被解释代码存储区 + 当前状态 + 执行进度；灵活、效率较低；适合用户自定义规则、业务灵活组合。
- **规则系统**：规则集、规则解释器、选择器、**工作内存**；专家系统/决策支持。

**案例（航空运营智能管理平台）**：**解释器**——动态定价规则、退改签规则、营销活动脚本可热更新；**规则系统**——风控与超售规则引擎 + 工作内存中的实时库存与航班上下文。

### 4.5 独立构件

- **进程通信**：构件为独立过程，连接件为**消息传递**（点对点、异步/同步、RPC）；分布式、微服务。
- **事件系统**：**隐式调用**，构件注册到事件，事件触发时自动调用；工具集成、一致性约束、语法检查等。

**案例（航空运营智能管理平台）**：**进程通信**——微服务间 **RPC/消息**，契合分布式与弹性伸缩；**事件系统**——`FlightCancelled`、`TicketSold` 等主题 **隐式触发** 通知、库存、检修阈值重算、分析入湖，降低点对点耦合。

### 4.6 其他

- **C2**：构件与连接件均有顶/底；构件顶连连接件底、构件底连连接件顶；构件间不直连；连接件可多连。
- **过程控制（闭环）**：软件与硬件呈反馈循环，输入→输出→环境新状态；空调、定速巡航。

**案例（航空运营智能管理平台）**：**C2**——通过 **网关/消息总线** 等连接件衔接服务，服务不横向直连，便于插拔与审计；**过程控制**——**设备运行参数 → AI/阈值告警 → 检修工单 → 反馈设备状态** 形成闭环（类比温控）。

---

## 五、架构复用与 DSSA（★★★☆☆）

### 5.1 软件产品线

- 一组系统**共享公共可管理特性集**，用**公共核心资产**按规定方式集成开发；核心资产库含架构、可剪裁元素、文档、测试计划等。
- **机会复用**：开发中发现可复用即复用；**系统复用**：开发前规划哪些复用。

**案例（航空运营智能管理平台）**：多条业务线共享 **统一身份、支付适配、消息通知、可观测性、数据同步框架**；立项时即规划 **核心资产库**（脚手架、契约模板、压测场景库），减少烟囱系统重复建设。

### 5.2 复用基本过程

- **获取**可复用资产（可靠、适用、易理解、易修改）→**管理**（构件库：存储、分类、检索、浏览、维护）→**使用**（选择、定制、组装集成）。

### 5.3 DSSA

- **垂直域**：特定系统族，通用软件体系结构；适用于成熟稳定领域。
- **水平域**：多系统/族的功能共有部分；不限于成熟领域。
- **三活动**：领域分析（领域模型）→领域设计（DSSA）→领域实现（可重用信息）。
- **四角色**：领域专家、领域分析人员、领域设计人员、领域实现人员。
- **五阶段**：定义领域范围→定义领域特定元素（词典、共性与差异）→定义设计/实现约束→定义领域模型与体系结构→产生/搜集可重用产品单元。

**案例（航空运营智能管理平台）**：**垂直域**——民航客运运营（航班、舱位、客票、航变）可参考行业参考架构沉淀 DSSA；**水平域**——**支付、消息、检索、权限** 等跨域能力作横向复用；四角色可对应信息中心领域专家、架构师、平台组与实施厂商。

### 5.4 基于构件的软件工程 CBSE（掌握）

- **定义**：基于分布对象技术、强调通过**可复用构件**设计与构造软件系统的软件复用途径。
- **构件（组件）**：自包容、可复用的程序集（源程序或二进制），对外**统一接口**；外部只能通过接口访问，不可直接操作内部。核心特性：**自包容**、**可重用**。

**构件的 5 大特征**

| 特征 | 说明 |
|------|------|
| **可组装性** | 所有外部交互经**公开定义接口**进行，且需支持自身信息的外部访问。 |
| **可部署性** | 自包含，能作为**独立实体**在构件平台运行；**二进制形式**，部署前无需编译。 |
| **文档化** | 完全文档化，用户依文档判断是否满足需求。 |
| **独立性** | 可独立组装、部署；若依赖其他构件需**显式声明**。 |
| **标准化** | 符合某种**标准化构件模型**，确保在 CBSE 过程中规范使用。 |

**构件的 3 种组装方式**

| 方式 | 说明 |
|------|------|
| **顺序组装** | 按顺序调用已有构件，用两个已有构件创造新构件。 |
| **层次组装** | 一个构件直接调用另一构件服务，被调用者为调用者提供所需服务。 |
| **叠加组装** | 两个及以上构件组合创建新构件，合并原功能并提供**新接口**。 |

**接口不兼容的 3 种情况**

| 情况 | 说明 |
|------|------|
| **参数不兼容** | 操作名相同，但**参数类型/个数**不同。 |
| **操作不兼容** | 提供接口与请求接口**操作名不同**。 |
| **操作不完备** | 一构件的提供接口是另一构件请求接口的**子集**，或相反。 |

**案例（航空运营智能管理平台）**：各微服务为 **可部署二进制/镜像**，经 **API 契约** 组装；**顺序组装** 购票流水线各步骤；**层次组装** 票务调库存、支付、核验；**叠加组装** 在基础服务上叠加 **限流、审计、缓存** 并暴露统一北向接口。网关与票务服务 **版本不一致** 时易出现参数/操作不兼容，需契约测试。

---

## 六、质量属性与架构评估（★★★★★）

### 6.1 九大质量属性（面向架构评估）

| 属性 | 描述 | 对策（简） |
|------|------|------------|
| **性能** | 响应能力、处理时间、吞吐量 | 优先级队列、增加资源、减少开销、并发、资源调度 |
| **可靠性** | 错误/异常下维持功能；平均失效时间 | 冗余、错误检测与恢复、异常处理、备份与恢复、负载均衡 |
| **可用性** | 正常运行时间比例；故障间隔/恢复速度 | 心跳、冗余、快速恢复、监控与预警、故障转移 |
| **安全性** | 合法服务、阻止非授权；机密性、完整性、不可否认、可控 | 认证、授权、加密、防火墙、入侵检测、审计日志 |
| **可修改性** | 快速、高性价比变更；含可维护、可扩展、重组、可移植 | 模块化、接口标准化、抽象封装、配置与版本管理 |
| **互操作性** | 与其他系统/环境交互 | 标准协议与接口、数据格式转换、中间件、服务注册发现 |
| **可移植性** | 适应不同运行环境 | 标准 API、抽象层/虚拟机、兼容性测试 |
| **可测试性** | 易于发现缺陷、验证功能与性能 | 分层模块化、测试接口、白盒支持 |
| **易用性** | 好学、好找、好操作、少犯错 | 统一交互与视觉、信息架构、反馈、无障碍、引导与帮助 |

**案例（航空运营智能管理平台）**（与对策表一一对应）：**性能**——优先级队列、弹性扩容、缓存、异步化；**可靠性/可用性**——多副本、健康检查、故障转移、限流降级、备份；**安全性**——RBAC、TLS、加密、审计、WAF、异常登录检测；**可修改性**——模块化与特性开关；**互操作性**——开放 API、对接民航局核验与承运人；**可移植性**——容器化与抽象云资源；**可测试性**——契约测试、全链路追踪；**易用性**——多终端一致体验与运营看板信息架构。

### 6.2 质量属性场景六要素

**口诀**：**原词换纸想读**——原（刺激源）词（刺激）换（环境）纸（制品）想（响应）读（响应度量）。

| 组成部分 | 含义 |
|----------|------|
| 刺激源 | 生成刺激的实体 |
| 刺激 | 到达系统时需考虑的条件 |
| 环境 | 刺激发生的条件 |
| 制品 | 被激励的对象 |
| 响应 | 激励后采取的行动 |
| 响应度量 | 对响应度量的方式 |

**案例（航空运营智能管理平台）**：以 **峰值购票** 为例——**刺激源**：数十万并发用户；**刺激**：提交订单请求；**环境**：国庆高峰 9–11 点；**制品**：票务服务集群；**响应**：扩容、排队、缓存命中、异步出票；**响应度量**：P99≤1 秒、TPS≥5000、错误率阈值。以 **实时告警** 为例——**响应度量**：采集延迟≤1 秒、计算≤2 秒、推送≤3 秒、准确率≥98%。

### 6.3 敏感点、权衡点、风险点

| 概念 | 定义 | 句式/示例 |
|------|------|-----------|
| **敏感点** | 一个或多个构件（及关系）的**特性** | “某设计/协议**将影响**某质量属性” |
| **权衡点** | **影响多个质量属性**的特性；多个敏感点的综合 | 加密级别**同时影响**安全性与性能 |
| **风险点** | 可能导致架构问题或无法满足质量属性的特性/决策 | “如果…，可能…，影响…” |
| **非风险点** | 对架构无负面影响或可接受 | 使用成熟中间件在当前架构下无明显风险 |

**案例（航空运营智能管理平台）**：

- **敏感点**：“采用 **同步分布式事务** 将 **影响** 购票延迟与吞吐”。
- **权衡点**：“**TLS 全链路 + 字段级加密** **同时影响** 安全性与性能、运维复杂度”。
- **风险点**：“若 **外部核验** 超时无降级，**可能** 导致购票失败率飙升”。
- **非风险点**：“采用成熟 **消息队列** 做异步出票在团队技能匹配前提下风险可控”。

### 6.4 ATAM（★★★★★）

- **目标**：对**性能、可用性、安全性、可修改性**在开发前评价与折中。
- **四阶段**：① 场景和需求收集；② 体系结构视图和场景实现；③ 属性模型构造和分析；④ 折中（识别敏感点、权衡点）。
- **实践四阶段**：演示→调查和分析（含上述四步）→测试→报告。
- **效用树**：树根→**质量属性**→属性分类→**质量属性场景**（叶子）；对场景按重要度与实现难易度标 H/M/L，形成优先级对（如 H,L）= 重要且易实现。
- **ATAM 关注**：**需求**（出发点与终点）。

**案例（航空运营智能管理平台）**：**效用树** 根为“航空智能运营平台”，子节点为 **性能/可用性/安全性/可修改性**，叶节点为具体场景，如“春运峰值仍满足 5000 TPS”“单区故障 RTO 内切换”“审计日志防篡改”“新增销售渠道不改核心票务”。对叶节点打 **(重要性, 实现难度)**，优先论证 **H/L** 场景。**四阶段**对应：收集春运与航变场景→对照逻辑/进程/部署视图→用压测与 FMEA 支撑属性模型→讨论 **事务 vs 最终一致**、**加密强度** 等折中。

### 6.5 SAAM 与 CBAM

- **SAAM**：场景技术；主要关注**可修改性**；场景开发、架构描述、单场景评估、场景交互、总体评估。
- **CBAM**：按**投资回报率 ROI** 选择架构策略；质量属性变化带来效用。

**案例（航空运营智能管理平台）**：**SAAM** 可评估“引入新退改引擎”对 **可修改性** 与模块交互的影响；**CBAM** 可比较 **多活机房** vs **单活+冷备** 的运维成本、停机损失与建设投入，辅助决策。

---

## 七、速记清单（考前扫一眼）

1. **4+1**：罗凯进五常 / 落实进步用 / 逻实进配。
2. **ABSD**：三基础（功能分解、风格选择、软件模板）；六阶段许设温敷实验；质量场景=变更/性能/可靠性/交互性。
3. **风格**：批处理整体传递 vs 管道-过滤器流式；仓库中央数据+独立构件 vs 黑板分级解空间；解释器=引擎+存储+状态+进度；进程通信=显式消息 vs 事件=隐式调用；C2 顶底连接规则。
4. **复用**：产品线=核心资产+规定方式；三阶段获取→管理→使用；DSSA 垂直/水平、三活动四角色五阶段。
5. **九大属性**：性能/可靠性/可用性/安全性/可修改性/互操作性/可移植性/可测试性/易用性及对策。
6. **场景六要素**：原词换纸想读。
7. **敏感点**：构件特性→影响某属性；**权衡点**：影响多属性；**风险点**：可能…影响…。
8. **ATAM**：四属性（性能、可用性、安全性、可修改性）；四阶段（场景需求→视图与实现→属性模型与分析→折中）；效用树=质量属性→分类→场景；关注需求。

---

*文档由《04-系统架构设计》提炼，保留必要表格与图形含义，便于背诵与考场回忆；案例与《考纲/航空运营智能管理平台项目素材-核心.md》一致，可按需裁剪写入论文。*
